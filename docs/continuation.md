# Continuation — rolling work state

What's happening now + what's next. `AGENTS.md` is the stable companion.

## Current phase

**Phase 3 — verification harness + first slices.** Phase 2 closed 2026-04-18 after three red-team passes (F1–F84). Architecture in `docs/architecture/` (split from a monolith into one file per top-level section on 2026-05-30 for maintainability; `README.md` is the index, and section numbers are stable so `§N.M` citations resolve unchanged). ADRs 0001–0040 Accepted (0004 + 0005 superseded by the Web UI surface-architecture cluster 0027–0033, 2026-05-30; **0031 superseded by 0038**, 2026-05-31; **0040 extends 0024** with the tenancy/IA model — Org → Space *hard-ceiling* → Collection/Doc per-doc ACL — 2026-06-01).

> **Resumed 2026-05-29** after a ~5-week pause. Authoring model upgraded Opus 4.7 → **Opus 4.8 (1M context)**. The from-scratch **Web UI surface-architecture decision is settled** — ADR cluster **0027–0033, Accepted 2026-05-30** (superseding 0004 + 0005). Immediate focus shifts to **implementation** of the Web UI surface (see *What's next*).
>
> **Authoring model upgraded again 2026-06-11: Opus 4.8 → Claude Fable 5** (first Claude 5-family model, Mythos-class tier above Opus). AGENTS.md identity + commit trailer updated forward-only; every prior commit/ADR keeps its original attribution.
>
> **Web UI surface-foundations cluster — DECIDED (ADR 0036–0039, 2026-05-31).** Before building the shell, four foundations were settled with a brand workflow (12 design explorations, `wf_b3a17378-0fb` / `wf_545b3708-b04`) + a research-and-adversarial-verification workflow (`wf_734a602e-3d6`, every library pin checked against live npm/specs 2026-05-31, which corrected several draft claims). **0036** — brand **Meridian Zero** (cold-Swiss; ultramarine accent + agent-cyan; AA-hardened token SSOT at `docs/brand/v2/meridian-zero.css`). **0037** — design system **Base UI** (`@base-ui/react@1.5.0`, zero-CSS headless; *not* Mantine/ShadCN; `date-fns`/`@date-fns/tz` are **optional** peers, uninstalled in v1 — corrected 2026-06-03, ADR 0037 Amendments) + a three-layer design-token architecture (primitive→semantic→component) where curated **and** user-authored themes are pure `:root` overrides, no rebuild. **0038** — owned editor adopts **Tiptap v3 directly** (drops the 0031 BlockNote bootstrap), with a DOM-free JSON-in server write path that removes the `happy-dom` shim from production (improves 0018). **0039** — installable **PWA**, **offline-read-only** in v1 (offline-write would violate invariants 3+5), responsive 3-pane collapse, and the iOS standalone-auth verdict (Lax cookie holds; one-time re-login expected). Now → Web UI shell scaffold (task #13).

Thirty-eight capabilities land end-to-end at the kernel layer and across three surface adapters — the Hono API trunk, the CLI (`ez`), and MCP (`/mcp`). The Web UI (`apps/app`, served by `apps/server`) is building out cell-by-cell: a capability declares `ui` only once its browser binding is proven (`doc.list`/`doc.create`/`doc.get`/`doc.update`/`doc.rename`/`doc.delete`/`doc.publish`/`doc.unpublish`/`space.list`/`workspace.get`/`collection.list` today); the rest sit in the shrink-only `UI_PENDING` ledger (`packages/contract-tests`). The set: `doc.{list,create,get,publish,unpublish,delete,restore,rename,update,move,add_guest,remove_guest}` (12), `collection.{create,list,update,delete,restore,move}` (6), `workspace.{get,update,member_add,member_list,member_remove,member_update_role}` (6), `audit.{list,get}` (2), `permission.{grant,revoke,list}` (3), `space.{create,update,archive,restore,get,list,member_add,member_remove,member_update_role}` (9). Write-path single-tx atomicity is **closed for both lanes**:

- **Content mutations** (P3.6): `HocuspocusSync` persists `doc_updates` inside the dispatcher's write-path tx; `BoundSyncService.rollback` evicts the resident Y.Doc on abort; `packages/dispatcher/prop/writepath-atomicity.test.ts` sweeps every in-tx query position across cold + warm paths (32 tests) asserting the five-row commit is all-or-none.
- **Metadata-only mutations** (2026-04-21, 4032c55 + d6a82d5): `ctx.outbox` un-stubbed at the trunk composition root — handler emits queue and flush via `createOutboxWriter` inside the dispatcher's `withSystemTx`; `doc.publish` / `doc.unpublish` emit `doc.publish_changed` (renamed from `doc.visibility_changed` at the Step-5 split, 2026-06-12) for render/cache invalidation (§5.4, F5). `packages/api-server/prop/metadata-only-atomicity.test.ts` (9 tests: 1 baseline + 8-ordinal N-way crash-fuzz) exercises the **real** `createApiDispatcher` via a driver-wrap that layers a Kysely query-fault plugin; reject arm verifies the publish pair stays NULL with `render_version` unchanged, `allowAuditCount === 0`, `publishChangedOutboxCount === 0`, and the error-audit survives in a separate tx; the baseline pins the 6-query tx shape with the slug-mint SELECTs *inside* (the Postgres mint-race guard). Closes Appendix C item 16; flips §6.5 + §10 from "design intent, not landed" to CLOSED.

AGENTS.md invariant 6 (soft-deletes recoverable via first-class capability) is reachable through the API.

## What's next

Three of the four surfaces are **landed** — the Hono API trunk (`packages/api-server`), the CLI (`apps/cli`, compiled `ez` binary), and MCP (`/mcp` on the trunk). The Web UI is the one unbuilt surface; its **architecture is now decided** (ADR 0027–0033, Accepted 2026-05-30) and implementation is the immediate focus.

**Web UI surface-architecture — DECIDED (ADR 0027–0033, 2026-05-30).** Redone from scratch via a 36-agent exhaustive review + 5 red-teamers + a cross-model Codex ADR pass; superseded ADR 0005 (Next.js) and ADR 0004 (BlockNote-as-final). The shape:

- **Topology (0027):** the Hono trunk is the top-level server (`@hono/node-server` **v1** — `getRequestListener` + raw `ws`; deliverable #2 retired the earlier "v2 `serve()` prerequisite" framing) — Vite + React SPA as static assets, JSON-RPC + `/auth` + `/mcp`, event-rendered static published HTML, embedded Hocuspocus on one port. No Next.js (`output:export` ⊥ `standalone`; Next can't host the collab WS).
- **Routing (0028):** TanStack Router; one typed-client seam (`packages/api-client`); raw `hc<AppType>` forbidden outside it.
- **API shape (0029, substrate reversed 2026-05-30; MIGRATED 2026-05-30):** **code-first** per-route factories — `hono-openapi` `describeRoute` + `validator` on `hono/factory` handlers, composed via `.route()` chaining (the `openapiRoutes([...] as const)` tuple is **retired**; `tsc` spikes showed `.route()` preserves the `hc` merge). Capability schemas reused directly at `validator` from `@editorzero/schemas` (SSOT, ADR 0034 — no restated route schema); typed error arm = explicit per-route `c.json` returns. **Landed** (see Recent history 2026-05-30): all routes + 5 domain index sub-apps + the `app.ts` trunk are code-first; `@hono/zod-openapi` dropped. Registry-driven *generation* of the route files is still future (the files are hand-maintained today, parity-guarded by the CLI↔server test).
- **Auth (0030):** Better Auth in-trunk, same-origin → `SameSite=Lax`, zero framework adapter.
- **Editor (0031 → superseded by 0038):** adopt **Tiptap v3 directly** + an owned thin block layer (clean-start), fused with track-changes (0032). DOM-free JSON-in server write path removes the `happy-dom` shim from production. (0031's BlockNote-bootstrap-then-eject sequencing is reversed; its owned-Tiptap + clean-start reasoning carries forward.)
- **Version history + track-changes (0032):** build ourselves on Yjs snapshots + `prosemirror-changeset` (restore appends a corrective update; anchors are Yjs `RelativePosition`; tracked *agent* edits are the differentiator).
- **Testing + RPC (0033):** typed error envelope at the route boundary, real-browser editor tests, `ui` parity cell fails the build.

**Tenancy & IA model — DECIDED (ADR 0040, 2026-06-01).** Model B: **Org** (= the deployment, conceptual; `workspaces` stays the tenant root) → **Space** (membership boundary, *hard ceiling*; a new `spaces` table *within* the workspace — **no rename**) → **Collection/Doc** (per-doc ACL via one polymorphic `grants` table; Collections are pure-nav, no ACL). Plus **Personal** spaces (auto-seeded at signup), item-scoped **guest grants** (the only widening past the ceiling), **publish** as an independent dimension, and **agents as peer grant-targets**. who-can-read(X) = **mode-select-then-union**: a doc's `access_mode ∈ {space, private}` (the de-overloaded successor to `visibility`) selects the baseline term, unioned with explicit + guest grants — **positive-only roles, no deny rows** (narrowing is a doc-level mode switch, not a per-member subtraction) — a local, audit-reconstructable lookup, never a graph walk. **Extends ADR 0024** (cashes out its reserved org + teams axes); the ADR is explicit that the enforcement machinery it leans on (Postgres RLS, the audit-replay engine, the four-way contract-tests harness, the `acting_as` scope-intersection) is **unbuilt today** — for the existing model too — and sequences each as a hard gate. **The one hard ordering against the #13 shell: the vocabulary lock** — UI label "Space" / client route `/spaces`, API path stays `/workspaces` — must precede the scaffold's sidebar/route tree; the reader-path slice (item 4) folds in Model B's publish-orthogonality. **No MVP cut — one complete first release; the steps in ADR 0040 §Evolution are a build *order*, not a scope.** **RLS decided (per @numman): not a committed layer** — multi-tenant SaaS is a product non-goal, and `workspace_id`-keyed RLS can't enforce the cross-Space boundary anyway; the app-layer floor (resolver-as-sole-read-path + Layer-2 + the cross-tenant/ceiling fuzzer, both backends) is the enforcement design. **Codex cross-model review folded in** (`4f935f1`): kept Model B + all forks, independently converged on resolver-as-sole-read-path, and surfaced the narrow-from-baseline gap (now resolved via the `access_mode` mode-switch — fork #5). Source-of-record visual: [`docs/ia/tenancy-models.html`](ia/tenancy-models.html).

**ADR 0040 build-order status (2026-06-12):** step 2 is **COMPLETE** — both the **replay engine *contract*** *and* the **integration property test (#23)** have landed. The contract first landed at `d0fd16c` (`reducer.ts` + `PersistentWorkspaceState` + `REPLAY_CLASS` over all 68 effect kinds). Codex's independent effect→state pass (#24) flagged four reconstruction bugs, all fixed-forward in **`3f1e436` (#25)** under one principle — *an effect carries every handler-computed value; replay never infers it from the envelope principal or a DDL default*: `created_by` (HIGH 1 — agent writes attribute to the human behind the agent, not the agent principal), `trash_retention_days`+`settings` (HIGH 3), and `deleted_at` as the **handler clock** (HIGH 4, the ADR 0017 recovery anchor, distinct from the audit-row clock). An adversarial review workflow then surfaced a **fourth instance of the same class** — `doc.rename` dropped `docs.slug` from the projection (handler re-derives + writes it, effect carried only title) — closed in the same commit, parallel to `collection.update`. Same commit adds a **per-kind forcing function** (`STATE_KIND_FIXTURES satisfies Record<StateKind,…>` + `it.each`) so "every `state` kind has a transition" is a compile-**and**-runtime gate, not prose. **HIGH 2** (genesis/bootstrap emitted no audit) is **LANDED (`c146d28`, #26, ADR 0041)**: the two `driver.system()` inserts in `create-auth.ts` now run inside `driver.withSystemTx`, emitting `workspace.create`+`member.add` through the **shared `AuditWriter`** (row shape + `outbox(audit.appended)` fan-out single-sourced) under a `system.workspace_bootstrap` **non-dispatchable** `capability_id` marker (SSOT `SYSTEM_AUDIT_CAPABILITY_IDS` in `@editorzero/scopes`; coherence **Check 5b** enforces disjointness from real capabilities — an allowlist, not a bypass). An auth integration test proves the two genesis rows **replay** to the live `workspaces`+`members` projection (invariant 3 closed for signup). The **integration property test (#23)** — real dispatch → replay → DB compare — **LANDED (`b98463a`)**: it drives all 16 shipped state-class capabilities through the real dispatcher, captures the typed audit records via a recording `AuditWriter`, and deep-equals `replay()` against a live-DB projection after every step — the test that would have caught the `doc.rename` slug drop, proven by a negative control. **Step 3 (additive foundations) is COMPLETE (`4a54d9e`, 2026-06-12)** — `SpaceId`/`GrantId` brands, `GRANT_ROLES`/`ACCESS_MODES`, the reserved Step-8 metadata-only ids (+ architecture §6.5 lockstep), `GrantRoleSchema`/ID schemas/`PrincipalRefSchema` (internal-only, H12), and the **Check-7 index-regex fix (H5)** pulled forward from Step 4 — the five partial-unique slug indexes now participate in SQLite↔Postgres parity (negatively verified). **Step 4 (the `spaces`/`space_members`/`grants` schema slice) is COMPLETE (2026-06-12)** — three lockstep files at one commit, `TENANT_SCOPE_COLUMNS` registration + a compile-time `satisfies Record<keyof Database, …>` lockstep + coherence **Check 11** (schema↔DDL↔scope-map, all legs negative-tested), and the §9.1 tuple extended in the same commit (see Recent history; dated amendment on the ADR records the landing decisions). **Step 5 (the reader-path slice) is COMPLETE (2026-06-12)** — the `visibility` → `access_mode ∈ {space,private}` + `published_slug`/`published_at` de-overload split across schema/DDL/schemas/audit/capabilities and every surface; `render_version` rename; `doc.publish_changed` outbox rename; mint-with-collision-suffix inside the write-path tx; idempotent re-publish keeps URL + original `published_at`; **delete clears the publish pair / restore never republishes**; replay walk extended (re-publish + delete-while-published transitions); the atomicity prop re-pinned at the 6-query shape. The **outbox→render consumer + `(public)` route stay deferred** (need §12 jobs runner, §7 markdown projection, an anonymous principal — `doc.publish_changed` is the seam); per H9, **no Space-scoped listing shipped** — `doc.list`/`doc.get` stay resolver-ungated. Dated amendment on the ADR records the landing decisions. **Step 6 (`workspaceAwareGate` + ceiling resolver, the BLOCKER-class slice) is COMPLETE (2026-06-12)** — the H8 `acting_as` ∩ delegator intersection live at the composition root (new structural deny `delegator_not_member`); `loadDocReadResolver` (capabilities `acl/ceiling.ts`, sole read authority, F88 `acl_deny` channel) gating `doc.get`/`doc.list` + all seven doc mutations; NULL-space = workspace-legacy baseline so the machinery landed with **zero observable narrowing** (191 pre-existing doc-handler tests untouched); no workspace-role bypass (an OWNER 403s, proven end-to-end); resolver-grade §8.1a fuzzer (oracle-equality + H6 + privacy, mutation-tested at landing); metadata-only tx pin 6 → 10 (in-tx ceiling preload). Codex + frontier red-team reviewed before landing — Codex's two HIGHs (doc.create placement hole; doc.move cross-bucket widening) fixed pre-commit via the resolver's placement-authority API (`canPlaceIn` gate on create; bucket-transition rule on move) and its MEDIUM closed by H8 composition-seam tests; dated amendment on the ADR records the decisions incl. the Step-7/8 obligations (collection ceiling, the Step-8 move ladder + `acl_transition` replacing today's blanket cross-bucket prohibition, space_id denormalization, ACL-audit-replay, the audit-read side-channel decision, and the agent-auth slice's delegation obligations — scopes-vs-rows asymmetry recorded). **Step 7 (the ACL effect family) is COMPLETE (2026-06-12)** — three commits, one family each per the step text: the space family (`space.create/update/archive/restore`, `space.member_add/remove/update_role`), the grant family (`acl.grant`/`acl.revoke` reshaped to mirror `GrantState` — legitimate, the kinds were `deferred` and never emitted — + `{space_id}` joined the `acl_deny` scope union), and the placement lockstep (`doc.move` optional `acl_transition {policy, before/after_space_id, dropped_grant_ids}` keeping cross-boundary moves ONE audit row; `collection.create/move` effects += `space_id`/`new_space_id` with `CollectionState.space_id` joining the §9.1 projection IN THE SAME COMMIT — the Step-4 lockstep rule, now closed; reducer rebinds whole subtrees from one value via the denormalization invariant). All nine kinds born `"state"` with transitions + total-record fixtures + semantic walks — the replay-sufficiency proof lands BEFORE any capability can emit them; capability outputs echo the new fields through the ADR 0034 SSOT schemas; replay-prop DB projection covers `collections.space_id`. Dated amendment on the ADR records the landing decisions. **Step 8 slice 1 (the full `permission.*` family — the first ACL capabilities + first `acl.grant`/`acl.revoke` emitters) is LANDED 2026-06-12** (three commits: the granting-authority ladder on the ceiling resolver; `permission.grant`/`permission.revoke` end-to-end across schemas/registry/routes/CLI/MCP + the replay-property extension closing the deferred ACL-audit-replay obligation; `permission.list` with the two-rule visibility gate; dated ADR amendment records the ladder, the user-subject standing rule + agent exemption, upsert semantics, the verb-asymmetric trash postures, and the list visibility rule). **The promised Codex slice review RETURNED + is FOLDED IN (same-day fix-forward, 2026-06-12)**: his MUST-FIX (anomaly-placed docs refuse EVERY grant, repair-first — closes the unmarked-ceiling-crossing window we both flagged independently) and SHOULD-FIX (`permission.list` tightened to administer-tier on both kinds — the panel is the sharing graph, not content) applied + regression-pinned; two NOTEs recorded in the ADR's dated review entry (agents-table existence obligation sharpened; trashed-space revoke wait intentional — `workspace.member_remove` is the offboarding kill-switch). **Step 8 slice 2a is LANDED 2026-06-12** — `space.create`/`space.update` + the signup Personal-space seeding (third bootstrap audit row; `spaces_personal_unique`-keyed idempotency via `@editorzero/db`'s `onConflictPersonalSpaceDoNothing`), the new `space:manage` scope (`space.create` excepted to `workspace:admin`), personal type/baseline pins, team-only minting, `/spaces` as the seventh trunk domain — plus the slice-1 `/permissions` middleware wiring fix with a structural 401-never-500 walk over every mounted domain prefix (see Recent history). **Step 8 slice 2b is LANDED 2026-06-12** — `space.archive` (refuse-on-live-descendants with typed counts; grants ride through untouched) + `space.restore` (the `canRestoreSpace` dead-row ladder — the ONE sanctioned trash-authority path on the ceiling resolver; sibling-slug + personal-twin preconditions), plus a fix-forward the slice surfaced: `collection.restore`/`doc.restore` restored blind into PARTIAL slug indexes (raw UNIQUE-violation 500 when a live sibling usurped the slug) — both now pre-check → typed `SlugCollisionError` (see Recent history). **Step 8 slice 2c is LANDED 2026-06-12 — the space slice is COMPLETE** (`space.member_add/remove/update_role`: membership-IS-baseline-reach, personal-roster pin with the deliberate add-refuses/remove-repairs asymmetry, subject-standing rule, hard-DELETE preimage postures, no-last-owner-guard-by-design, P3 param+body routes — see Recent history). **The promised Codex whole-space-slice review RETURNED + is FOLDED IN (same-day fix-forward, 2026-06-12)**: his MUST-FIX (restore paths could mint live descendants under an ARCHIVED space via the inverse path — `collection.restore`/`doc.restore` now require the bound space LIVE; `ParentDeletedError.parent_kind` widened to `"collection" | "space"`, wire code unchanged), SHOULD-FIX (`member_update_role` gained the post-authority personal refusal — it would have MUTATED corrupt personal roster rows; remove stays the one kind-blind repair verb), and NOTE (the dead-row restore ladder dropped the owner-role membership rung — archive refuses while ANY roster row exists, so membership on a dead team space is corrupt and corrupt state must not confer authority; ladder body parameterized so live/dead share code without sharing the term) all applied + regression-pinned, no rebuttals; dated ADR review entry records the dispositions + his explicit keeps. **The space read family (`space.get`/`space.list`) is LANDED 2026-06-12** — the first Space-scoped reads; visibility = baseline reach ∨ administer (admin-honesty term; personal owner-only incl. against admins; agents grant-only; closed-vs-private distinction RESERVED for join-request flows), trash-invisible, `space.list` unpaginated by design (`name ASC, id ASC`); Appendix A rows landed under the coherence drift check (see Recent history). **The guest family (`doc.add_guest`/`doc.remove_guest`) is LANDED 2026-06-12** — design settled with Codex BEFORE building (all five decisions concurred, his two SHOULD-FIXes built in: the typed `GrantLifecycleConflictError` lane conflict in BOTH directions — `permission.grant`'s guest-edge 409 flipped to it too — and `doc.remove_guest`'s three distinct edge dispositions); `doc.list_grants` DROPPED, not deferred (administer-gated `permission.list(resource_kind=doc)` already covers the panel — an alias would be a drift surface); remove_guest works on TRASHED docs (else guest edges in trash are immortal — revoke refuses them); add_guest skips subject standing BY DESIGN and stays available mid-anomaly (owner-tier-bounded — the slice-1 MUST-FIX reserved exactly this verb for recovery sharing); guest `owner` unmintable by schema (`GuestGrantRoleSchema` = `BASELINE_ACCESS_ROLES`); subject-existence debt recorded for BOTH subject kinds; replay walk extended with the guest lane incl. a surviving never-a-member edge (see Recent history). **The `doc.move` cross-boundary branch is LANDED 2026-06-12 — the sole `acl_transition` emitter; Step 6's blanket same-bucket rule RETIRED** (design settled with Codex pre-build — all five decisions concurred; his MUST-FIX — `canPlaceIn(target)` never waived, even for owner-tier anomaly repair — pinned by test; his SHOULD-FIX upgraded: `dropped_grants` full preimages REPLACING ids; his extra test pinned: private + adopt_baseline ends zero-grant AND still unreadable to destination members — adopt is "shed crossings", not "set access_mode=space"; see Recent history + the dated ADR amendment). **The full §8.1a capability-matrix × dual-driver fuzz is LANDED 2026-06-12 — Step 8 is COMPLETE** (capabilities + verification): `packages/api-server/prop/tenant-isolation.test.ts` drives the production composition over both drivers with one-sided authority oracles, exact visible-set equality, tenant-B bit-identity, and cross-backend outcome-digest equality; anti-vacuity floor + deterministic canaries; mutation-tested (see Recent history + the dated ADR amendment). **The space-collection family slice 1 is LANDED 2026-06-12** (design settled with Codex BEFORE code, as promised — concurrence on all three questions, his SHOULD-FIX built in, both test pins landed): `collection.create` gains optional root-only `space_id` (existence-404 → the new `assertCanPlaceInSpace` resolver term — agents do NOT ride the open-space user baseline), child creates INHERIT the parent's stored binding behind `assertCanPlaceIn` (derivation, not input — mutual exclusion is a schema rail), the replay-walk crossing deferral is CLOSED (legacy→space→legacy under both policies), and the §8.1a fuzz matrix grew 18 → 19 verbs with new canaries, mutation-tested (see Recent history + the dated ADR amendment). **The `collection.move` crossing branch is LANDED 2026-06-12 — the space-collection family is COMPLETE** (design settled with Codex pre-build; his MUST-FIX tagged destination union `legacy_root | space_root | collection` REPLACED `new_parent_id` in the input shape; per-doc `assertCanAdministerDoc` sorted by `doc_id`; one audit row with full dropped-grant preimages; the slice-1 `cross_bucket_move_unsupported` bridge rail RETIRED; CLI grew JSON-valued object flags; fuzz matrix still 20 verbs but the move arm now draws the destination union + a crossing canary — see Recent history + the dated ADR amendment). **Next on this track: onward Step-9+ build order per the ADR.** This track runs **parallel to** the Web UI shell (#13 below): the replay engine gates ACL capabilities, the shell gates only on the vocabulary lock.

**Implementation sequence (first deliverables):**

1. **Composition root + boot entrypoint — LANDED 2026-05-30.** `getApiApp()` (`packages/api-server/src/server.ts`) constructs the stack and `createApiApp` assembles the trunk; the `serve()` production entrypoint is `apps/server` (the `editorzero-server` bin). `createApiApp` is no longer invoked only in `app.ts` + tests — editorzero boots as a server. The 0029 code-first route chain, the 0030 auth instance, and the dispatcher wiring converge here. The **materialized typed-RPC precompile** (`packages/api-client/src/client-type.ts`) **landed** too. SQLite-only at the root today (ADR 0027 floor); Postgres `DATABASE_URL` fails loud (ADR 0007 dual-backend deferred). See Recent history.
2. **Co-hosting + auth smoke — LANDED 2026-05-31.** Proven on `@hono/node-server` **v1** (no v2 bump): the HTTP trunk + `serveStatic` SPA assets + the `/collab` WS upgrade co-host on one owned `http.Server` (raw `ws` `noServer` + `server.on("upgrade")` → `hocuspocus.handleConnection`), with cookie **authN at the upgrade** (the shared `resolver`, now exposed on `BootedApp`) and per-document **authZ at `onAuthenticate`** (tenant-scoped doc lookup; throw → Hocuspocus permission-denied). `apps/server/src/cohost.integration.test.ts`. The **static mount is now production-folded** (`attachSpa` in `apps/server/src/spa.ts`, 2026-06-12 — see Recent history); **the WS mount is now production-folded too (2026-06-12, task #15 — see Recent history + the ADR 0030 dated amendment)**: `attachCollab` mounts on the serve layer through `startServer`'s attachment seam, WS clients enter the dispatcher's *own* embedded Hocuspocus (`HocuspocusSync.handleWsConnection` — one shared instance, so HTTP-transact broadcasts reach attached clients), and **all four ADR 0030 production-attach blockers are CLOSED**: universal `readOnly` (invariant 3 — no WS write until the audited write lane), normalized `Origin` allow-list (absent Origin refuses on the cookie path), per-Auth-frame principal re-resolution (revocation freshness; invariant-5 reuse of `effectiveScopes` + the Step-6 ceiling's `assertCanRead`), and `onAuthenticate` registered by construction with a deny-all default. Rollback with WS clients now force-evicts the resident `Y.Doc` (proven-eviction-or-poisoned-fail-closed). Still future for *production* collab (slice B, in order): the **audited WS write lane**, **broadcast-after-commit** (a pre-commit broadcast can still leave a phantom delta client-side until resync — recorded residual), role-aware write standing, bearer/agent WS auth (H8-aware), event-driven revocation closes.
3. **SPA bootstrap + shell scaffold (task #13) — STARTED; skeleton landed 2026-06-02 (`5112801`).** Vite + React + TanStack Router; sign-in + the `doc.list × Web UI` parity cell. **Increments in:** the `apps/app` Vite 8 + React 19.2.6 skeleton + the ADR 0035 §2 reserved-prefix dev-proxy contract (`5112801`); then **pnpm 10.34.1 + file-based TanStack Router + the §5 release-age cooldown** (`6fdc1a9` + `65cf281`, 2026-06-03 — see Recent history). All green on build/typecheck/test. **Next #13 sub-steps:** ✅ token cascade + theming runtime (0036/0037, `0786ad1`); ✅ api-client transport + SPA data-layer (react-query, `8cc124d`+`1c103d3`); ✅ auth guard + bare authed shell frame (`_authed` pathless layout + `AppShell`, zero-dep, `389844d`); ✅ sign-in + sign-up screen over the Better Auth seam (`5b5307e`); ✅ Playwright + axe e2e harness (`packages/e2e`, `4fff0ac`); ✅ `doc.list × Web UI` parity cell + `packages/contract-tests` + honest `surfaces` narrowing (`eb97cfe`); ✅ Base UI chrome — responsive nav + mobile Drawer, first `@base-ui/react` import (`0e371e3`); editor (0038) **Slice A landed 2026-06-12** (owned `@editorzero/blocks` layer + DOM-free write path; BlockNote/happy-dom evicted — see Recent history); ✅ editor **Slice B landed 2026-06-12** (`/doc/$docId` route — singular, `/docs` is a reserved API prefix — + `doc.get`/`doc.update` ui cells; task #34 complete); ✅ **PWA layer landed 2026-06-12** (ADR 0039: injectManifest SW + SSOT-derived denylist + prompt updates + install UX; e2e now builds + trunk-attaches the SPA — task #35); ✅ **`space.list × Web UI` cell landed 2026-06-12** (the Spaces screen at the SINGULAR `/space` — `/spaces` is the trunk's API domain, the `/doc` precedent; sidebar "Spaces" entry; preceded by the reserved-prefix SSOT fix-forward — see Recent history); ✅ **`workspace.get × Web UI` cell landed 2026-06-12** (the sidebar workspace IDENTITY block — name + slug + monogram on every authed screen, warmed in the `_authed` layout's `beforeLoad`; an identity block, not the mock's switcher: the deployment IS one workspace); ✅ **`collection.list × Web UI` cell landed 2026-06-12** (the sidebar Collections tree — read-only rows, client-side DFS assembly of the flat wire order; the LAST of ADR 0040's three named shell structures); ✅ **`doc.create × Web UI` cell landed 2026-06-12** (the docs panel's "+ New doc" inline title form → navigates into the new doc's editor; 409 sibling-slug arm surfaced as a typed alert); ✅ **`doc.rename × Web UI` cell landed 2026-06-12** (the editor toolbar's clean-canvas-gated Rename control — and its e2e flushed out a latent capability bug: `doc.rename` had NO sibling-slug pre-check, so a collision bubbled a raw UNIQUE violation as 500 on every surface; fixed at the capability with the self-excluded mirror of `doc.create`'s pre-check); ✅ **`doc.delete × Web UI` cell landed 2026-06-12** (the editor toolbar's morph-confirm Trash control; the Trash *screen* is blocked on a trash-listing capability gap — recorded in the punch list); ✅ **`doc.publish`/`doc.unpublish × Web UI` cells landed 2026-06-12** (the doc header's publish toggle — minted `published_slug` as status text; the public reader route stays the later slice) (+ chrome grows per-cell: nav entries with their screens; drag-reorder per the ADR 0037 Owned Tree note binds collection.move/doc.move in a later cell). **Scaffolding gated by ADR 0035 (Accepted 2026-05-31):** `apps/app` / `@editorzero/app`, Vite dev server + same-origin reverse proxy to the trunk (preserves 0030's Lax/no-CORS in dev), file-based routing, `apps/app/dist`→`serveStatic`, exact-pin + committed-lockfile + pnpm `minimumReleaseAge` cooldown (prereq: pnpm 10.0.0→current 10.x). **The editor/design/PWA pins in 0035 are amended by the 0036–0039 foundations:** build on **Base UI** + the three-layer design tokens (0037, default theme = Meridian Zero / 0036), the owned **Tiptap v3** editor (0038, *not* BlockNote/Mantine), and layer the **PWA** (0039, `vite-plugin-pwa` injectManifest, denylist derived from the 0035 reserved-prefix SSOT). The `@base-ui/react` `date-fns`/`@date-fns/tz` peers are **optional** (`peerDependenciesMeta.optional`) — install `@base-ui/react` alone; they enter only if a calendar primitive (none in v1) needs them (corrected 2026-06-03, ADR 0037 Amendments). Mind the Hocuspocus 3.4.4→4.1.0 version decision (0038).
4. **Reader-path slice — DATA MODEL LANDED 2026-06-12 (ADR 0040 Step 5; see build-order status above).** The split + publish machinery shipped; still open from this item: the outbox→render consumer (ADR 0014 queue; blocked on the §12 jobs runner), the neutral block-JSON→HTML projection + fixture gate, the head-metadata contract, the anonymous-principal design, and the publish-snapshot-vs-live product decision. (Space-scoped `doc.list`/`doc.get` *gating* waits for the ceiling resolver — ADR 0040 Step 6.)

**Current-code follow-ups (Codex-found, ADR 0029/0033):** (1) several `doc.create`/collection routes advertised any-UUID vs the capability's UUIDv7 — **RESOLVED 2026-05-30**: routes reuse the capability's `@editorzero/schemas` schema directly at `validator`, so no restated route schema can over-advertise (UUIDv7 pattern survives on the `z.input`/wire side into the OpenAPI doc); the parked manual hardening is obsolete. (2) **RESOLVED 2026-05-30** — `doc.create` previously could leak a raw DB unique-violation (`internal`/500) instead of a typed 409. Closed by adding the NULL-aware sibling-slug pre-check to the `doc.create` handler (mirrors `collection.create`): a SELECT scoped to `collection_id IS NULL` / `= collection_id` + `slug` + `deleted_at IS NULL` (matching the `docs_root_slug_unique` / `docs_nested_slug_unique` partial indexes) throws `SlugCollisionError` before the INSERT; the partial indexes remain the race-window backstop. The route `describeRoute` now declares the 404 (bad `collection_id`) + 409 (`slug_collision`) arms it could already return at runtime. 5 capability unit tests + 1 full-stack auth-chain e2e (two creates, same title → 409 + create-allow/create-error audit trail).

**CLI dogfood punch list** (2026-04-22; deferred behind API/capability coverage; convergent across two dogfood subagents):

- **Capability-level gaps** (missing from registry → affect every surface by invariant 4):
  - No `auth.signup` capability — signup is a Better Auth HTTP endpoint only. Backlog until a slice commits to designing it.
  - `doc.create` has no content-on-create path (no `--body`, stdin, or initial-block-set input).
  - **No trash-listing capability** (2026-06-12, found by the `doc.delete` ui cell): `doc.list`/`collection.list` exclude trashed rows with no `include_deleted`-style input, so nothing can enumerate a workspace's trash — the browser Trash screen (and a CLI `ez docs list --trashed`) is blocked on it. Needs design, not just a flag: trash visibility must answer who sees what (own-trash vs admin-trash; the ceiling resolver's trashed-doc postures from the Step-8 verb asymmetries are the precedent vocabulary) — ADR-worthy when picked up.
- **Cross-surface contract hygiene** (principle now durable in AGENTS.md § Working rules — *"surface adapters reuse the capability schema (SSOT, ADR 0034)"*, which retired the older "mirror" framing):
  - CLI flag parser does not yet reuse `@editorzero/schemas` (route layer now does). When it re-points, it inherits the schema's `z.coerce.number()` numeric/enum handling for free — the shared schema was designed (ADR 0034 P4) so one definition validates HTTP query strings *and* CLI/MCP numbers. MCP tool schemas likewise import the same input modules as they migrate.
  - `auth.logout` leaves the server session live — belongs at `@editorzero/auth`, not the CLI adapter.
- **CLI-adapter UX backlog** (`ez` only): no `EZ_BASE_URL` env var; no `--format json|toon` flag; ANSI escapes in piped help / no `NO_COLOR`; `ez` vs `editorzero` naming drift.

**Gated on @numman decision:** Phase-4 entry gate revision (Open Question 3) — Appendix C's "all entries CLOSED" rule is unsatisfiable against the current tree; rule revision + dual-backend scope answer land at the phase boundary. Last formal sweep (d3a55d2, 2026-04-19) predates the workspace/audit/membership slices; a fresh sweep belongs to phase-boundary review. Spec source: `docs/architecture/appendix-c-phase-3-entry-checklist.md` (§ Appendix C).

## Open questions for @numman

1. **Commercial arm** — OSS-only or OSS + hosted? AGPL-3.0 + DCO doesn't pre-commit the answer. Doesn't block Phase 3.
2. **Agent offline-edit semantics** — do agents get offline/reconcile, or always-online? Default "always-online" stands unless challenged. Doesn't block Phase 3.
3. **Phase-4 entry gate** — revise Appendix C's "all entries CLOSED" rule; name an explicit minimum-CLOSED set and assign every other item to a named deferral bucket. Inputs to the revision: (a) ADR 0007 dual-backend scope (keep Postgres commitment, or SQLite-first with Postgres deferred), (b) pre-boundary polish lifts (BlockNoteEditor smoke, SQLite conformance harness above). **Blocks Phase-4 entry.**

## Recent history

- **2026-06-12** — **`doc.publish` + `doc.unpublish × Web UI` cells LAND: the doc header's publish toggle — one commit** (task #57; the tenth + eleventh proven ui cells — every doc-lifecycle verb except move/guests is now browser-reachable). The doc screen's panel header gains the **Publish ⇄ Unpublish toggle** — placed with title + slug, NOT in the editor toolbar, because publish is doc-level state orthogonal to content (ADR 0040 Step 5); success invalidates `doc.get` + `doc.list` only, so the canvas is untouched (no re-base, no dirty-state interaction — contrast rename). Published state shows the **minted `published_slug` as header status** ("published · <slug>" — collision-suffixed at mint, so not necessarily the doc slug; the public reader route is a later slice, so there is no URL to link yet) and the list's green `st-pub` chip becomes user-reachable for the first time. No confirm step: both directions are one click apart; re-publish after unpublish re-mints per the documented release-the-URL semantic. Proving spec pins the full loop: zero chips → publish → header status carries the minted slug + the ONE list chip appears (cross-screen server state) → unpublish → pair cleared, chip back to base outline; axe on the published header. Both capabilities declare `ui`, `UI_PENDING` 29 → 27. Suites: app 135 (+4), capabilities 930, contract-tests 6, e2e 32 (+1); typecheck clean.
- **2026-06-12** — **`doc.delete × Web UI` cell LANDS: the editor toolbar's Trash control — one commit** (task #56; the ninth proven ui cell — create/rename/delete now complete the doc-lifecycle verbs reachable from the editor). The toolbar gains **Trash** (the mock's 03-documents `documents-danger` vocabulary: warn-ink on the ghost outline) morphing into an **inline confirm** — the confirm step exists because the *browser* can't undo its own action yet: soft-delete is recoverable BY DESIGN (invariant 6, `doc.restore` on API/CLI/MCP), but the **Trash screen is blocked on a capability gap the cell surfaced** — `doc.list` has no `include_deleted`-style input, so NOTHING can enumerate trash; recorded in the punch list as ADR-worthy (trash visibility must answer own-trash vs admin-trash — the Step-8 trashed-doc verb asymmetries are the precedent vocabulary). **No dirty-canvas gate, deliberately** (contrast the rename control): trashing discards unsaved edits along with the doc — that is the point of the action; disabled mid-save only, so an in-flight ops batch can't race the soft-delete. Success: drop the doc's `doc.get` cache, invalidate `doc.list`, navigate home. Proving spec: trash → home → row gone while siblings remain (server state) → direct `GET /docs/get/:id` 404s → **restore over the API returns the row to the list** (invariant 6 visible end-to-end through the browser surface) + axe on the open confirm. `doc.delete` declares `ui`, `UI_PENDING` 30 → 29. Suites: app 131 (+3), capabilities 930, contract-tests 6, e2e 31 (+1); typecheck clean.
- **2026-06-12** — **`doc.rename × Web UI` cell LANDS + the e2e flushes out a latent capability bug — one commit** (task #55; the eighth proven ui cell). The editor toolbar gains a **Rename control** (the new-doc disclosure morph: ghost button → inline form prefilled with the current title), bound to `doc.rename` — the capability with real rename semantics (title-slot rule: `docs.title` + re-derived slug + the canvas heading-1 move together in ONE audited mutation), distinct from editing the heading block in the canvas (a content op that leaves the row title behind — the documented v1 seam this cell closes for browser users; the route header's "later slice" note cashed in). **Clean-canvas gate**: the control disables while the canvas is dirty/saving, because the post-rename refresh re-bases the editor wholesale (`setContent`) — gating makes canvas == server at every rename, so NO path can discard unsaved edits; the rebase also rewrites the doc.get cache, so the panel header (title + slug) re-renders with zero wiring of its own. No-op rename (unchanged title) closes the form without a wire call. **The proving e2e flushed out a REAL latent bug** (the dogfood-at-the-boundary payoff): `doc.rename` re-derives the slug into the same partial-unique namespace `doc.create` mints into but had **no sibling-slug pre-check** — a live same-level sibling holding the target slug bubbled a raw UNIQUE violation from `docs_root_slug_unique`/`docs_nested_slug_unique`, which the dispatcher projects to **`internal` (500), on every surface** (the same class the restore-path fix-forward closed for `collection.restore`/`doc.restore`). Fixed at the capability: the NULL-aware level-scoped pre-check mirrored from `doc.create` **plus one extra term — EXCLUDE SELF** (a title-case tweak whose slug is unchanged must not refuse against the doc's own row); `SlugCollisionError` → typed 409; the route's `describeRoute` declares the arm it could now return (the shared `errorResponse` mapper already carried `slug_collision`). Four new capability tests pin: root collision (typed, row untouched — pre-check precedes the UPDATE), self-exclusion, trashed-sibling-doesn't-block (partial-index parity), level-locality (root slug ⊥ nested namespace). The browser form's 409 arm reuses the create vocabulary ("pick a different title"); shared `inline-form.css` replaces the one-commit-old new-doc.css (one source for both disclosure forms). `doc.rename` declares `ui`, `UI_PENDING` 31 → 30. Proving spec: rename → header + slug + CANVAS heading all reflect it (the title-slot bridge, visible only through the re-base) + list row server-state proof; duplicate-title 409 stays on-screen with the form reusable; dirty canvas disables the trigger; axe with the form open. Suites: app 128 (+8), capabilities 930 (+4, 20 on rename), api-server 293, contract-tests 6, cli 87, e2e 30 (+2); typecheck clean.
- **2026-06-12** — **`doc.create × Web UI` cell LANDS: the "+ New doc" inline form — one commit** (task #54; the seventh proven ui cell — the first *mutation from chrome*; the editor's Save was the first mutation overall but lives inside the canvas). The mock's bar affordance (`+ New doc`, `btn--ultra`) lands in the docs panel header and **morphs in place into an inline title form** — a title is REQUIRED, not cosmetic: `doc.create` derives the slug and refuses a sibling collision with a 409, so a fixed default title ("Untitled") would hard-fail on the SECOND click, and no doc.rename cell exists to fix one up afterwards. Success: invalidate `doc.list` → navigate into `/doc/$docId`, where the seeded `heading/1` title block is the first thing the user sees. The 409 has its own UX arm (`classifyCreateError` — "pick a different title", retrying the same one can never succeed); everything else gets the generic retry line; both messages + the `createDoc` wire call (201-arm `Extract`) live unit-tested in `lib/docs.ts` (route + form components stay e2e-covered per the coverage split). Focus management IS the disclosure announcement (trigger unmounts; the input takes focus — pinned by `toBeFocused`). Bare cell creates at the workspace ROOT — placing into a collection needs a picker (later increment, with the tree cell's screens). `doc.create` declares `ui`, `UI_PENDING` 32 → 31. Proving spec drives the real form end-to-end (create → editor heading → back to list, server state) + the duplicate-title 409 arm (alert visible, still on-screen, form reusable) + axe with the form open. Suites: app 120 (+10), capabilities 926, contract-tests 6, e2e 28 (+2); typecheck clean.
- **2026-06-12** — **Production WS attach LANDS: all four ADR 0030 blockers closed + the mount folded into the serve layer — one commit** (task #15 slice A; dated ADR 0030 amendment records the decisions; design settled with Codex over cmux BEFORE building — both MUST-FIXes + both SHOULD-FIXes adopted, one deepened). The smoke's private bare-Hocuspocus wiring is GONE: `attachCollab` (`apps/server/src/collab.ts`) mounts `/collab` upgrades on the raw `http.Server` via `startServer`'s new **attachment seam** (`ServerAttachment`; attachments close FIRST in the drain — upgraded sockets count as open server connections, so `server.close()` would otherwise hang on live clients; teardown pinned with a deliberately-lingering client), and WS clients enter the dispatcher's own embedded Hocuspocus through the one public `HocuspocusSync.handleWsConnection` (connection context carries ONLY the `__ws` hydration marker → the reserved `onLoadDocument` branch replays committed `doc_updates`, proven across two sync instances over one DB — server-restart shape). **The four blockers**: (1) *universal `readOnly`* — sync's own `onAuthenticate` forces `connectionConfig.readOnly = true` after policy pass; invariant 3 makes WS write unsatisfiable until the audited write lane, so NO policy outcome grants write today (state-asserted per Codex SHOULD-FIX: zero `doc_updates` rows + fresh-read compare, not just the nack frame); (2) *Origin allow-list* — normalized `new URL(...).origin` comparison both sides (Codex SHOULD-FIX; folded/opaque/malformed refuse, malformed config fails closed), absent Origin refuses on the cookie path (browsers always send it; tolerance only shields header-faking non-browsers); (3) *revocation freshness* — nothing identity-shaped rides connection context; `collabAuthorize` re-runs the shared `BetterAuthResolver` against the upgrade headers **on every document Auth frame** (Codex MUST-FIX 1; Hocuspocus re-presents them per frame — sign-out then next attach denies, pinned), then reuses the gate's own terms (invariant 5): newly-exported `effectiveScopes ⊇ {doc:read}` + the Step-6 ceiling's `assertCanRead` on the live row (soft-deleted denies — restore first); *users-only* cookie path (static `effectiveScopes` trusts agent scope claims verbatim — the live H8 intersection lives in `workspaceAwareGate`; bearer/agent WS = slice B); (4) *`onAuthenticate` by construction* — `HocuspocusSync` registers the hook unconditionally in its constructor with `collabAuthorize` an optional dep **defaulting to deny-all** (supersedes the ADR's `createAuthenticatedCollabHocuspocus` factory idea: a hook-less server — Hocuspocus's "any Auth frame = full access" hole — is unrepresentable, and a composition root that forgets the policy fails CLOSED with zero churn on 12 existing call sites; the dispatcher's `DirectConnection` lane never fires auth hooks — 3.4.4-verified). **Codex MUST-FIX 2, deepened**: with a WS client holding the doc resident, rollback's old direct-disconnect path could leave aborted state in memory → the next `ctx.transact` would persist deltas computed on it (silent invariant-3/7 violation; worse than Codex stated — our `debounce: 2000` config means `unloadDocument` refuses while a debounced store pends, and only `DirectConnection.disconnect`'s drain made the no-WS path work). Landed: `#evictResident` force-closes the doc's WS subscriptions (per-doc Close frame; multiplexed socket survives) and actively drives `unloadDocument` inside a bounded drain; unprovable eviction → the doc joins `#poisoned` and BOTH open paths refuse loudly (retry-then-throw under the doc lock). The old "WS-client limit" regression guard — which pinned silent pollution as a known limit — is REPLACED by the fail-closed + recovery pin (deterministic DirectConnection squatter), plus a real-WS-client evict-and-rehydrate test. *Recorded residual (slice B)*: the pre-commit broadcast already reached clients (Yjs merges are additive — phantom persists client-side until resync; readOnly means it can't come back; no production WS client exists yet) → the deep fix is **broadcast-after-commit**. Deny logging uses the pre-reserved `hocuspocus.authenticate` event. Suites: sync 56 (new 6-test `ws-attach` integration lane: deny-default, forced-readonly scope, payload contract, post-close refusal, `__ws` cold hydration, WS rollback eviction), api-server 293, apps/server cohost REWRITTEN production-shaped (11 tests over `startServer`+`attachCollab` verbatim: multi-doc multiplex, cross-tenant deny, readOnly state assert, HTTP→WS live convergence, sign-out freshness, drain-with-lingering-client) + 14-test collab unit lane (origin matrix, reserved-prefix pin, refusal branches incl. resolver-throw destroy), dispatcher + cli green; typecheck + coherence 0/0. **Slice B recorded** (ADR amendment): audited WS write lane → broadcast-after-commit → role-aware write → event-driven revocation closes → bearer/agent WS with the H8-aware term.
- **2026-06-12** — **`collection.list × Web UI` cell LANDS: the sidebar Collections tree — one commit** (task #53; the sixth proven ui cell — the LAST of ADR 0040's three named shell structures, "sidebar + Spaces grid + Collections tree"). The wire is a FLAT `order_key`-ordered array; nesting is the client's pure **`flattenCollectionTree`** (DFS, children in wire order under each parent), unit-tested where the policy lives: **orphans render as roots** (a live child under a TRASHED parent is reachable state — hiding it would make a reachable collection un-navigable), **nothing the wire returns is ever hidden** (a corrupt parent cycle — unmintable through capabilities — has no root to enter through; a leftover sweep surfaces its rows at root depth instead of dropping them), arbitrary depth (the token sheet's `.ind`/`.ind2` classes cover two levels; past that the same 10+16·depth scale continues inline — `treeRowIndent`, pinned). Rows are READ-ONLY at the bare-cell stage (no collection screen exists to link to; drag-reorder is its own later cell per the ADR 0037 Owned Tree note — it binds collection.move/doc.move); the `.tw` caret marks has-children, not a toggle; the section is honestly ABSENT on an empty workspace. Warmed in `_authed`'s `beforeLoad` in PARALLEL with `workspace.get` (both behind the session 401 gate); the untestable `useWorkspace`/`useCollections` hook wrappers were dropped in the same commit — route files read `useQuery(options)` directly (the doc.list pattern; routes are e2e-covered, and the wrappers were the only sub-floor coverage holes). `collection.list` declares `ui`, `UI_PENDING` 33 → 32. The proving spec pins the honest empty state (no Collections section at all), then root + nested child + second root created over `POST /collections/create` render in DFS order with the child carrying `.ind` — **the depth assertion caught a real spec bug at first run**: the create echo's id field is `collection_id` while the list summary's is `id`; reading the wrong one silently minted a root (undefined `parent_id` serializes away, the create still 200s) and only the indentation class exposed it. Suites: app 112 (+11), capabilities 926, contract-tests 6, e2e 26 (+2); typecheck + coherence 0/0.
- **2026-06-12** — **`workspace.get × Web UI` cell LANDS: the sidebar workspace identity block — one commit** (task #52; the fifth proven ui cell). The block the mock draws as a workspace *switcher* lands as an **identity block** — the deployment IS one workspace (ADR 0040 Model B: `workspaces` is the tenant root), so there is nothing to switch to and the block is non-interactive: monogram avatar + name + slug in the token sheet's `.ws` vocabulary, rendered by the shared `SideContent` (desktop aside + mobile drawer for free). Data: `lib/workspace.ts` (typed `hc` derivation, `useWorkspace` hook on the `useSession` pattern, `workspaceMonogram` helper — unit-tested); warmed in the `_authed` layout's `beforeLoad` AFTER the session ensure (an expired cookie hits the session 401 → login redirect first, so the workspace fetch only ever runs authenticated; its own failure is a real fault → error boundary). `workspace.get` declares `ui`, `UI_PENDING` 34 → 33. The proving spec pins **bootstrap-derived server state with zero spec-side setup**: the genesis workspace name is derived from the founder's email local-part (`founder@…` → "founder's workspace") — asserted verbatim; the slug asserts its deterministic `founder-` prefix only (the suffix is minted per run from the workspace id); the mobile drawer carries the same block (shared component, one render path); axe on both. Suites: app 101 (+7), capabilities 926, contract-tests 6, e2e 24 (+2); typecheck + coherence 0/0.
- **2026-06-12** — **`space.list × Web UI` cell LANDS at `/space` + reserved-prefix SSOT fix-forward — two commits** (task #51; the first Web UI cell on the ADR 0040 surface; ADR 0040 dated amendment records the vocabulary-lock route adjustment). **Commit 1 — the SSOT had drifted from the trunk three ways**: `/permissions` (Step-8 slice 1) and `/spaces` (slice 2a) landed as trunk route domains without joining `RESERVED_API_PREFIXES` — although ADR 0040's vocabulary-lock line explicitly recorded both as "additive to that SSOT + its equality test" — leaving the dev proxy blind to them, the production SPA fallback serving shell HTML for their unmatched GETs, and the SW denylist passing their navigations to the app-shell cache; fixed, plus a **contract-tests forcing function** (every registry-derived HTTP binding must fall under a reserved prefix — the list must stay import-free for vite.config, so the gate replaces derivation). **The gate's first run exposed a third, older instance**: the list said `/audit` while the trunk has always mounted `/audits` — a dead entry shadowing a missing one since the static-attach slice; corrected in the same commit. **Commit 2 — the cell**: the lock's two recommendations ("client route `/spaces`" + "additive to the SSOT") became mutually exclusive when slice 2a mounted the API domain, so the LABEL half stands ("Space"/"Spaces" in all copy) and the route follows the `/doc/$docId`-vs-`/docs` singular precedent → the Spaces screen is **`/space`** (future detail `/space/$spaceId`); `lib/spaces.ts` data layer (typed `hc` derivation, react-query binding, label/meta helpers — fully unit-tested) + the Meridian Zero `.spaces`/`.sp` grid from the landed token sheet (zero new CSS; non-interactive cards at the bare-cell stage — no detail route exists, the e2e honesty bar) + the sidebar "Spaces" entry; `space.list` declares `ui`, `UI_PENDING` 35 → 34; proving Playwright spec asserts the **signup-seeded Personal space renders with zero spec-side setup** (server state end-to-end), team spaces created over `POST /spaces/create` render in the capability's `name ASC` order with kind chips, and axe passes; the SW-denylist suite pins `/space` client vs `/spaces` API. Suites: app 94 (+10, coverage 100% stmts), capabilities 926, contract-tests 6, constants 3, server 21, e2e 22 (+2); typecheck + coherence 0/0.
- **2026-06-12** — **ADR 0040 `collection.move` crossing branch LANDS — the second `acl_transition` emitter; the space-collection family is COMPLETE, one commit** (task #50 second half; dated ADR amendment records the decisions; design settled with Codex over cmux BEFORE building — his MUST-FIX + both SHOULD-FIXes + the extra test pin all landed as written, no rebuttals). **The MUST-FIX reshaped the input**: a tagged destination union `{kind:"legacy_root"} | {kind:"space_root", space_id} | {kind:"collection", collection_id}` REPLACES `new_parent_id` — space root becomes expressible (the gap the fix-forward bullet below recorded), illegal states are unrepresentable (no both-set refine rail; per-arm `.strict()`), and `acl_policy` is required iff sourceBucket ≠ destinationBucket (the doc.move rails verbatim: `acl_transition_policy_required`/`acl_policy_not_applicable`, both 400 on path `["acl_policy"]`, AFTER authority so a non-administer caller learns nothing). **Crossing = an audited bulk ACL transition over EVERY subtree doc — live AND trashed** (trashed state is ACL-bearing; restore would otherwise resurrect a doc inside the new bucket with stale grants): per-doc `assertCanAdministerDoc` **sorted by `doc_id`** (deterministic first-denied doc cross-driver and cross-replay — the SHOULD-FIX; DB row order never decides the deny), destination standing once per kind (space_root → the new `assertCanPlaceInSpace`, collection → `assertCanPlaceIn`, legacy_root → none — the other SHOULD-FIX), atomic refuse. **Uniform two poles**: `adopt_baseline` hard-DELETEs every doc-scoped grant in the subtree — both lanes, both subject kinds — RETURNING full preimages sorted by `grant_id`; `keep_grants` = zero ACL writes; **ONE audit row** (`collection.move` effect += `acl_transition?` — unbounded `dropped_grants` recorded as deliberate self-hosted-v1 posture; the future rail is a byte-size typed refusal, never a spill table). Root UPDATE carries `space_id`; descendants rebind in a second UPDATE same-tx WITHOUT an `updated_at` stamp (mirrors the reducer's `rebindSubtreeSpace` denormalization maintenance); `before_space_id` honesty via `storedSpaceRefOf` (trashed-resolvable ref reports itself, dangling reports null). **Anomalous source always crosses** (repair path — administer collapses to owner-tier, the admin backstop deliberately fails); **the Codex extra pin landed as written**: anomalous subtree OUT under `adopt_baseline` runs the per-doc ladder BEFORE any drop and leaves the ACL world byte-identical on failure (`snapshotAclWorld` equality); INTO an anomaly stays impossible (`canPlaceIn` fail-closed). Same-bucket regime unchanged in semantics but the policy-must-be-ABSENT rail now lives on BOTH the resolver-free fast path and the resolver path, and all-legacy moves still never load the resolver (pinned structurally by the bare-driver test). Slug namespace stays parent-scoped + space-BLIND — a root collection crossing buckets keeps its slot (pinned). **SSOT extraction**: `AclTransitionPolicySchema`/`AclTransitionOutputSchema` moved to `packages/schemas` shared/grant.ts; doc/move re-exports — both movers share one transition vocabulary. **The CLI grew JSON-valued flags** (destination is the first object-valued input field): `deriveJsonFlagKeys` marks structured fields by zod class, the generator JSON.parses exactly those flags before the shared-schema parse — surface plumbing, NOT schema tolerance (a `z.preprocess` would degrade `z.input` to `unknown`, poisoning the typed hc client and the OpenAPI doc). **Fuzzer reworked within the 20-verb matrix**: `WCollection` += `parent_id` → the oracle walks `modelSubtree` (BFS) so per-doc administer covers runtime children; the move arm draws the destination union + `acl_policy` on ~40% of draws; `applyAllowed` rebinds the model subtree from the output echo and drops exactly the echoed grant ids; a second deterministic canary crosses a live legacy collection into a personal space root under `adopt_baseline`. **Mutation-tested**: per-doc administer disabled → caught by 2 suites; trashed docs excluded from the transition → 3; descendant rebind skipped → 3; all reverted clean, dual-driver digest equality green. **The new oracle arm unmasked a latent oracle bug** (recorded in the ADR): the `permission.revoke` oracle forbade trashed-doc revokes, contradicting the handler's documented offboarding contract (revoke works on trashed docs; only a fully-missing row refuses) — vacuously wrong since §8.1a landed, first reachable draw exposed it; fixed in the same commit. Suites: capabilities 926 (+23; 53 on move alone), audit 54, api-server 293 + auth-chain 118, dispatcher replay walk extended with collection-level crossings under both policies, cli 87 (+6), prop dual-driver ≈ 3.4s warm; monorepo typecheck + coherence 0/0.
- **2026-06-12** — **Space-collection slice 1 fix-forward: `collection.move` same-bucket rail — the recorded refusal didn't exist, one commit** (task #50 first half; dated ADR entry records the gap + decisions). Post-landing self-review of slice 1 found `collection.move` bucket-BLIND: the "stays same-space-bucket with typed refusal" both Codex and I treated as current state was never built — no binding comparison, no standing beyond gate `doc:write`, no rebind. Slice 1 made the gap capability-reachable (space-bound collections became mintable): a reach-less caller could graft a legacy subtree under a space collection or extract a space-bound one to legacy root, minting parent/child binding mismatches. Window lived ~1 hour on main, no release. Now enforced: step-2b rail — `placementOf` both ends, both-legacy or both-the-same-live-space, anomalies fail closed (incl. both-ends-in-a-trashed-space), cross-bucket → typed 400 `cross_bucket_move_unsupported`; same-space re-parents require `assertCanPlaceIn(target)` (one term covers grafting INTO + restructuring WITHIN); legacy↔legacy reach-free with the no-resolver-load hot path pinned STRUCTURALLY (a test driver with no space tables at all). Fuzz matrix 19 → 20 (legacy-skewed arm + legacy→root canary; mutation: rail disabled → caught seed 3); replay walk gains a same-bucket space re-parent. **Recorded for the crossing slice**: `new_parent_id: null` means LEGACY root — "space root" is inexpressible in the input shape (create can mint there; move can't target it) → the crossing branch needs an explicit destination-bucket input. Suites: capabilities 913 (+10), prop 49 green dual-driver, typecheck + coherence 0/0.
- **2026-06-12** — **ADR 0040 space-collection family slice 1 LANDS: `collection.create` gains `space_id` + child inheritance — one commit** (task #49; dated ADR amendment records the decisions; design settled with Codex over cmux BEFORE building — all three questions concurred, his SHOULD-FIX built in, both his test pins landed). **Two placement regimes, mutually exclusive by schema rail**: root create takes optional `space_id` (`null`/omitted = legacy bucket, zero new queries on the hot path; non-null = existence-404 FIRST per the doc.move target precedent, then standing), child create INHERITS the parent's stored binding — derivation, not input (`space_id` + non-null `parent_id` = 400 at the shared ADR 0034 schema, so every surface refuses identically; a redundant must-match input was rejected as a drift surface). **Standing**: root-into-space uses the ceiling resolver's new `assertCanPlaceInSpace` (the F88 projection of `hasBaselineReach`, `acl_deny {space_id}`) — **non-delegated agents do NOT ride the open-space user baseline** (the Codex SHOULD-FIX; falls out of the subject collapse, not a special case); child creates ride the existing `assertCanPlaceIn(parent)` — legacy always placeable, space needs reach, **anomalous parents fail closed so inheritance can never copy a dangling ref into a fresh row**. Ordering: 404 → authority → slug pre-check. **Slug namespace recorded as parent-scoped + space-BLIND** (space-bound roots contend with legacy roots; bucket-scoped namespaces = separate product decision, recorded so it isn't rediscovered as a bug). **Reducer + HTTP route: zero edits by construction** — the Step-7 effect already carried `space_id` (emission-only until now) and the route imports the shared schema via `validator` (ADR 0034 working as designed); http-binding pin += `space_id`. **Replay-walk crossing deferral CLOSED**: space.create → root create `{space_id}` → child create (inherits; the walk uses the INHERITED child as crossing destination — Codex pin 2) → re-grant → `doc.move` legacy→space under `adopt_baseline` (dropped-grant preimage applied by replay) → back under `keep_grants`. **Fuzz matrix 18 → 19**: three generator arms + mutual-exclusion rail probe, oracle arm restating the two-regime formula, shadow-model collection tracking, two deterministic canaries (personal-space owner ALLOW through the owner rung; api-key agent deny — Codex pin 1a at fuzz grade). **Mutation-tested**: `assertCanPlaceInSpace` disabled → caught seed 1 by the agent canary; child-arm `assertCanPlaceIn` disabled → caught seed 6 by the oracle; both reverted clean, dual-driver digest equality green. **`collection.move` stays same-bucket** with typed refusal — the crossing branch (N-doc bulk transition policy) is its own future slice (Codex (c) concurrence). Suites: capabilities 903 (+16, incl. the new space-binding describe with both Codex pins), api-server 292, dispatcher 36, prop 49 (extended walk + 19-verb dual-driver fuzz ≈ 4s warm); monorepo typecheck + coherence 0/0.
- **2026-06-12** — **§8.1a fuzzer Codex review RETURNS — no blocker, both SHOULD-FIXes applied same-day** (dated ADR entry records dispositions). (1) Final sweep widened: BOTH `doc.list` + `space.list` for every principal shape (users, api-key agents, delegated pairings) — was user-`doc.list` only, leaving late space mutations and agent/delegated read semantics to opportunistic in-sequence hits. (2) `normalize()` now PRESERVES array order — blanket sorting masked cross-backend ordering divergence on contractual-order outputs; sole recorded exception `dropped_grants` (handler-sorts by grant_id, minted ids cross-driver-nondeterministic → bag). Dual-driver re-run green, so the order claim holds for real. His `is_guest=1` space-grant generator note recorded as intended representable-corruption posture. His concurrence: concurrency owned by write-atomicity props, A-side audit by replay.
- **2026-06-12** — **ADR 0040 §8.1a capability-matrix × dual-driver tenant-isolation fuzzer LANDS — the Step-8 verification remainder, one commit** (task #48; dated ADR amendment records the design). `packages/api-server/prop/tenant-isolation.test.ts` drives the REAL production composition (`createApiDispatcher` + `workspaceAwareGate({loadDelegatorRoles})` + default registry) over randomized two-tenant worlds on BOTH drivers — SQLite always, Postgres via testcontainers (`EDITORZERO_SKIP_POSTGRES_TESTS=1` bypass). Matrix = the 18 metadata-only + pure-read ACL verbs (transact-bearing excluded with cause — they need a `HocuspocusSync`; resolver-grade fuzz + unit suites own them). **Four properties**: pollution-never-allows + tenant-B bit-identity across eight tenant-scoped tables (audit + outbox included — attribution can't leak either); one-sided authority oracles (independent §7/§8 restatements over a plain-data world model — false-allow is the security failure, false-deny is liveness owned by unit suites); exact `doc.list`/`space.list` visible-set equality against a drift-tracked shadow model that applies every ALLOWED mutation (+ final per-principal sweep proving the model tracked reality); cross-backend outcome-digest sequence equality (shared injected clock across audit/outbox/`ctx.now()`, per-seed deterministic minter FACTORIES so both driver runs mint identical worlds, minted-uuid normalization, array canonicalization). **Anti-vacuity floor** (≥1 allow + ≥1 refusal per verb per run + pollution-arm floor) caught three allow-starved verbs during development (`doc.remove_guest`, `space.restore`, `space.archive` — the last because every seeded space had live descendants); fixed with authority/target biasing, correlated publish→unpublish / add→remove pairs, and a guaranteed-empty live team space per world. **Deterministic canaries**: delegated-non-member read pins (`delegator_not_member` must fire even on always-allowed verbs) + a crossing-without-authority probe — mutation testing showed the uniform draw can miss the crossing-administer window for a full run (~7% per-run miss odds). **Mutation-tested at landing**: ceiling private-leak → caught by visible-set equality; guest-revoke refusal disabled → caught by the oracle; crossing `assertCanAdministerDoc` disabled → caught by the canary. All reverted clean. `createApiDispatcher.driver` widened to structural `ApiDispatcherDriver = Pick<SqliteDriver, "withSystemTx" | "scoped">` (its header pre-authorized exactly this); `@testcontainers/postgresql` joins api-server devDeps. ~1.1k dispatches/driver/run, dual-driver lane ≈ 3s warm; **100k-nightly remains OPEN** until a scheduled lane exists (raise `SEEDS` — seed-deterministic by construction). Architecture §8.1a bullet + build-status note updated (placement honestly corrected from the sketched `packages/db` path to the composition root). Prop lane 47 → 49 tests.
- **2026-06-12** — **ADR 0040 Step 8 doc.move cross-boundary branch LANDS — the sole `acl_transition` emitter, one commit** (task #47; dated ADR amendment records the decisions; design settled with Codex over cmux BEFORE building — all five decisions concurred, MUST-FIX + extra test pinned, SHOULD-FIX upgraded). **Two regimes by placement bucket**: same-bucket stays Step-6 navigation (`doc:write` + `assertCanRead`); crossing = `assertCanAdministerDoc(source)` + `assertCanPlaceIn(target)` (the literal `doc.create` term — **the Codex MUST-FIX pin**: a non-guest OWNER-grant holder on an anomalous doc administers the source but CANNOT repair into a space without standing; `acl_deny` scoped to the COLLECTION proves which gate refused). **`acl_policy` conditionally required, typed 400s both ways** (`acl_transition_policy_required` on a bare crossing / `acl_policy_not_applicable` on a policied same-bucket move), authority FIRST so the rails never leak placement to non-administer callers (ordering pinned). **`adopt_baseline` hard-DELETEs every doc-scoped grant row — both lanes, both subject kinds — RETURNING full preimages sorted by `grant_id`; `keep_grants` = zero ACL writes.** Effect reshape (zero emitters existed): `AclTransition.dropped_grant_ids` → `dropped_grants: AclTransitionDroppedGrant[]` (9-field `GrantState` mirror; output echo additionally carries `created_at`, the effect strips it). **Anomaly movable OUT only** (administer collapses to owner-tier — the admin backstop deliberately fails there, pinned with a guest-read admin; INTO refused via `canPlaceIn` fail-closed); `before_space_id` honesty via the resolver's new **`storedSpaceRefOf`** (trashed-resolvable ref vs dangling null — same preloaded snapshot as `placementOf`, zero extra queries). **`access_mode` rides unchanged** (the Codex extra test: private doc + guest/non-guest grants crossing under adopt → zero grants AND a destination member still can't read; control proves the member's reach is real). Step-6-era pins rewritten as new-regime pins (handler-wiring: the widening fear case is now never-silent-but-possible; http-binding +`acl_policy`). **Replay-walk crossing steps DEFERRED with cause** — no capability mints a space-bound collection yet (`collection.create` pins `space_id = null`); reducer transition semantics stay pinned fixture-level; walk comment + ADR mark the unlock. Suites: capabilities 887 (39 on move alone), api-server 292, audit 52, dispatcher 36, prop 47, contract 5, cli 81, mcp 31; typecheck + coherence 0/0.
- **2026-06-12** — **ADR 0040 Step 8 guest family LANDS: `doc.add_guest`/`doc.remove_guest` — the explicit ceiling-crossing lane, one commit** (task #46; dated ADR amendment records the decisions; design settled with Codex over cmux BEFORE building — all five decisions concurred, no rebuttals, his two SHOULD-FIXes built in). **The first `is_guest = 1` emitters** — same `acl.grant`/`acl.revoke` kinds (the marker is a carried column; reducer unchanged), scopes reuse the permission family (`permission:grant`/`permission:revoke`), rate 60. **The typed lane conflict (SHOULD-FIX A)**: new `GrantLifecycleConflictError` (`grant_lifecycle_conflict`, 409, `existing_lane` + `grant_id`, audit fold `{kind:"conflict"}`) thrown by `doc.add_guest` on a NON-guest edge AND by `permission.grant` on a guest edge (was a generic `ConflictError`) — the caller's next verb is deterministic, not retry-backoff copy; api-server mapper + `errEnvelopeOneOf` (two codes on one 409) + api-client `API_ERROR_CODES` (+1 → 14) moved in lockstep — **coherence Check 10 caught the client half at landing** (the drift check working). **`doc.remove_guest` is EDGE-addressed** (`doc_id, subject_kind, subject_id` — the unique edge IS the identity; `permission.revoke(grant_id)` stays the forensic verb and keeps refusing guest edges) with three distinct dispositions (SHOULD-FIX B): absent → 404 on the grant edge; `is_guest = 0` → lifecycle conflict routing to permission.revoke; guest → hard DELETE, full-preimage echo. **Works on TRASHED docs** (404 only when the row is gone entirely): revoke refuses guest edges, so refusing trash here would make guest edges in trash IMMORTAL and resurrect on restore; authority over the STORED placement (the doc.restore posture). **`doc.add_guest`'s deliberate asymmetries**: NO subject standing checks (the verb's point — slice-1's refusal rails route here; cross-workspace users + unprovisioned agents accepted as written, existence debt recorded for BOTH kinds), NO anomaly refusal (a guest edge carries its marker by construction — mid-anomaly recovery sharing is the job the slice-1 MUST-FIX text reserved for this verb; WHO stays owner-tier-bounded, pinned: admin backstop acl-denies on anomaly), guest `owner` unmintable BY SCHEMA (`GuestGrantRoleSchema = z.enum(BASELINE_ACCESS_ROLES)` — the ceiling ignores guest grants for owner-tier, so the row would lie). Same-lane convergence mirrors permission.grant (idempotent echo zero-writes / role UPDATE under the same grant_id / ON CONFLICT zero-row 409s). **`doc.list_grants` DROPPED, not deferred** — `permission.list(resource_kind=doc)` is the panel; an alias would drift. **Attribution tests surfaced the ceiling's real agent rules** (worth recording): the admin backstop fails closed for ALL agent principals, and `owner_user_id` confers attribution NEVER authority — an api-key agent administers only via its own non-guest owner-role grant; a delegated agent via its delegator's tier. Replay walk extended: add_guest mint → guest-lane role convergence (same grant_id) → idempotent re-add → edge-addressed remove, plus a SURVIVING guest edge for a never-a-member subject (final projection compares both `is_guest` values on one resource). Routes `POST /docs/add_guest/:doc_id` + `POST /docs/remove_guest/:doc_id` (P3, pre-split Param/Body schema exports); CLI/MCP derive free. Suites: capabilities 871 (+58), api-server 292 (+5), api-client 21, contract-tests 5 (UI_PENDING += 2), cli 81, mcp 31, prop 47 (extended walk), dispatcher 36; monorepo typecheck + coherence 0/0. **Next: the `doc.move` cross-boundary branch** (the sole `acl_transition` emitter), then the §8.1a capability-matrix × dual-driver fuzz.
- **2026-06-12** — **ADR 0040 Step 8 space read family LANDS: `space.get`/`space.list` — the first Space-scoped reads, one commit** (task #45; dated ADR amendment records the decisions; Appendix A rows added in the same commit — the coherence Appendix-A drift check REFUSED the commit until they existed, the forcing function working). **Visibility = baseline reach ∨ administer, per resource** — composes the two existing ceiling predicates (`hasBaselineReach` ∨ `canAdministerSpace`), no new resolver surface. The administer term is honesty, not widening (an admin can already member_add themselves into any team space — hiding the row while granting the verb is theater); **personal spaces stay owner-only against admins** (the privacy pin, now on the read side); **agents have NO Org baseline** (grants/delegation only — pinned on both verbs); delegated agents see their delegator's view (H8, free from the resolver). **Closed vs private RESERVED**: in v1 both confer nothing implicitly — the distinction belongs to future join-request flows; the reads deliberately don't pre-implement discoverability. **`space.list` unpaginated by design** (org-structural scale + the filter is a resolver predicate, not SQL — a cursor would page over invisible rows; the `doc.list` shape, additive cursor later), `name ASC, id ASC`. **Trash-invisible** (404 before visibility, even for admins; trash browse = future family). `workspace:read`, rate 600, `audit.access_log` (per-space collapse bucket on get — `.parse` narrowing instead of doc.get's documented cast — constant bucket on list). Routes `GET /spaces/get/:space_id` (P2 param reuse) + `GET /spaces/list`; CLI/MCP derive for free (no hand lists — verified); `SpaceRowOutputSchema` reused verbatim (SSOT — the reads restate nothing). Suites: capabilities 813 (+35 — the get visibility matrix runs every subject class; list pins the filter per class, the privacy-pin-vs-backstop split, ordering incl. the id tiebreak, trash exclusion), api-server 287 (+4), contract-tests 5 (both cells bound + UI_PENDING += 2), cli 81, mcp 31, prop 47; monorepo typecheck + coherence 0/0. **Next: `doc.add_guest`/`remove_guest`/`list_grants`** (the guest unique-edge upgrade-vs-409 design note queued for Codex), then the `doc.move` cross-boundary branch, then the §8.1a matrix fuzz.
- **2026-06-12** — **Step-8 slice-2 Codex review (whole space slice 2a+2b+2c) RETURNS — all three findings accepted, one fix-forward commit** (task #44; dated review entry on ADR 0040 records dispositions + keeps; no rebuttals this round). **MUST-FIX — the archive invariant was breakable through the INVERSE path**: `space.archive` refuses on live descendants (correct) but trashed descendants don't block it — and `collection.restore` never read `collections.space_id`, so "trash collection → archive its now-empty space → restore the collection" minted a live collection under a dead space; `doc.restore` had the doc twin through its parent collection's binding. Both restore verbs now require the bound space LIVE (missing row = dangling binding = refuse too): collection.restore step-2a checks its own `space_id`; doc.restore checks the live parent collection's. `ParentDeletedError.parent_kind` widened to `"collection" | "space"` (`parent_id: CollectionId | SpaceId`) — same wire code `parent_deleted`, envelope is code-only so zero client-visible shape change; route 409 descriptions name the archived-space cause. Remediation order: `space.restore` first, then descend — archive→restore symmetry now holds pairwise at every level. Regression tests pin the refusal + nothing-mutated, the restore-space-first unblock, and the dangling-binding arm. **SHOULD-FIX — `member_update_role` gained the post-authority personal refusal** (`personal_space_membership_pinned`, the member_add block verbatim): it selected only `id` and would have MUTATED a corrupt personal roster row — corruption as steady state. `member_remove` stays the ONE kind-blind repair verb (the 2c asymmetry narrows to remove only). Pinned: corrupt row not editable + owner still denied; outsider still sees acl_deny (kind not leaked). **NOTE — the dead-row restore ladder dropped the owner-role membership rung**: archive refuses while ANY roster row exists, so membership cannot legitimately survive onto a dead team space — a row found there is corrupt, and corrupt state must not confer restore authority. Ladder body parameterized (`spaceOwnerTierBody(space_id, membershipRung)` — Codex's suggested shape): live predicate keeps the rung; `canRestoreSpace` = personal owner ∨ non-guest owner space grant ∨ admin backstop. Pinned on both the resolver and the handler. **Codex's explicit keeps**: the owner-grant dead-row rung is defensible (state-as-of-delete + the workspace kill-switch), no-last-owner stands, slug-precheck posture matches the repo. Suites: capabilities 778 (+9), api-server 283, prop 47, contract-tests 5; monorepo typecheck + coherence 0/0. **Next: the Step-8 remainder** — space read family (`space.list`/`space.get` — sketched: visibility = baseline reach ∨ administer; open spaces confer the Org-member baseline implicitly, closed/private confer nothing implicitly, personal owner-only), `doc.add_guest/remove_guest/list_grants`, the `doc.move` cross-boundary branch, the §8.1a matrix fuzz.
- **2026-06-12** — **ADR 0040 Step 8 slice 2c LANDS: `space.member_add`/`space.member_remove`/`space.member_update_role` — the space slice COMPLETE, one commit** (task #42; dated ADR amendment records the landing decisions; next: Codex's promised review of the whole space slice). **The organizing fact: space membership IS baseline reach** — a `space_members` row is what the Step-6 resolver's `baselineReach` reads, so `member_add` mints read (and, at `role='owner'`, administer) standing; the verbs sit on `space:manage` with `assertCanAdministerSpace` as the bound, over the `GRANT_ROLES` vocabulary (the Step-4 DDL CHECK), and tests pin that a minted/promoted owner-role membership immediately confers roster administration (the ladder reads it back). **Personal spaces hold no roster — `member_add` refuses (`personal_space_membership_pinned`, AFTER authority so kind isn't leaked), remove/update_role deliberately DON'T**: they act on rows that can't legitimately exist there (natural 404), and remove stays kind-blind so a corrupt out-of-band row is REPAIRABLE — the asymmetry is the point (add would CREATE the violation; refusing remove would make corruption permanent). **Subject standing** = live workspace membership (`subject_not_workspace_member`, the permission.grant rule verbatim; the membership row also proves user existence, so slice-1's agents-table caveat has no analogue). **Hard-DELETE postures**: no revive branch (duplicate → 409 `member_already_exists`; `MemberAlreadyExistsError` gained an optional `space_id` — one class/wire-code for both rosters); remove = `DELETE … RETURNING` full preimage (echo + effect carry `workspace_id` + removed `role` per the Codex Step-7 rule), roster-miss → 404 on the USER; update_role keeps `role_unchanged` no-op rejection + re-predicates the UPDATE on the pre-checked role (concurrent change → 409, never silent clobber). **No last-owner guard — deliberately**: the structural admin backstop administers an owner-less team space (archive/restore ride the same ladder), so nothing strands; self-removal allowed (pinned). **Removal ≠ revocation**: doc grants persist (H1; the L1 gate is the kill-switch) — pinned in-suite AND in the replay walk, where a second space's roster row SURVIVES its subject's `workspace.member_remove`. **Routes follow the derived-binding convention** — the parity matrix REJECTED the first body-only draft at landing (`unbound api cell: space.member_add derives POST /spaces/member_add/:space_id` — the forcing function working): `POST /spaces/member_*/:space_id`, path param + JSON body merged via the P3 Param/Body split derived from the one base schema. Replay walk extended: add → promote → remove on the walk space (emptying the roster BEFORE the archive step — the 2b refusal), plus the surviving second-space row; `EXPECTED_STATE_KINDS` += all three. Matrix rows (`space:manage`, rate 60). Suites: capabilities 769 (+52), api-server 283 (+6), prop 47 with the extended walk, contract-tests 5, cli 81, mcp 31, dispatcher 36, auth 12; monorepo typecheck + coherence 0/0. **Next: Codex review of the whole space slice (2a+2b+2c), then the Step-8 remainder** — `doc.add_guest`/`remove_guest`/`list_grants`, the `doc.move` cross-boundary branch, the §8.1a matrix fuzz.
- **2026-06-12** — **ADR 0040 Step 8 slice 2b LANDS: `space.archive`/`space.restore` — the space trash lifecycle, two commits** (task #42 in progress — the member family is slice 2c; dated ADR amendment records the landing decisions). **Commit 1 — a fix-forward the slice itself surfaced.** Deriving `space.restore`'s sibling-slug precondition exposed a latent class bug in BOTH existing restore verbs: the slug unique indexes are PARTIAL (live rows only), so a sibling can legally claim a trashed row's slug — `collection.restore`/`doc.restore` then re-entered the index domain blind, surfacing as a raw `SqliteError: UNIQUE constraint failed` (untyped 500). Reproduced first, then fixed: a step-2b NULL-aware sibling pre-check (excluding self, live rows only) in both handlers → the same typed `SlugCollisionError` (409 `slug_collision`) the create/rename paths raise; route declarations gained the second 409 code via doc.update's `z.enum` two-code pattern (doc.restore's 409 arm was missing entirely — it never declared `parent_deleted` either). `space.restore` is born with the check. **Commit 2 — the slice.** **`space.archive`** (ADR line: "refuses if the Space has live Collections/docs/members; the caller empties first"): 404-first → `assertCanAdministerSpace` → three live-descendant counts → typed **`SpaceHasLiveDescendantsError`** (`has_live_descendants`, 409, `descendant_counts: {collections, docs, members}` so the caller knows what to empty) → trashed-guard UPDATE. The doc count joins **through LIVE collections only** (a live doc stranded under an already-trashed collection doesn't block — corrupt-state honesty); members count ALL rows (no soft-delete on `space_members`; emptying = removing); **grants are neither counted nor touched — they ride through archive (H1 state-as-of-delete), so restore reinstates the exact ACL** (pinned). **`space.restore`** needed authority over a DEAD row, which `assertCanAdministerSpace` structurally refuses (its liveness gate IS the slice-1 anomaly-grant fix) — so the ceiling resolver gained **`canRestoreSpace`/`assertCanRestoreSpace`, the ONE sanctioned dead-row ladder**: the ladder body (`spaceOwnerTierIgnoringTrash` — personal → owner only incl. against admins; team → owner membership ∨ surviving non-guest owner grant ∨ admin backstop) split from the liveness wrapper (`spaceOwnerTier`) so the predicates cannot drift, intent-named so no other call-site quietly acquires trashed-space authority. Handler order: 404 (missing or LIVE) → ladder → sibling-slug pre-check (live usurper → `SlugCollisionError`) → **personal-twin pre-check** (owner already has a live personal space → `ConflictError` — "exactly one live personal space per member" survives the trash round-trip) → trashed-guard UPDATE. Tests pin the grant-yourself-then-leave escape (owner-grant holder archives), grants surviving archive, the personal owner-vs-admin split on BOTH verbs, and replay tracking `deleted_at` through both flips (walk: archive → restore on the same space). `/spaces/{archive,restore}/:space_id` routes; Appendix A += both rows (`space:manage`, rate 10); contract-tests `UI_PENDING` += both. Suites: capabilities 717 (+40 incl. the ceiling's new dead-row describe + the fix-forward regressions), api-server 277 (+5), prop lane 47 with the extended walk, contract-tests 5, dispatcher 36, auth 12; monorepo typecheck + coherence 0/0. **Next: slice 2c — `space.member_add/remove/update_role`** (archive-refuses-on-members makes archive usable on member-bearing spaces only once 2c lands), then Codex's promised at-landing review of the whole space slice.
- **2026-06-12** — **ADR 0040 Step 8 slice 2a LANDS: `space.create`/`space.update` + signup Personal-space seeding — the first space verbs, two commits** (task #42 in progress — archive/restore + the member family are slices 2b/2c; dated ADR amendment records the landing decisions). **Commit 1 — the `/permissions` middleware wiring fix.** Mounting `/spaces` exposed a slice-1 defect: the `/permissions` trunk mount landed WITHOUT `principalMw`/`dispatcherMw` attachment — every live request would 500 on undefined `c.var.principal` (fail-loud, not fail-open; route unit tests inject a fake principal, so only a real-trunk request could catch it). Fixed + a **structural auth-chain walk** (negative-controlled via stash) asserting every mounted capability-domain prefix 401s-never-500s unauthenticated; `/spaces` joined the walk in commit 2. Also learned: **the pre-commit hook tests the WORKING TREE, not the staged snapshot** — the fix could not land ahead of the space tests because untested in-flight files sank the coverage floor; sequencing is now "finish the slice, land the subset commits in order". **Commit 2 — the slice.** New L1 scope **`space:manage`** (member-wide; the administer ladder is the bound) + **`space.create` requires `workspace:admin`** (creation shapes the org; no resource exists for the ladder). **`space.create` mints TEAM only** — `kind` is not an input; the signup hook is the ONLY personal mint path; **no auto-membership row** (one mutation = one audit row; the admin backstop administers the fresh space); slugify + live pre-check → typed `SlugCollisionError`, partial-unique index as race guard. **`space.update`**: patch = `{name, slug, space_type, baseline_access}` via ADR 0034 P3 derivation (ZodEffects has no `.pick()` — one base object + shared refine predicate derives Input/Param/Body schemas, no restated field); 404-first; `assertCanAdministerSpace` (admin backstop on team; personal owner-only incl. against admins — pinned); **personal pins** `space_type`/`baseline_access` (`personal_space_type_pinned` — structurally private; name/slug cosmetic); slug pre-check excludes self; `UPDATE … RETURNING` zero-row → `ConflictError`; effect patch carries ONLY sent fields. **Signup seeding**: a third bootstrap row inside the same `withSystemTx` (`space.create` effect under `system.workspace_bootstrap`) — kind=personal/type=private/owner=user/name "Personal"/slug `personal`/baseline view; **idempotency keys on the `spaces_personal_unique` PARTIAL index, not the PK** (a retry mints a different SpaceId) — and SQLite matches conflict targets to partial indexes statically, so the `kind='personal'` predicate must be an INLINE literal: encapsulated as **`onConflictPersonalSpaceDoNothing` in `@editorzero/db`** next to the DDL (the coherence no-raw-kysely-outside-db rule pushed the `sql.lit` hatch into the db package — the rule working as designed). **Recorded obligation**: the future member-invitation flow must mint per-user personal slugs in shared workspaces. The genesis integration test now pins THREE audit rows replaying to workspace+owner+Personal-space; the dispatcher replay walk drives `spaceCreate` → `spaceUpdate` (name+type patch) through the real dispatcher with replay-vs-DB equality; `/spaces/{create,update/:space_id}` routes (seventh domain on the `hc`-merge chain) + route unit tests; Appendix A gains both rows; contract-tests `UI_PENDING` += both. Suites: capabilities 677 (+34), api-server 272+118 auth-chain (+7 route, +1 walk), auth 12, db 119 (+2), dispatcher 36, prop lane 47 with the extended walk, contract-tests 5; monorepo typecheck + coherence 0/0. **Next: slice 2b — `space.archive`/`space.restore`** (refuse-on-live-descendants; restore needs a dead-row authority path — `canAdministerSpace` structurally fails closed on deleted rows), **then 2c — `space.member_add/remove/update_role`**, then Codex's promised at-landing review of the whole space slice.
- **2026-06-12** — **Codex slice-1 review RETURNED + folded in (same-day fix-forward; task #43).** Brief sent over cmux with five named worry areas; he reviewed it "like a permission boundary, not a feature diff" and returned one MUST-FIX, one SHOULD-FIX, two NOTEs — all dispositioned same-day. **MUST-FIX (applied): anomaly-placed docs refuse EVERY `permission.grant` — repair-first.** He confirmed my top worry against the handlers: the membership-only anomaly posture minted `is_guest = 0` edges with NO standing proof — on `space.restore` they'd become ordinary non-guest claims that crossed the ceiling while it was unavailable (marker-invariant violation: crossings must be guest-marked). Now a typed `ValidationError` (`anomalous_placement_requires_repair`) fires after the authority assert and before the edge lookup, uniform across subject kinds (no agent carve-out) — no write path exists mid-anomaly; revoke stays available on anomaly docs (mints nothing). Both of us independently rejected the restore-revalidates alternative (a restore that silently deletes/demotes grants is worse than "no grants while anomalous"). Regression pinned exactly as he specified plus four more: anomaly grant mints NO row → space restored (CLOSED) → reach-less subject routes to the guest flow — at no point an unmarked cross-ceiling edge; dangling-ref variant; agent-uniformity during anomaly; authority-still-first (plain member gets `acl_deny`, not the repair rail — placement state not leaked); post-restore agent positive control (the refusal was the anomaly, not the doc). **SHOULD-FIX (applied): `permission.list` requires administer-tier** — docs `assertCanAdministerDoc`, spaces `assertCanAdministerSpace`, reach disjunct dropped (whoever can edit the panel can read it). The full panel is the sharing GRAPH (subject ids, roles, grantor attribution, guest markers, timestamps) — `workspace.member_list` went admin-only on the same reasoning and it binds harder here: a cross-space guest reading one doc must not harvest the workspace's sharing graph. Reader-level transparency = a deliberately separate FUTURE redacted capability (participants/avatars, no ids/grantors/markers), never a widening of this one. 3 visibility tests rewritten in place (guest-denied + owner-by-grant-allowed; space owner-role-membership rung pinned distinctly from the admin backstop). **NOTEs (recorded in the ADR's dated review entry):** the agents-table obligation sharpened — existence + same-workspace pre-check at grant time AND a stated disposition for pre-existing inert agent edges; trashed-space revoke wait is intentional — the real offboarding kill-switch is `workspace.member_remove` (L1 refuses removed members regardless of surviving rows; the replay walk pins a grant outliving its subject's membership conferring nothing), edges are hygiene not access. **Codex explicitly kept:** owner-by-grant as transferable file ownership (the re-share hole stays closed by guest-grants-confer-nothing); idempotent re-emit under the same `grant_id` ("this edge exists with this role", not "a row was inserted"); the user-principal-only admin backstop (safe-but-strict — widen deliberately with resolver role-visibility if admin-delegated automation ever needs it). Superseded ADR bullets got in-place pointers; route descriptions updated (grant 400 lists the repair rail; list 403 = administer-tier). Suites: capabilities 643 (+5), everything else unchanged; coherence 0/0; monorepo typecheck clean.
- **2026-06-12** — **ADR 0040 Step 8 slice 1 LANDS: the granting-authority ladder + `permission.grant`/`permission.revoke` — the FIRST ACL capabilities, the FIRST real `acl.grant`/`acl.revoke` emitters, two commits** (task #41 in progress — `permission.list` is the slice's commit 3; dated ADR amendment records every landing decision). **Commit 1 — the granting-authority ladder on `loadDocReadResolver`** (`canAdministerDoc`/`canAdministerSpace` + F88 asserts scoped `{doc_id}`/`{space_id}`, `hasBaselineReach`): doc owner-tier = `created_by` ∨ non-guest `owner`-role doc grant (**guest grants confer NOTHING** — scenario 7, re-share-proof), else by placement — legacy → workspace owner/admin backstop, space → space owner-tier (**PERSONAL: `owner_user_id` ONLY** — the scenario-3 privacy pin holds against admins; TEAM: owner membership ∨ non-guest owner space grant ∨ admin backstop), anomaly → owner-tier only; the **admin backstop is USER-principal-only** (the resolver can't see delegator roles — fails closed for every agent; a delegated agent administers exactly its delegator's owner-tier reach). Deriving the ladder surfaced a **latent bug**: the personal-space OWNER had zero baseline reach (no `space_members` row exists by construction — the CHECK pins `owner_user_id`), so the signup-seeded Personal space would have 403'd its own owner's doc placement; `baselineReach` gained the structural owner term. Preloads widened in place (same ≤4 SELECTs; grants ride `role`/`is_guest`, spaces ride `kind`/`owner_user_id`). Ceiling matrix grew the authority describes (26 new cells: creator × every placement incl. anomalies, outsider all-false, admin legacy/team-true personal/anomaly-false, owner-grantee exactly-its-doc, guest-owner-grantee reads-but-administers-nothing, space-owner-tier × {member, grantee}, delegated-agent collapse without backstop, personal owner-only × admin-false, missing/trashed never, assert deny shapes). **Commit 2 — the two capabilities end-to-end.** `ROLE_SCOPES.member` += `permission:grant`/`permission:revoke` (**L1 scope is coarse; the ladder is the bound** — the doc:write + placement-gate layering). ADR 0034 SSOT: `@editorzero/schemas/permission/{grant,revoke}` + a shared **`GrantRowOutputSchema`** in `shared/grant.ts` (one row shape for grant echo / revoke preimage / future list items; `resource_id`/`subject_id` stay plain strings — polymorphic columns have no single brand). `permission.grant` handler order: resource 404 FIRST incl. soft-deleted (**trash-invisible**, the doc.get posture) → `assertCanAdminister*` → **the user-subject standing rule** (live workspace membership + for space-placed docs baseline reach into that Space *as the subject*, evaluated via a subject-side resolver calling the SAME `hasBaselineReach` — no parallel predicate to drift; failing either = typed `ValidationError` `subject_not_workspace_member`/`subject_lacks_space_standing` **routing to `doc.add_guest`**; legacy AND anomaly placements need membership only — no live ceiling to cross; space-resource grants need membership only — the grant IS the standing; **agent subjects exempt** — no baseline exists to cross, `is_guest=0` stays honest; agent-row existence is a **recorded obligation**, no agents table yet) → **upsert on the unique edge** (guest edge → 409 `ConflictError`, the marker is never silently flipped; same role → idempotent echo, ZERO writes, `acl.grant` re-emitted under the same `grant_id`; different role → UPDATE role only, `created_by`/`created_at` IMMUTABLE; absent → INSERT `is_guest=0` under `ON CONFLICT (edge) DO NOTHING`, zero-row → 409 per the member_add Branch-C posture). `permission.revoke`: grant 404 (`subject_kind: "grant"`; Layer-2 makes cross-tenant ids indistinguishable from absent) → guest edge refused (`ValidationError` → `doc.remove_guest`; no dead-end, no guest edge can exist yet) → authority against the RESOURCE — **trashed-DOC edges ARE revocable** (offboarding: trimming a trashed doc's ACL must not require restoring it) but **trashed-SPACE edges are NOT** (`canAdministerSpace` structurally fails closed → `acl_deny`, restore-first; never bypass the predicate), orphans → 404 on the resource, edge inert until repair → hard DELETE with RETURNING as the authoritative **full preimage** output (H1/Codex-HIGH-1: the echo + `acl.revoke` row are the only durable record). Audit subjects: grant → the RESOURCE (`resource_kind` ∈ `SubjectKind` by construction), revoke → the GRANT. Registry + barrel + EXPECTED_IDS; hand-written `/permissions/{grant,revoke}` routes (sixth domain sub-app on the `hc`-merge chain) matching the derived bindings; CLI/MCP ride the registry; contract-tests `UI_PENDING` += both. **The replay property now drives the ACL family through the REAL dispatcher** (grant → role-converge under the same `grant_id` → idempotent re-grant with zero row writes → hard-DELETE revoke → a second edge that SURVIVES the walk past its subject's `member_remove` — grants persist through membership soft-delete, revocation is explicit) with replay-vs-DB deep-equality after every step + `acl.grant`/`acl.revoke` joining `EXPECTED_STATE_KINDS` — **the Step-7 deferred ACL-audit-replay obligation, closed**. Suites: capabilities 613 (+77; 65 new permission tests incl. the unique-edge fact — ONE row per edge, guest-ness is a property not a parallel row), api-server 256 (+8 route tests), prop lane 47 green with the extended walk, schemas 100% lines, dispatcher 36, cli 81, mcp 31, api-client 21, contract-tests 5; monorepo typecheck + coherence 0/0. **Commit 3 — `permission.list`** landed the same day: resource-oriented `{resource_kind, resource_id}` (a subject-oriented sweep is a future capability with a different authority rule — not conflated); visibility = docs → `assertCanRead` (readers see the panel, guest edges INCLUDED — surfacing escape hatches is what the marker is for), spaces → baseline reach ∨ `canAdministerSpace` (covers the admin backstop without reach; personal spaces stay owner-only on the live panel, the forensic plane is separately see-all); 404-first on missing/trashed (recorded asymmetry: a trashed doc's panel is not enumerable while revoke still works BY ID for offboarding); pagination mirrors `member_list` (peek-limit, composite `(before_created_at, before_grant_id)` cursor, newest-first + id tiebreak) except `before_grant_id` validates v7 AT THE SCHEMA — grant ids are minted v7, the handler brands the cursor half, so a garbage token is a clean 400 not a brand-constructor 500; `workspace:read`, collapsible read. Suites after commit 3: capabilities 638 (+25), api-server 260 (+4), coherence 0/0. **Next: the promised Codex slice review (the grant-authority rule was explicitly queued for his Step-8 pass), then the Step-8 remainder** (`space.*` + signup Personal-space seeding, `doc.add_guest/remove_guest/list_grants`, the `doc.move` cross-boundary branch, §8.1a matrix fuzz).
- **2026-06-12** — **ADR 0040 Step 7 LANDS: the full ACL effect family — three commits, one family each, replay-sufficiency proven before any emitter exists** (task #39; dated ADR amendment records every landing decision). **Commit A — the space family** (`space.create/update/archive/restore`, `space.member_add/remove/update_role`): `space.create` carries the full `SpaceState` post-state with the spaces row's `kind`/`type` riding as **`space_kind`/`space_type`** (`kind` is the union discriminant; the reducer maps back); `space.update`'s patch is the **mutable subset only** `{name, slug, space_type, baseline_access}` — `space_kind` is structural and `owner_user_id` is pinned by the personal-space CHECK; `space.archive` carries the handler clock (the HIGH-4 contract); `space_members` replays as a **hard-DELETE projection** (add→remove nets to no entry, H1). **Commit B — the grant family**: `acl.grant`/`acl.revoke` **reshaped to mirror `GrantState` exactly** — legitimate, not an append-only violation: both kinds were `deferred` (no transition) and no shipped capability ever emitted them, so no persisted row carries the sketch shape; flat `resource_kind ∈ {space,doc}`/`resource_id` (B1's table shape supersedes the sketch's nested scope union), `subject_kind ∈ {user,agent}`, the `GrantRole` ladder, `is_guest: 0|1` (the guest-grant marker), `created_by`; **`acl.revoke` carries the FULL row preimage** (the row is hard-deleted — the audit body is the only durable forensic record); re-grant under the same `grant_id` converges (role-change posture); **`{space_id}` joined the `acl_deny` scope union** (`@editorzero/errors`, the Step-7 line item). **Commit C — the placement lockstep**: `doc.move` gained **optional `acl_transition {policy: adopt_baseline|keep_grants, before_space_id, after_space_id, dropped_grant_ids}`** — the §7 compound effect keeping a cross-boundary move exactly ONE audit row (invariant 3); `dropped_grant_ids` is the exact hard-deleted set, replay removes precisely those keys; absent on every same-bucket move (= every move the shipped handler performs — the Step-6 rule denies the rest; the Step-8 cross-boundary branch is the sole future emitter). `collection.create` += `space_id` (handler writes the column explicitly — null until Step 8 adds space-targeted creation + parent inheritance) and `collection.move` += `new_space_id` (echo; the handler never rewrites bindings), with **`CollectionState.space_id` joining the §9.1 projection IN THE SAME COMMIT** — the Step-4 amendment's deferred bullet, closed per its own lockstep rule. Reducer-side, `collection.move` **rebinds the WHOLE subtree** by BFS over the projection's own parent links — the denormalization invariant (a descendant always carries its root's binding) makes the single `new_space_id` sufficient post-state for every descendant, **unit-proven before any capability can emit a non-null binding**. All nine kinds **born `"state"`** with transitions + `STATE_KIND_FIXTURES` total-record entries (a future kind without a fixture fails tsc; every fixture must apply to `EMPTY_STATE` without throwing) + semantic walks (lifecycle, net-to-empty, re-grant convergence, subtree rebind, adopt_baseline/keep_grants/absent grant handling, absent-entity no-ops for truncated-log resilience). Capability outputs echo the new fields through the ADR 0034 SSOT schemas (collection create/move outputs += `space_id`/`new_space_id`); the dispatcher replay-prop's `projectFromDb` SELECTs `collections.space_id` so live-vs-replayed equality covers the column; §16.3 catalog mirrors all shapes. Coherence Check 6 is matrix→union for IMPLEMENTED capabilities only, so effects-before-capabilities is green by design (Appendix A already maps `permission.grant/revoke` → `acl.grant/revoke` for Step 8). Suites: audit 52 (+21), capabilities 536, api-server 248, dispatcher 36, prop lane 47, cli 81, mcp 31, contract-tests 5; monorepo typecheck + coherence 0/0. **Post-landing Codex pass (inside the reshape window — shapes are append-only the moment Step 8 emits): two HIGHs applied** — `acl.revoke` += `workspace_id`/`created_by` and `space.member_remove` += `workspace_id`/removed `role` (both the hard-delete-preimage class: after the DELETE the audit row is the only durable record, so it carries the FULL row preimage; reducer still removes by key) — **and one MEDIUM**: §16.3's hand-mirrored effect union had drifted (six stale shapes), so the mirror is retired for a canonical pointer to `effect.ts` + the architectural contract only. **The audit-read side-channel fork is DECIDED in the same exchange (cross-model concurrence): see-all-by-design** — `workspace:admin` floor, never widens to member, personal-space privacy = privacy from peers/reads not from the forensic record, future softening = UI redaction never resolver-filtering (recorded in the Step-8 ship-gate bullet) — which clears that gate for Step 8. **Next on this track: Step 8 — the ACL capabilities** (`permission.grant/revoke/list` first under the three-green ship-gate, then `space.*`, `doc.add_guest/remove_guest/list_grants`, the `doc.move` cross-boundary branch, signup seeding the Personal space).
- **2026-06-12** — **ADR 0040 Step 6 LANDS: `workspaceAwareGate` + the doc-read ceiling resolver — the BLOCKER-class slice, Codex + frontier red-team gated** (task #38; dated ADR amendment records every landing decision). **Two composed seams, per H4/H10.** Gate-side: `workspaceAwareGate({ loadDelegatorRoles })` in `@editorzero/dispatcher`, production-wired in `getApiApp` reusing the auth resolver's `createLoadRoles` (one role source, two consumers) — lands the **H8 `acting_as` ∩ delegator intersection**: a delegated agent's effective scopes are `agent.scopes ∩ roleScopes(delegator)` against the LIVE membership row at check time (removal cuts outstanding tokens instantly; demotion narrows them), with the new structural `DenyReason` kind **`delegator_not_member`** so a stale delegation is never reported as a misleading `missing_scope`; check order cross_workspace → human_only → delegation → scope arithmetic, structural checks first so no DB lookup for cross-tenant/human-only rejects; `scopeOnlyGate` survives as the dependency-free unit-harness default. Handler-side: **`loadDocReadResolver`** (`packages/capabilities/src/acl/ceiling.ts` — the SOLE read authority, invariant 5) preloads ≤ 4 single-table plugin-scoped SELECTs and evaluates purely in memory; denies throw F88 `PermissionDeniedError` with the pre-reserved **`acl_deny`** reason → 403 + deny audit row. **The formula as landed:** `created_by` always reads (implicit permanent owner, both modes); doc-grants (guest or not) read in both modes; `'private'` = creator + doc-grants ONLY (space grants/baselines are baseline-tier — private cuts that tier); `'space'` adds membership + space-grants + the open-space Org baseline (**user subjects only** — agents are not Org members); **no workspace-role bypass** (an OWNER 403s on a closed-space doc, proven end-to-end in auth-chain); **delegated agents evaluate the ceiling as their delegator** (own agent-grants ignored while delegated — `agent ∪ delegator` would exceed the human). **NULL-space = workspace-legacy baseline** (root docs + unspaced collections read as the pre-Spaces world ⇒ **zero observable narrowing on live data**: all 191 pre-existing doc-handler tests passed untouched); dangling collection/space refs + soft-deleted spaces **fail closed**; soft-deleted collections still BIND (restore evaluates stored placement); a doc's Space is ONE denormalized hop — `parent_id` is never walked, making space_id denormalization a **binding Step-7/8 obligation**. **Wiring:** `doc.get` asserts after its 404 (trash stays invisible) and before `ctx.transact`; `doc.list` FILTERS (no per-doc deny rows); all 7 doc mutations assert post-fetch — content-path `update`/`rename` gained a ceiling pre-read BEFORE their first write (auto-commit handle; late deny can't roll back), proven by side-effect tripwires; **placement-changing writes are gated (Codex review, both HIGHs applied pre-commit)** — the resolver carries a placement-authority API (`placementOf` → legacy|space|anomaly; `canPlaceIn` = baseline-reach ONLY, since `created_by`/doc-grants are doc-scoped and no doc exists yet): `doc.create` asserts `canPlaceIn(collection_id)` after the target 404 and before ANY write (an outsider with `doc:write` could otherwise plant a doc inside a closed Space and become its permanent implicit owner via `created_by`; root creates skip the resolver load), and `doc.move` enforces a **bucket-transition rule** — only legacy→legacy and same-space→same-space survive; every cross-bucket move (closed→root would WIDEN to the legacy baseline; anomaly placements immovable even for the creator) throws `acl_deny` off the already-loaded resolver (zero extra queries; the Step-8 ladder + `acl_transition` audit field replace the blanket prohibition); `collection.*` stays ungated until collections are space-linked (recorded, incl. the delete-cascade surface). **Metadata-only tx pin 6 → 10** (4 in-tx ceiling SELECTs, snapshot-consistent; per-table tag asserts added). `AccessPath` += inert `space_id`/`collection_id` (forensic — the resolver derives placement from rows, never the caller's claim). **Verification:** ceiling matrix 14 (every subject × placement cell incl. anomaly arms + the placement-authority matrix — CREATOR places identically to a tie-less member, proving creator status is doc-scoped), handler-wiring proof 26 (one auditable list of gated handlers + byte-identical-row deny checks + the create/move placement cases: outsider-into-closed rejected rowless, member/grantee/delegated-agent allowed, agent denied even on OPEN spaces, cross-bucket moves denied for everyone incl. the creator), gate unit 14 (8 new H8 cases incl. both narrowing directions + no-lookup-on-structural-deny), **H8 at the composition seam (Codex MEDIUM closed): the REAL `workspaceAwareGate` + REAL `createLoadRoles(driver)` injected into `createApiDispatcher` exactly as `createApiServer` wires them, driven by a synthetic delegated principal — `delegator_not_member` deny + deny audit row with dual-identity attribution, guest-delegator intersection surfacing the INTERSECTED scope set, member-delegator allow**, end-to-end 403-acl_deny audit-row test, and the **resolver-grade §8.1a fuzzer**: 60 seeded two-tenant worlds/commit, resolver-vs-independent-oracle over every subject × doc, **H6 cross-tenant grant garbage both directions** (sharpest case: host-subject grant row in the FOREIGN tenant pointing at a host doc — catches lost workspace scoping on the grants preload), oracle-independent privacy sweep; **mutation-tested at landing** (dropped grants subject-match and broken delegated-identity both caught at seed=1). **Deferred, recorded:** ACL-audit-replay property (no grants/spaces-mutating capability until Step 7/8) and the full §8.1a capability-matrix × dual-driver fuzz (Step 8). Suites: capabilities 536 (+40), dispatcher 36, api-server 248, prop lane 47 (incl. fuzzer), db 115, cli 81, app 84, sync 50, audit 31, contract-tests 5; coherence 0/0; monorepo typecheck clean. **Next on this track: Step 7 — the ACL effect family** (spaces/grants effects + replay projection extension, `CollectionState.space_id` joins the projection with its first effect), **then Step 8 — the ACL capabilities** (space.*, permission.grant/revoke, doc.add_guest/remove_guest, signup seeding, mode switch).
- **2026-06-12** — **ADR 0040 Step 5 LANDS: the `visibility` de-overload — `access_mode` + `published_slug`/`published_at` + `render_version`, across every layer and surface** (task #37; dated ADR amendment records every landing decision). **The split:** `docs.visibility {workspace,public,private}` is **retired**; `access_mode ∈ ACCESS_MODES = {space,private}` (read scope — **not caller-settable**: `doc.create` hardcodes `'space'`, the mode switch is the Step-8 ACL capability, and the strict-reject probes now pin BOTH `access_mode` and the legacy `visibility` key to 400 at the wire) + the orthogonal **publish pair** `published_slug`/`published_at` ("published" = `published_at IS NOT NULL`), + `visibility_version` → **`render_version`** (architecture §3.5 line had pre-authorized the rename decision). `docs_published_slug_unique ON (workspace_id, published_slug) WHERE published_slug IS NOT NULL AND deleted_at IS NULL` in both dialects under the H5-fixed Check 7. **Publish semantics (the slice's real decisions):** `doc.publish` SELECTs the doc + the live published taken-set *inside the write-path tx* and mints from the internal slug with `-2`/`-3` collision suffixes (per-collection-unique internal slugs vs workspace-unique published slugs; SQLite single-writer serializes the mint, the Postgres race is index-backstopped); **idempotent re-publish keeps BOTH the URL and the original `published_at`** (only `render_version` bumps — F5 always-bump); **`doc.soft_delete` CLEARS the publish pair** (a trashed doc leaves the public site instantly, the URL releases, restore-time collision class eliminated) and **`doc.restore` never republishes** (deliberate `doc.publish` required); unpublish releases the URL (not parked — squatting prevention). **Outbox event renamed** `doc.visibility_changed` → **`doc.publish_changed`** `{doc_id, published_slug, published_at, render_version}` (nulls = "no longer served"), publish/unpublish-only; the **render consumer + `(public)` route stay deferred** (§12 jobs runner / §7 markdown projection / anonymous principal don't exist — the event is the seam, same documented-deferral posture as the original publish slice). Per **H9, no Space-scoped listing shipped** — `doc.list`/`doc.get` stay resolver-ungated until Step 6. **Replay:** `DocState` = `access_mode` + the pair (the publish effect carries the handler-computed pair — the doc.rename-slug "effect carries every handler-computed value" contract; `render_version` stays out, boundary item 4); reducer's `doc.soft_delete` nulls the pair unconditionally; the prop walk gained **re-publish** and **publish→delete→restore** steps so the idempotent-reuse and clear/no-republish transitions are proven against the live DB, not just unit fixtures. **Atomicity prop re-pinned:** the metadata-only tx is now exactly **6 queries** (2 mint SELECTs + UPDATE + audit INSERT + 2 outbox INSERTs) with a new `countSelects` tag assertion — moving the mint SELECTs out of the tx re-opens the Postgres race and fails the baseline. **Web UI mapping:** chip label = read scope (`docAccessModeLabel`, vocab-locked "Space"), green `st-pub` modifier = `published_at IS NOT NULL` (`docTagClass`) — the two dimensions render orthogonally; column header "Visibility"→"Access"; e2e Space-chip assertion unchanged-green. **Sweep scale:** ~30 files of fixtures/assertions across capabilities (496), api-server (244 — incl. auth-chain wire assertions like minted `paula-s-doc`), dispatcher unit+prop (28+42), db unit+integration both backends (115+40), sync (50), cli (81), apps/app (84 + typecheck via REBUILT api-server→api-client materialized type), e2e spec prose. Coherence 0/0 (Check 7+11 verify the rename in lockstep), monorepo typecheck clean. **Next on this track: Step 6 — `workspaceAwareGate` (BLOCKER-class; Codex + red-team gated; F88 handler-deny channel committed by the ADR).**
- **2026-06-12** — **ADR 0040 Step 4 LANDS: `spaces`/`space_members`/`grants` schema slice + coherence Check 11 + §9.1 replay-tuple extension** (task #36; dated ADR amendment records every landing decision). **Three lockstep files at ONE commit** (`schema.ts` + `sqlite-ddl.ts` + `postgres-ddl.ts`; greenfield `ensureSchema`-only, no migration): **`spaces`** — kind∈{team,personal} tied to `owner_user_id` by table CHECK `(kind='personal') = (owner_user_id IS NOT NULL)`; type∈{open,closed,private}; **`baseline_access` ∈ GRANT_ROLES minus `owner`** (the implicit role an *open* space confers on non-member Org members; Step-6 resolver is the sole consumer); `UNIQUE (id, workspace_id)` as the F99 composite-FK target; partial-unique `spaces_slug_unique` + `spaces_personal_unique` (≤1 live personal space per member — the Step-8 signup seed relies on the *constraint*), all visible to dialect parity only because of the Check-7 H5 fix. **`space_members`** — composite PK; **role on the GRANT_ROLES ladder, not workspace ROLES** (the ceiling rule compares membership against grants, so one ordered vocabulary); composite FK `(space_id, workspace_id) → spaces(id, workspace_id)` makes wrong-tenant pairing unrepresentable; hard-DELETE. **`grants`** — single polymorphic ACL table, **no FK by design (H6)**; `grants_edge_unique` + `grants_by_subject` (`is_guest` included → guest enumeration is index-only) + `grants_by_resource` (Codex MEDIUM 5) in the same commit; **hard-DELETE H1 honored at the DDL level** (no `deleted_at`/`updated_at` columns exist to misuse); `is_guest` INTEGER 0/1 in **both** dialects (Kysely `0 | 1`). `collections.space_id` nullable, no FK (B1 — pure nav, never a grant source). All three in `TENANT_SCOPE_COLUMNS` + three plugin-floor tests (tenant.unit, 33 total) + three dual-backend isolation tests (tenant-tables.integration, real Postgres via testcontainers). **`TENANT_SCOPE_COLUMNS` gained a compile-time lockstep** — `satisfies Record<keyof Database, "workspace_id" | "id">`: a `Database` table missing from the map (typed-reachable but **unscoped** — the tenant-leak class) now fails tsc, as does an extra map key; stronger than the planned script-only leg. **Coherence Check 11** covers the legs types can't see: `Database`/`SystemDatabase` interface tables+columns ↔ sqlite DDL (Check 7 makes it transitive to Postgres) ↔ scope-column existence in DDL — **all three legs negative-tested** (renamed DDL column / dropped interface key / phantom scope column each produced named errors, then reverted). **§9.1 tuple extended in the same commit as the tables (the ADR's line-167 sequencing):** `PersistentWorkspaceState` += `spaces`/`space_members`/`grants` (+`spaceMemberKey`); reducer untouched — no `space.*` effect exists until Step 7, so a correct replay yields empty maps and the live tables are provably empty (no capability writes them before Step 8); replay-prop `projectFromDb` now SELECTs all three so the deep-equal goes honest the moment anything touches them. `GrantState` carries no `deleted_at` (revoke REMOVES the key — grant-then-revoke nets to no entry). **`CollectionState.space_id` deliberately deferred** to the Step-7 effect commit (a projected column no effect carries would fail replay at first fixture touch — documented in `state.ts`). `SPACE_KINDS`/`SPACE_TYPES` pinned beside `GRANT_ROLES`. Drive-bys: postgres.unit's hand-kept DROP list gained the trio (it failed **loudly** — `relation "spaces" already exists` — exactly its design; keep-in-sync comment added) and two dead `useLiteralKeys` suppressions cleared. Suites: db 115 unit + 40 integration (both backends, Docker), dispatcher 28 + prop-lane 42, audit 31, scopes 12 (2 new pins), monorepo typecheck clean, coherence 0/0. **Next on this track: Step 5 — reader-path slice** (`published_slug`/`published_at` + partial unique index; the `visibility` → `access_mode` + `published_*` de-overload; outbox→render consumer; no Space-scoped listing before the Step-6 resolver).
- **2026-06-12** — **ADR 0039 PWA layer LANDS — installable, offline-read shell, SSOT-derived SW boundary** (task #35; dated amendment on the ADR records the empirical refinements). **`vite-plugin-pwa@1.3.0` + `workbox-*@7.4.1`** exact in the catalog (1.3.0 published 2026-05-05 — past the cooldown; workbox-build/-window are required peers). **Hand-authored `apps/app/src/sw.ts`** (`injectManifest`, never `generateSW`): shell-only precache (html + every hashed js/css + ONLY the three runtime-loaded latin `@fontsource` subsets + icons — ~923 KiB), `NavigationRoute` denylist **derived** from `RESERVED_API_PREFIXES` in `src/lib/sw-denylist.ts` (boundary regex `^<prefix>(?:[/?]|$)` because workbox matches `pathname + search` — unit test pins the semantics), **zero runtimeCaching** (authed JSON/auth NetworkOnly by omission), no navigation preload, SKIP_WAITING only on message. **`registerType: 'prompt'`** end-to-end: `injectRegister: false`, single registration site `components/pwa-prompt.tsx` (`useRegisterSW`), update toast with Reload/Later, SW lifecycle surfaced as UI state (zero-console gate; ADR 0019 keeps the SW out of OTel), best-effort `storage.persist()`. **Install UX both paths** (`lib/pwa.ts` policy, 100% covered): `beforeinstallprompt` capture → Install card (verified firing live on Chromium against the built app), iOS one-time Share→Add-to-Home-Screen hint (iPadOS-as-macOS detected via multi-touch probe), both gated off when already standalone. **Manifest** from Meridian Zero tokens (`#f4f6f8` paper, stable `id:'/'`, `start_url:'/?source=pwa'`, 192/512/512-maskable icons rasterized from the crosshair mark into `public/icons/` + apple-touch-icon + apple-* metas, `viewport-fit=cover`). **e2e: the lane now BUILDS the SPA and statically attaches it to the trunk** (`EDITORZERO_SPA_DIST` — the ADR 0027/0035 production path is now continuously e2e-proven); 4 new specs on the trunk origin assert SW activation + precache contents (this is the ADR's **Rolldown smoke**, green), reserved-prefix fetches network-only (offline ⇒ network-error, never shell), reserved-prefix NAVIGATIONS get JSON not shell, axe on the production-served login. `virtual:pwa-register/react` verified a true no-op under `vite dev` (read the shipped dev stub) — dev loop + dev-origin specs stay SW-less. 20/20 e2e, 84/84 app unit, app coverage 100/98.19/97.05/100.
- **2026-06-12** — **ADR 0038 Slice B LANDS: browser editor route + `doc.get`/`doc.update` ui cells — task #34 complete.** Route is **`/doc/$docId` — singular deliberately**: `/docs` is a `RESERVED_API_PREFIXES` entry (ADR 0035 §2), so the client path may not collide (header comment documents the why). Loader warms `docQueryOptions` via `ensureQueryData`; panel header renders the docs-row title + slug **read-only** (title-drift seam documented in-file: editing the canvas h1 does not bump `docs.title` until the `doc.rename` ui cell lands); `<DocEditor key={docId}>` remounts per doc. **`DocEditor`** runs Tiptap over the *same* owned extensions the server schema compiles from (`editorExtensions()`), `immediatelyRender: true`; explicit **Save** = `pmDocToBlocks(editor.getJSON())` → `buildSaveOps` (hashes every base block → `diffBlocksToOps`, per-block `expect_prior_content_hash` preconditions) → ONE `doc.update` POST → **re-base** = `fetchQuery` + `setContent` (adopts server-minted ids; next diff is against what actually persisted). 409 → conflict alert + Reload-and-discard (v1 policy; merging is track-changes territory). Policy/transport seam unit-tested in `lib/doc-editor.ts` (fake client; app coverage 100/97.72/96.42/100); the component is e2e-proven by the **double-marked** Playwright spec (`proves-capability-cell: doc.get` + `doc.update`) — list-row → editor render → typed edits → ops → full-reload-from-server asserts. `UI_PENDING` 23→21. **Two live visual-smoke catches** (dev-browser, both themes): (1) **the post-save re-base served the route loader's cached snapshot** — app default `staleTime: 30_000` meant `fetchQuery` returned PRE-save blocks: canvas wiped to the loader state, `baseRef` desynced from the server (next save would 409 against nothing). The e2e spec passed because it only asserted status + post-reload state — **hole closed**: spec now asserts the canvas right after Save; fix = `staleTime: 0` on the re-base fetch. (2) **`.win` floored at 560px** — a brand-sheet artifact (the SSOT frames `.win` inside fixed screen-tabs); the app shell now claims `min-height: 100dvh` via an `app-shell.css` override — the SSOT copy stays byte-identical (coherence Check enforces; first edit of the copy was rejected by the gate and moved to the app layer, where chunk-css order guarantees the override: entry sheet loads before the `_authed` layout chunk, verified in the built bundle). **Mechanics:** `@editorzero/blocks` gains `WireBlockSchema` + `parseBlocks` (strict wire-shape parse that re-canonicalizes content — the SPA never trusts `blocks: unknown[]` structurally) and **`"sideEffects": false`** (without it the barrel's tiptap re-exports landed in the SPA *entry* chunk — now entry 380 kB, editor route lazy 290 kB); pm.ts JSON interfaces made deliberately mutable (Tiptap's `JSONContent` rejects readonly arrays); pnpm-workspace `overrides` pin `@tiptap/extension-{bubble,floating}-menu` to 3.24.0 (`@tiptap/react` declares them as *ranged* optional peers — they drifted to 3.26.0 past the exact-pinned core).
- **2026-06-12** — **ADR 0038 Slice A LANDS: owned block layer + DOM-free server write path; BlockNote + happy-dom evicted from the trunk** (task #34 first half; ADR 0038 dated amendment records every refinement). **New `@editorzero/blocks`** — the owned editor substrate: `model.ts` (canonical `Block`/`StyledText`, `normalizeContent` one-spelling rules, `id:""` = unminted browser-block sentinel), `tiptap.ts` (the owned schema via `Node.create`/`Mark.create` from `@tiptap/core` directly — doc/text/paragraph/heading + bold/italic/code with `excludes:"_"`; `getEditorSchema()` compiles it DOM-free for the server), `pm.ts` (block JSON ⇄ PM JSON, loud throws outside the v1 tier), `ops.ts` (**op-semantics SSOT**: `applyOpsToBlocks` server applier + `diffBlocksToOps` browser diff in ONE module — props patches shallow-merge then reparse under the target type's zod attribute schema; reorders lower to remove+insert; LCS-stable insert anchoring emitted in reverse doc order), `hash.ts` (isomorphic WebCrypto SHA-256 over canonical JSON for `expect_prior_content_hash`, known-vector-pinned against `node:crypto`). **`@editorzero/sync` rewritten:** `readBlocks`/`writeBlocks`/`seedBlocks` ride `@tiptap/y-tiptap`'s `updateYFragment` — diff-in-place against the live fragment, ONE `ydoc.transact` per write → exactly one `doc_updates` row (§6.5), equality-matched children keep Yjs identity/history — all **pinned by unit tests** (update-event count + `fragment.get(1)` object identity); `setDocTitle` is now synchronous in-place block-0 replacement; `live-editor.ts`/`dom-shim*` **deleted**, `ensureDomGlobals` gone from the api-server boot — **no DOM shim anywhere, prod or test** (stronger than the ADR predicted). `DOC_FRAGMENT` value `"document-store"` documented as durable format. **Pins:** `@tiptap/{core,pm,react} 3.24.0` + `@tiptap/y-tiptap 3.0.4` exact in the catalog (3.26.1/3.0.5 published 2026-06-11 = inside the cooldown); lockfile resolves a **single** `prosemirror-model@1.25.4`; blocknote refs in the lockfile: zero. **The law test:** `apply(before, diff(before, after)) ≡ after` (modulo minted insert ids) swept over 140 seeded cases **through the real `DocUpdateInputSchema` parse** — diff output is proven to validate against the wire contract; a second sweep attaches real hashes and proves no spurious 409s on a clean base. **Wire-shape deltas** (consumers: none broken — all suites): BlockNote's default-prop bag (`textColor` etc.) dropped from `props`; the mount's "normalisation-tail" empty paragraph is gone (the full-stack e2e now asserts exactly 3 blocks post-insert, was 4). **Tooling decision:** Biome `useLiteralKeys` → `off` — it forbids the only spelling TS's `noPropertyAccessFromIndexSignature` allows on index-signature types (`Block.props`, PM `attrs`); the standoff previously dodged case-by-case (`setAttribute`, `in`-narrowing) became unavoidable in a layer whose job is open prop bags; the TS flag is the safety-bearing one. Suites: blocks **124** (new pkg 99.4/95.2/100/99.4), sync 50, capabilities 494, api-server 244, dispatcher 28, contract-tests 5/5, apps/server 21; monorepo typecheck clean; coherence 0/0. AGENTS.md gotchas rewritten (owned write path / DOC_FRAGMENT / pre-minted ids / tiptap lockstep-pin entries replace the BlockNote ones). **Next (Slice B, the cell half):** `/docs/$docId` route in apps/app (`@tiptap/react` over the same owned extensions), doc.get loader + explicit-save diff→ops mutation with 409 surface, `doc.get`/`doc.update` ui cells + `UI_PENDING` ledger moves + `proves-capability-cell` e2e markers + axe.
- **2026-06-12** — **Production static attach + docker artifact LAND** (ADR 0012/0027/0035; verification-stack step 8 lights up). One container now serves the whole single-box product; `pnpm smoke:deploy` proves it end-to-end (health → SPA shell at a client route → immutable hashed asset → genesis sign-up → `doc.create`→`doc.list` round-trip → reserved-prefix honesty → teardown), run green before commit. **SSOT move:** `RESERVED_API_PREFIXES` + `isReservedApiPath` relocate from `apps/app/src/lib` to **`@editorzero/constants`** — consumers now span browser and node (dev Vite proxy, client-route guard, trunk SPA fallback, the future ADR 0039 PWA denylist); registry-derive TODO carries over. `vite.config.ts` imports it by **subpath** (`@editorzero/constants/reserved-prefixes`): Vite externalizes bare specifiers at config-eval, so plain node ESM loads the module — the package root's extensionless `module:Preserve` re-export chain is node-unloadable, while the leaf module is import-free **by documented contract** (a new face of the extensionless-dist gotcha: any internal import added to that module breaks the dev loop + e2e lane). **`attachSpa`** (`apps/server/src/spa.ts`, wired in the bin behind new optional `spa_dist` config / `EDITORZERO_SPA_DIST`; unset → trunk-only, dev/e2e unchanged): file pass over `apps/app/dist` (hashed `/assets/*` → `public, max-age=31536000, immutable`; shell + root files → `no-cache`) + GET-only `index.html` fallback gated by `isReservedApiPath` — an unmatched path under a trunk prefix stays an API-shaped 404/401, never HTML. **Upstream pairing bug found:** the documented `serveStatic` `onFound` + `c.header()` pattern silently drops the header with hono 4.12.14 (post-finalize `c.header()` swaps a NEW Response into the context while serveStatic returns the captured old one) — headers are set on the *returned* Response instead; the integration suite pins that, absolute-root support, and traversal safety (10 tests). **Bundler canonicalized:** `apps/server/scripts/bundle.mjs` (parameterized `--out`) replaces `packages/e2e/scripts/bundle-trunk.mjs` — one esbuild config; e2e calls it with `--out tmp/server.mjs` (14/14 green); `better-sqlite3` becomes a real *prod* dep of `apps/server` (the artifact requires the native external at runtime; `@editorzero/db` keeps its inject-from-consumer dev posture). **Docker:** multi-stage `Dockerfile` — corepack-pinned pnpm 10.34.1; in-image `npm pkg delete scripts.prepare` (lefthook's hook install needs the git repo the image deliberately lacks — scripts aren't part of the frozen-lockfile check); `pnpm deploy --prod --legacy` provides the lockfile-faithful native closure (pnpm 10 requires `--legacy` without injected workspaces; **`--ignore-scripts` on deploy is a trap** — it silently drops the built `.node` binding from the deployed tree); runtime stage = slim, non-root `node`, `HEALTHCHECK` via node-fetch against `/infra/health`, SIGTERM drain already in the bin. `docker-compose.yml` per ADR 0012's default template (env-driven port + data dir; `BETTER_AUTH_SECRET` required with no baked default; registration `first-user` = the audited genesis — ADR 0012's installer-URL sketch superseded, dated amendment added; AGENTS.md step-8 row corrected to `/infra/health` + the manual-lane decision, image-build-per-push is too heavy for pre-push — phase-boundary revisit). The cohost smoke's "fold static mount into the serve layer" follow-up is closed; the WS mount stays gated on #15. Suites: apps/server 13 (10 new spa), constants 3 (moved), config 7, apps/app 59, e2e 14/14. **Next:** editor HTTP-first (0038) → PWA (0039).
- **2026-06-12** — **ADR 0040 Step 3 LANDS: additive tenancy vocabulary + coherence Check-7 index-parity fix** (`4a54d9e`; H5/H12; the parallel track to #13). **Zero behaviour change; registry untouched.** `@editorzero/ids`: `SpaceId`/`GrantId` v7 brands + generators (`TeamId` waits for the Teams slice; **no `OrgId`** — multi-org is a product non-goal). `@editorzero/scopes`: `SUBJECT_KINDS` += `space`/`grant` (audit subjects for the Step-7/8 effects; the only schema consumer is the audit-list filter enum, so the widening is additive); **`GRANT_ROLES = [owner,edit,comment,view]`** — deliberately distinct from the workspace `ROLES` vocabulary (fork #3; they share the word "owner") and **positive-only** (fork #5, no deny member); `ACCESS_MODES = [space,private]` (becomes `docs.access_mode` at Step 5); **11 reserved Step-8 Model B mutators** appended to `METADATA_ONLY_CAPABILITIES` (`permission.grant/revoke`, 7× `space.*`, `doc.add_guest`/`remove_guest`) — none is dispatchable until its `registerCapability` lands, so membership now just settles the write-path posture early; architecture §6.5's block updated in lockstep behind a `-- reserved --` divider (Check 3 keeps the two lists equal; the divider's tokens don't match the id grammar, so the parser drops them). New `grant-vocabulary.unit.test.ts` pins both vocabularies' membership, the ROLES distinctness, and the reserved ids' grammar. `@editorzero/schemas`: `SpaceId`/`GrantId` Input/Output schemas, `shared/grant.ts` (`GrantRoleSchema`/`AccessModeSchema`), and `shared/principal-ref.ts` — **`PrincipalRefSchema` is INTERNAL-ONLY (H12)**: a nested `{kind,id}` capability-input field would *not* trip the CLI/MCP generators' top-level-ZodObject guard, silently shipping a broken `--subject` flag and a misleading MCP arg, so capability inputs stay flat `subject_kind`/`subject_id` (the `member_add` exemplar); the doc-comment carries the warning. **Check-7 fix (H5, pulled forward from Step 4 per plan):** `scripts/coherence.ts`'s index regex now matches `CREATE [UNIQUE] INDEX … [WHERE …]` and diffs whitespace-normalized **signatures** (UNIQUE-ness + columns + predicate) — the **five partial-unique slug indexes were previously invisible** to SQLite↔Postgres dialect parity (the old regex required bare `CREATE INDEX` and stopped at the column list). **Negatively verified:** dropping `UNIQUE` from `workspaces_slug_unique` and truncating `docs_root_slug_unique`'s predicate in one dialect each produced a named `signature differs` error, then reverted. Suites: ids 17, scopes 10 (6 new), capabilities 494, api-server, cli 81, contract-tests 5/5; full pre-push lane green. **Next on this track: Step 4** — `spaces`/`space_members`/`grants` across the 3 lockstep DDL files at ONE commit, `TENANT_SCOPE_COLUMNS` registration, and the schema↔DDL↔TENANT_SCOPE_COLUMNS coherence check (the `grants` unique tuple + two lookup indexes land in the same commit, guarded by the fixed Check 7).
- **2026-06-12** — **Web UI #13: responsive shell chrome LANDS — Base UI Drawer mobile nav + honest primary nav** (`0e371e3`; ADR 0035/0036/0037/0040). First `@base-ui/react` import (**1.5.0 exact**, install-at-first-import; optional date-fns peers stay out per the 0037 amendment; Drawer API verified at point-of-use against base-ui.com + the installed dists). Before this the token sheet's 1120px breakpoint simply hid `.side` — mobile had **no path to the nav or principal chip**. `SideContent` extracts the sidebar interior so the static desktop `<aside>` and the mobile **Base UI Drawer** render the *same* component (no drift copy); the hamburger is `Drawer.Trigger` (auto aria-haspopup/expanded/controls), the panel a named modal dialog (sr-only `Drawer.Title`, focus trap, scroll lock, Escape/outside/swipe-left dismiss). `app-shell.css` adds only what the mocks kept screen-local (off-canvas panel, scrim, burger) styled via tokens against Base UI's data-attributes (`[data-starting-style]`/`[data-ending-style]`/`--drawer-swipe-movement-x`); the sheet's reduced-motion guard already covers it; the drawer hosts the same `.side` class for interior styles with a higher-specificity display re-assert (the sheet's breakpoint rule targets the static aside). **Primary nav carries ONLY screens that exist** — one entry, "All Documents" → `/` (the doc.list cell), router-active `aria-current="page"`; the mock's Overview/Spaces/Shared/Trash entries join as their routes + cells land (`UI_PENDING` governs; dead links would fail the e2e honesty bar exactly like SSO buttons on the login). Space switcher waits for a `workspace.get` ui cell (whoami has no workspace name). e2e `chrome.spec.ts`: desktop current-marking + hidden burger + axe; mobile drawer round-trip with **axe against the open modal**; nav-click closes + lands. 14/14 e2e; dev-browser smoke both themes. **Next #13:** editor (0038) → PWA (0039); chrome grows per-cell.
- **2026-06-12** — **Web UI #13: `doc.list × Web UI` parity cell + `packages/contract-tests` + honest `surfaces` LAND** (`eb97cfe`; invariant 4, ADR 0033 §3 / 0040 H11). The H11 silent hole is closed mechanically: all 24 capabilities had declared `surfaces:["api","cli","mcp","ui"]` with **zero** ui bindings and nothing that could fail. Now **declared = bound**: 23 capabilities dropped `"ui"`; `doc.list` kept it because its real Web UI binding landed in the same commit. **`packages/contract-tests`** (pre-push `contract` hook now live — its directory guard found `test/`) derives the cross-surface matrix from the registry: **api column** — every api-declaring capability's derived HTTP binding must be a served trunk route (`openApiDocument` walk); the derivation `deriveHttpBinding` **lifted** from `apps/cli/src/generator` into `@editorzero/capabilities` (registry convention → one SSOT module for the CLI generator + the matrix; `getObjectShape`'s cast dropped in the move). **cli column** stays enforced in `apps/cli`'s `parity.unit.test.ts` (generator is app-internal; a copy here would drift — documented in the matrix header). **mcp column** — `isMcpTool` must accept every mcp-declaring capability (catches the humanOnly+mcp contradiction `tool-filter.ts`'s comment defers to "contract-matrix parity failure") + `toToolConfig` must produce a registrable tool. **ui column** — declared ⇔ proven: set equality between ui-declaring capabilities and `proves-capability-cell: <id>` markers in `packages/e2e/test/*.spec.ts`, both directions; the marker is the bookkeeping bond, the marked spec running against the real bundled trunk at pre-push is the behavioral proof. **UI ledger** — every capability is ui-bound or parked in `UI_PENDING` (23 entries, shrink-only, stale entries fail): unbound cells are *visible debt*; v1-complete on the ui column = empty ledger (no-MVP-cut). **The cell itself:** `apps/app/src/lib/docs.ts` mirrors the session.ts split — `fetchDocList` (the docs.list `hc` union carries real error arms unlike whoami's 200-only, so `DocList` derives from the `Extract<…,{status:200}>` success arm), `docListQueryOptions` consumed by BOTH the route loader (`ensureQueryData`) and `useSuspenseQuery` (no loading-state UI at bare-cell stage), and unit-tested presentational policy (wire `"workspace"` renders **"Space"** per the vocab lock; `st-pub` reserved for `public`; deterministic UTC dates). Home route renders the Meridian Zero `.panel`/`.tt` tabular-doc pattern straight from the landed token sheet — **zero new CSS**; shared envelope reader extracted to `lib/wire-error.ts`. `packages/e2e/test/docs.spec.ts` carries the marker and proves the cell against real server data (empty state on fresh DB → two docs created over the same `POST /docs/create` the other surfaces use → slugs + Space chips + dates render, axe-clean both states); founder creds extracted to `test/credentials.ts` (the registration gate means later specs sign in, never sign up). Visual smoke (dev-browser) verified both themes. apps/cli's branch coverage dipped when fully-covered `http-binding` left the package — **backfilled `command.ts`'s untested branches honestly** (bad-id throw, flag forwarding, non-string base-url fallback → 92.14%) instead of touching thresholds. Suites: capabilities 494, cli 81, app 60 (100% lines), contract-tests 5/5, e2e 11/11. **Gotcha worth keeping:** cross-package vitest imports resolve workspace deps to **dists** — after editing a dependency package, `pnpm build` before trusting a consumer's suite (in-package relative imports see source; consumers don't). **Next #13:** Base UI primitives + full chrome (responsive nav + Drawer, install `@base-ui/react` at first import), then editor (0038) → PWA (0039).
- **2026-06-11** — **Web UI #13: sign-in/sign-up keystone + Playwright/axe e2e lane LAND** (`5b5307e` + `4fff0ac`; ADR 0030/0033/0036/0041). The Web UI is now *usable by a human end-to-end*: create an account in a browser, land in the authed shell, sign back in later — proven in Chromium at pre-push. **Sign-in screen** (`5b5307e`): `packages/api-client/src/auth.ts` is the one sanctioned raw-fetch seam for Better Auth's `/auth/*` (outside the typed `hc` surface — ADR 0030; `signInEmail`/`signUpEmail` → `{ok}|{ok:false,status,message}` with the BA `{message}` body projected and an HTTP-status fallback); the SPA-side policy lives unit-tested in `apps/app/src/lib/auth.ts` (`authenticate()` mode dispatch + transport-failure mapping; `safeRedirectTarget()` clamps the attacker-writable `?redirect=` to internal paths — rejects absolute/`//`/`javascript:` forms); `components/auth-form.tsx`+`.css` render the Meridian Zero State-2 card (one card, both modes, foot-line toggle, reveal-password affix, the agent footnote per ADR 0016, a single `role="alert"` failure channel) extracted from `docs/brand/v2/screens/12-login.html` tokens-only; the mock's SSO/"Forgot?" controls are deliberately absent (no server features behind them — dead controls would fail the e2e honesty bar). On success: invalidate the session query (a stale principal must not survive a user switch) → `navigate({href: clamped})` (typed `to` can't express a full internal href). Sign-up doubles as the fresh-install bootstrap (ADR 0041 audited genesis) — no separate first-run wizard. **e2e lane** (`4fff0ac`, verification-stack step 7 lights up): `packages/e2e` boots **two real servers, one browser origin** — the trunk is `apps/server` **esbuild-bundled from source and run under plain `node`** against a fresh SQLite per run (`module:Preserve` keeps every dist extensionless → unrunnable under node, and bun can't host `better-sqlite3` (native, bun#4290); the bundle externalizes `better-sqlite3` + restores `require` for CJS deps via a `createRequire` banner — **also the candidate shape for the production server artifact at the docker slice**), and the apps/app Vite dev server reverse-proxies `RESERVED_API_PREFIXES` to it (`vite.config.ts` honours `EDITORZERO_TRUNK_ORIGIN`). `EDITORZERO_PUBLIC_ORIGIN` = the *Vite* origin — the one the browser sees — so Better Auth's `trustedOrigins` check runs against a real cross-process `Origin` header (ADR 0030's single-origin cookie model exercised, not bypassed). **7 specs green in ~6s**: guard bounce carrying `?redirect=`, axe (WCAG 2.1 A/AA) on the login in both modes, sign-up → audited owner bootstrap → authed shell (principal chip `User`/`OWNER`) + axe, fresh-context sign-in honoring the guard's target, wrong-password surfacing the real BA message via `role="alert"`, and the `ez-theme`→`html[data-theme]` contract incl. dark-theme axe. Pre-push `e2e` step now runs the suite unconditionally (`test:e2e`; the package `test` script is a no-op echo so the fast pre-commit lane never launches browsers); one-time `playwright install chromium` documented in CONTRIBUTING. Pins exact + cooldown-clear: `@playwright/test 1.60.0`, `@axe-core/playwright 4.11.3`, `esbuild 0.27.7` (lockfile-replayed). Visual smoke via dev-browser before commit confirmed the State-2 card renders faithfully (grid paper, registration-mark lockup, agent-wash footnote) in both modes. **Review → hardening commit `b59a5b2` (same day):** Codex cross-model pass on the auth path returned 2 HIGH + 1 MEDIUM, all applied — (i) `safeRedirectTarget` rewritten **canonicalize-then-compare** (raw-`\`/`%5c` rejected pre-parse, `new URL(redirect, base)` + same-origin + `%2f`-in-pathname check, returns re-assembled `pathname+search+hash` never the input; the old prefix checks passed `/\evil.com` → `http://evil.com/`); (ii) **registration gate**: new `registration_mode` config (`EDITORZERO_REGISTRATION_MODE`, **default `first-user`** — sign-up 403s once a user exists; `open` restores self-serve) feeding a *required* `registrationMode` on `CreateAuthOptions`; the gate is a request-level `hooks.before` (`createAuthMiddleware` + `APIError`), **deliberately not** `databaseHooks.user.create.before` — that hook runs inside BA's adapter tx which holds the single pooled SQLite connection, so a count via `driver.system()` there deadlocks (reproduced); the count goes through the new quoted-identifier `countTableRows` in `@editorzero/db` (kysely imports stay in-package per coherence); (iii) rejection-arm e2e `origin.spec.ts` — cookie-bearing cross-origin POST → 403 + no set-cookie (BA's origin check only fires when cookies ride; cookieless = CLI-class traffic, Fetch-Metadata covers browser forms), post-genesis sign-up → 403 + no account minted. The parallel adversarial 4-dimension workflow (security/correctness/test-honesty/conventions, each finding adversarially refuted) independently converged on the clamp and added 4 more, all in `b59a5b2`: lefthook's multi-line e2e `run: |` masked the CLI lane's exit code (sh returns the *last* line's status) → single `&&`-chain; theme specs could vacuously pass axe on an un-rendered code-split page → heading render-anchors first; login ledes said "workspace" (ADR 0040 vocab-lock break) → "instance"; `bundle-trunk.mjs` now runs incremental `tsc -b` before bundling (`@editorzero/*` resolves to **dists** — only the entry bundles from source — so the e2e lane could silently green-light stale builds). 9/9 e2e against the freshly-bundled trunk; 12/12 auth, 52/52 app. **Next #13:** `doc.list × Web UI` parity cell + `packages/contract-tests` + the 23-cap `surfaces` narrowing (ADR 0040 H11 — one commit), then Base UI chrome.
- **2026-06-03** — **Web UI #13: auth guard + bare authed shell frame LANDS** (`389844d`; ADR 0028/0030/0033/0036) — protected routing is real end-to-end with **zero new deps** (plain divs + the landed Meridian Zero tokens + the typed session data-layer). **Router context:** `__root` adopts `createRootRouteWithContext<{queryClient}>` so route `beforeLoad` guards read the cached session from the router context (not a singleton import); the context type going concrete makes `createRouter`'s `context:{queryClient}` **type-required**, so `__root.tsx`+`main.tsx` are one atomic edit. **Guard:** a pathless `_authed` layout whose `beforeLoad` prefetches the principal via `ensureQueryData(["session"])` and, on a **401 only**, throws `redirect({to:'/login', search:{redirect: location.href}})` — so an unauthenticated visit never flashes protected chrome, while 403/5xx/network **re-throw** to the error boundary (the 401-vs-rethrow policy is the pure, unit-tested `lib/auth-guard.ts#isLoginRequired`; the server is the real auth boundary — this is a UX layer that can't be bypassed into access). `routes/index.tsx` moves under the layout → `_authed.index.tsx` (the generator hard-errors on a duplicate top-level `/`). **Shell:** `components/app-shell.tsx` renders the 3-zone frame with real landmarks (`<aside>` nav + `<header>` bar + `<main>` body); the sidebar principal chip is derived by the pure, unit-tested `lib/principal.ts#describePrincipal` — whoami carries **no name/email**, so it shows kind (human square `av--u` vs agent notched `av--agent`) + roles (user) or token-kind + scope-count (agent). **login.tsx** gains a cast-free `validateSearch` for the post-sign-in `redirect` target (the `in`-operator narrowing makes it a *named* access, threading the `noPropertyAccessFromIndexSignature` ⊥ `useLiteralKeys` standoff); the Better Auth sign-in call is a later increment. **Coverage:** `src/components/**` joins `routes/**`+`main.tsx` in the e2e-covered (Playwright+axe) set excluded from vitest unit coverage — the shell's *logic* lives in `lib/**` at the 95/90/95/95 floor (`auth-guard`+`principal` at 100%); **42 app tests, 100% lib coverage**. **Vocabulary lock (ADR 0040) holds** — no `/spaces` route yet (lands at the IA slice), `/login` stays top-level, home copy says "Space" while the API call underneath stays `/workspaces`. Load-bearing router APIs verified at point-of-use before building (`createRootRouteWithContext` curried form, `redirect`, `ensureQueryData` rejects-on-throw vs `prefetchQuery`'s swallow, `validateSearch` on `BaseRouteOptions` — all in the installed 1.170.10). All gates green: typecheck (src+test) clean, vite build emits per-route chunks (auto-code-splitting), coherence 0/0, biome clean, pre-push contract/e2e/lint/typecheck/integration/property. **Landed just ahead** (`f7a9de3`, Slice 0): corrected ADR 0037 + this file — `@base-ui/react@1.5.0`'s `date-fns`/`@date-fns/tz` peers are **optional** (`peerDependenciesMeta.optional:true`, verified), not mandatory, so the next slice installs `@base-ui/react` alone. **Known debt for the parity-cell slice (ADR 0040 H11):** 24 capabilities declare `surfaces:[…,'ui']` but `parity.unit.test.ts` only enforces the `cli` surface and `packages/contract-tests` doesn't exist — the build is green while UI parity is unenforced; the `doc.list × Web UI` cell must add that enforcement, not just a screen. **Next #13:** Base UI primitives + full chrome (responsive nav + Drawer, install `@base-ui/react` at first import) + the Playwright/axe e2e harness → `doc.list × Web UI` parity cell + `packages/contract-tests`.
- **2026-06-03** — **Web UI #13: SPA data-layer + client error vocabulary LAND** (`8cc124d` api-client transport + `1c103d3` data-layer; ADR 0030/0033/0025). The first *data* seam over the typed-RPC client. **api-client transport** (`8cc124d`): `createHttpClient` now forces `credentials:"include"` (before `...init`, so override-able) — the Better Auth Lax cookie rides along on the SPA's same-origin / dev-proxy calls; harmless for the Node/CLI clients (no cookie jar). **Data-layer** (`1c103d3`): `apps/app/src/lib/{api-client,query-client,session}.ts` — a same-origin `createHttpClient({baseUrl:""})` singleton (relative paths → page origin in prod / Vite proxy in dev, cookie auto-attached, no `auth` resolver), a module-level `QueryClient` (`retry:false` so a 401/`permission_denied` surfaces as `isError` immediately, not after 3 retries), and `fetchSession`/`useSession` over `GET /infra/whoami` (the ADR 0025 principal route — **not** BA `get-session`). `WhoamiSession` is **derived from the materialized client type** (`Awaited<ReturnType<…["json"]>>`; no exported wire schema to reuse) — SSOT, and from `ApiClient` alone so apps/app needs no `hono/client` import. The whoami `hc` union is **200-only**; the middleware 401 `{error:"unauthenticated"}` is read **cast-free** on the `!res.ok` arm (the `in` operator narrows the `unknown` body — no `as`) and thrown as `ApiError`. `main.tsx` wraps `RouterProvider` in `QueryClientProvider`. **Client error vocabulary:** new `@editorzero/api-client/api-error.ts` — `ApiError` + `ApiErrorCode` (13-code union derived from one array) + `isApiError`/`isApiErrorCode`, all cast-free; lives in api-client (the shared CLI+UI package), **not** server-only `@editorzero/errors`, as the browser-safe projection of the wire `{error}` envelope. **Drift guard:** coherence **Check 10** fails the commit if `API_ERROR_CODES` diverges from `errorResponse`'s typed `{error} as const` envelopes (the kernel erases the per-route error union behind `hc` → nothing to derive → the mirror needs a guard); `diffLists` generalized with a default `kind` param (Check 3 output unchanged). **deps:** `@tanstack/react-query` exact `5.100.14` (latest `5.101.0` is inside the 3-day cooldown), `@editorzero/api-client` + `@editorzero/ids` (`workspace:*`). **Adversarial review** before commit — a 6-lens workflow (`wf_23550fe4-25e`, 12 agents / 676k tokens: cast-free, runtime-contract, dep/build-graph, Check-10, test-honesty, wiring), each finding verified by a refuter — surfaced **2 confirmed** (both folded in): (i) Check 10's `[a-z_]+` envelope class would phantom-flag a future *digit*-bearing code (e.g. `oauth2_denied`) correctly mirrored in both files → broadened to the `[a-z][a-z0-9_]*` identifier alphabet the sibling parsers use; (ii) `WhoamiSession`'s branded id fields silently resolved to **`any`** because apps/app didn't declare `@editorzero/ids` (pnpm-strict can't resolve the client type's transitive `import("@editorzero/ids")`) — added the dep (matching apps/cli) + a **compile-time guard** in `session.test.ts` (`IsAny<…id>` → `never`) that fails typecheck if the brand ever regresses. 4 findings refuted (incl. two hallucinated scratch-file failures — the probing agents' own `tsc` emit had leaked into gitignored `dist/`, cleaned). All gates green: typecheck clean, 33 app + 13 api-client tests, **100% coverage**, coherence 0/0, pre-push contract/e2e/lint/typecheck/integration/property. **Next #13:** AppShell chrome + Base UI primitives (0037, install-at-point-of-use) + auth guard (`useSession` → redirect) + CommandPalette (cmdk) + Playwright/axe → `doc.list × Web UI` parity cell + `packages/contract-tests` → editor (0038) → PWA (0039). **Vocabulary lock (ADR 0040)** — UI "Space" / route `/spaces` / API `/workspaces` — must precede the sidebar/route tree.
- **2026-06-03** — **Web UI #13: Meridian Zero token cascade + theming runtime LANDS** (`0786ad1` + `4800a32`, ADR 0036/0037). First *implementation* slice of the shell (no editor/data-layer deps yet). **Token cascade:** the two SSOT sheets (`docs/brand/v2/{meridian-zero,themes}.css`) are copied **byte-for-byte** into `apps/app/src/styles/` (self-contained bundle — no cross-package `@import` reaching into `docs/`), and `index.css` loads the whole design system into a **single low-priority `@layer meridian`** — *not* the 4-layer primitive/semantic/component split the synthesis draft implied: the SSOT is one fused tokens+base+components sheet whose AA-remediation tail depends on *source order*, which a multi-layer split would break; one layer preserves it and still lets future component CSS win by being unlayered/higher. **Verified empirically** that Vite 8 inlines `@import … layer()` correctly — built CSS emits `@layer meridian{:root{…}}` with all tokens, all three `[data-theme]` themes, and the AA tail intact. **Fonts self-hosted** as `@fontsource-variable/{space-grotesk,archivo,jetbrains-mono}` (no external fetch; build subsets to woff2). **`theme.ts`** is the post-hydration runtime — `applyTheme`/`getStoredTheme`/`setTheme`/`initTheme` over a `data-theme` attribute + `localStorage`, default (Meridian Zero light) modelled as the attribute's *absence*, graceful fallback on unreadable storage; a tiny inline guard in `index.html` mirrors it pre-paint (no flash-of-default); 100% line/branch/fn coverage via injected root/storage seams. `setAttribute("data-theme")` (not `dataset.theme`) sidesteps the `tsconfig noPropertyAccessFromIndexSignature` ⊥ Biome `useLiteralKeys` standoff. **Drift guard:** coherence **Check 9** fails the commit if a copy diverges from its SSOT (Biome doesn't lint `.css`, so bytes stay stable); the `4800a32` follow-up widens the pre-commit coherence glob to include `apps/app/src/styles/**` so editing the *copy* — the exact drift targeted — triggers the byte-match, not just editing the SSOT. **Correction to the draft plan:** `--bg-hover` is **not** a latent bug — `var(--bg-hover)` appears only in a design-*exploration* mockup (`design-02-phosphor.html`), never in the SSOT sheets, so the speculative "add it to base `:root`" was dropped; define a hover-bg token when a component first needs one. **Supply-chain pre-audit for the next steps** (provenance + cooldown, cutoff ≤ 2026-05-31): `@base-ui/react@1.5.0` (2026-05-19; mui/base-ui, 5.2M wk dl — the canonical scope; `@base-ui-components/react` froze at `1.0.0-rc.0`, *not* a typosquat — same maintainers/repo), `date-fns@4.4.0` (2026-05-29), `@date-fns/tz@1.5.0` (2026-05-21), `cmdk@1.1.1` (2025-03-14), `@tanstack/react-table@8.21.3` (2025-04-14, TanStack/table) — all verified legitimate + cooldown-clear. **Refined the remaining foundation ordering to install-at-point-of-use** (each dep enters in the step that first imports it, audit cited in that commit — no standalone bulk-install commit leaving deps unused for several slices; ~~the date-fns peers still go in `@base-ui/react`'s `add`~~ **[corrected 2026-06-03: the date-fns peers are *optional* and do NOT go in — `@base-ui/react` installs alone; ADR 0037 Amendments]**). **Next #13:** api-client seam (`credentials:"include"` + typed error-envelope unwrap) → SPA data-layer (`@tanstack/react-query` + `api.ts`/`auth.ts`/`useSession` + `QueryClientProvider`) → AppShell chrome + Base UI primitives + auth guard + CommandPalette + Playwright/axe → `doc.list × ui` parity cell + `packages/contract-tests`.
- **2026-06-03** — **Web UI #13: pnpm posture + file-based router LAND** (`6fdc1a9` + `65cf281`, ADR 0035 §4–§5). Two commits after the skeleton. (1) `packageManager` pnpm **10.0.0 → 10.34.1** (current 10.x; 11.x is a major beyond ADR 0035's sanctioned same-major bump) — the prerequisite for the release-age cooldown; frozen install confirmed **zero** dependency churn. (2) **File-based TanStack Router** + the **supply-chain cooldown**. Router deps exact-pinned (ADR 0035 §6): `@tanstack/react-router 1.170.10` (post-incident clean — the GHSA-g7cv-rxg3-hmpx `1.169.5`/`1.169.8` are 404 on the registry, version list skips them), `@tanstack/router-plugin 1.168.13` (peer `@tanstack/react-router ^1.170.10` + `vite >=8`, both satisfied), `@tanstack/router-cli 1.167.12` (`tsr generate`; pinned to `.12` not the `.13` latest because it shares the plugin's bundled generator `@tanstack/router-generator 1.167.12` → byte-identical trees, and `.12` is 4 days old vs `.13`'s 1 day → past the cooldown window). `src/routeTree.gen.ts` is **gitignored** (ADR 0035 §4): derived from `src/routes/**`, regenerated by `tsr generate` in the typecheck/test scripts + the vite plugin at dev/build — never a committed artifact that can drift (chose the `tsr` CLI over committing the tree to keep the repo's SSOT/drift-prevention posture; over no-gen-step because typecheck needs the file without a bundler run). It carries `@ts-nocheck` and is skipped by Biome via `.gitignore` (`vcs.useIgnoreFile`). Routes: `__root` (outlet shell), `/`, `/login` — the only client routes the first slice needs (ADR 0035 §2), none colliding with the reserved API prefixes; `main.tsx` mounts `<RouterProvider>` + registers the router type for compile-checked navigation; placeholder `App.tsx` removed; `tanstackRouter` precedes `react()` (load-bearing) with `autoCodeSplitting` → build emits per-route chunks. **Cooldown** (ADR 0035 §5): `minimumReleaseAge: 4320` (3 days) in `pnpm-workspace.yaml`, set **after** the router pins were captured in the lockfile so it governs only future version *changes*; `engines.pnpm` floor → `>=10.16.0`. All gates green (apps/app typecheck + `tsconfig.test.json`, vite build, vitest 100% lib coverage with routes + gen tree excluded, biome, coherence 0/0; pre-push contract/e2e/lint/typecheck/integration/property). **Next #13:** Base UI + Meridian Zero tokens (0037/0036) → api-client sign-in + `doc.list × Web UI` parity cell → editor (0038) → PWA (0039).
- **2026-06-03** — **Invariant 3a proven end-to-end: integration property test #23 LANDS** (`b98463a`, completes ADR 0040 build-order step 2). The property that *"the audit log alone reconstructs final persistent state"* now runs against the **real dispatcher**, not hand-written effects: it drives all **16 shipped state-class mutation capabilities** through `createDispatcher`, wraps the real `createAuditWriter` in a **recording `AuditWriter`** that performs the real `audit_events`+`outbox(audit.appended)` INSERT *and* captures the typed `AuditWriteInput` (the captured `record` IS the typed `AuditRecord` — no effect-column JSON re-narrowed, no cast), and after **each** dispatch deep-equals `replay([...genesis, ...captured])` against a `PersistentWorkspaceState` projected from the live `workspaces`/`members`/`collections`/`docs` columns. Per-step, so a drift pinpoints the exact capability whose effect diverged from its handler's write. **This is the test that would have caught the #25 `doc.rename` slug drop** — proven by a negative control: reintroducing `slug:"stale"` in the effect turns the suite red with the exact `- "doc-one-renamed" / + "stale"` diff (reverted before commit). **Honors Codex's two contract constraints** (#24): (1) compare `docs.title` only under metadata-only sequences — the metadata writes stay fully real while `doc.create`/`doc.rename`'s `ctx.transact` title-block step runs against an **ephemeral `Y.Doc`** and is discarded (invariant 3b boundary; no DOM shim, so `seedBlocks`/`setDocTitle` complete in node); (2) **FAIL if a shipped capability emits a deferred-classed kind** — every emitted allow-effect is asserted non-`deferred` in `REPLAY_CLASS`, and a coverage assertion pins that all 16 state kinds were exercised so a refactor can't silently drop one. **Genesis (ADR 0041):** the dispatcher layer doesn't run the signup hook, so the suite seeds the workspace+owner rows directly AND prepends the two matching `workspace.create`+`member.add` genesis `ReplayRow`s (same constants) so the replay baseline already holds the workspace `workspace.update` later patches. One monotonic clock feeds both the dispatcher (`ctx.now()`→`deleted_at`) and the writer, so soft-delete timestamps match by construction; a per-step `outcome === "allow"` assertion guards against a silent deny/error making `replay == DB` trivially true. Runs in the **pre-push prop lane** (3 files / 42 prop tests green). All gates green (dispatcher typecheck incl. `prop/**/*`, coherence 0/0, biome, full pre-push contract/e2e/lint/typecheck/integration/property). **ADR 0040 build-order step 2 (replay engine + property test) is now COMPLETE** — next on that track is step 3 (additive brands/scopes/schemas) → the `spaces`/`space_members`/`grants` schema slice; it stays parallel to the Web UI shell (#13).
- **2026-06-03** — **Genesis bootstrap is now audited: ADR 0041 + #26 LANDS** (`c146d28`, closes Codex review HIGH 2). Signup's post-commit hook wrote the root `workspaces` row + owner `workspace_members` row via `driver.system()` with **no** `audit_events` — so the log alone could not reconstruct the genesis workspace/owner (an invariant-3 hole). Closed by **auditing genesis, not excepting it**: the two inserts move into `driver.withSystemTx`, emitting `workspace.create`+`member.add` through the **shared `AuditWriter`** (row shape + `outbox(audit.appended)` fan-out single-sourced) under a `system.workspace_bootstrap` **synthetic, non-dispatchable** `capability_id`. SSOT = `SYSTEM_AUDIT_CAPABILITY_IDS` in `@editorzero/scopes` (+ an `isSystemAuditCapabilityId` guard — the runtime half of *"a recognised audit row carries a registered capability id OR a system-audit id"*); `scripts/coherence.ts` **Check 5b** enforces the markers stay disjoint from implemented capabilities, so a marker can never silently become / be shadowed by a dispatchable capability. The tuple is **atomic among itself** (NOT with the already-committed Better Auth `user` row — the pre-existing user-without-workspace gap is unchanged); each audit row emits only when its `onConflict().doNothing()` insert actually mutated, so retries don't double-audit. **Codex co-designed the seam** (cross-model, ADR-level): `system.` prefix, `withSystemTx`+`asAuditTx`, post-commit framing, allowlist+coherence — no genesis exception. **Tests:** auth proves the two genesis rows **replay** to the live `workspaces`+`members` projection (invariant 3 end-to-end for signup); scopes gains its **first unit test** (guard true/false + type-narrowing) + a `tsconfig.test.json`/two-step `typecheck` so its test types are gate-checked like every other package; api-server's full-stack auth-chain audit assertions now **scope to dispatched capabilities** (the 28-test blast radius of every signup now writing 2 genesis rows — fixed by excluding the system-audit markers, since genesis is proven in auth). All gates green with Docker up (audit 31 / scopes 4 / auth 10 / api-server 244; coherence 0/0; pre-push contract/e2e/lint/typecheck/integration/property all green). **Next:** #23 — the integration property test (real dispatcher → recording `AuditWriter` → `replay()` → deep-equal vs a live-DB projection).
- **2026-06-02** — **Effect→state contract fix-forward LANDS** (`3f1e436`, #25; remediates Codex's effect→state pass #24). One principle across the audit replay engine: *an effect carries every handler-computed value; replay never infers it from the envelope principal or a DDL default.* Closed **HIGH 1** (`created_by` on `doc.create`/`collection.create` — agent writes attribute to the human behind the agent, not the agent principal), **HIGH 3** (`workspace.{create,update}` carry `trash_retention_days`+`settings`), **HIGH 4** (`deleted_at` carries the handler `ctx.now()` — the ADR 0017 recovery anchor, distinct from the audit-row clock; state now models `deleted_at: number | null`, not a boolean). An **adversarial review Workflow** (5 dimensions → verify-per-finding; 13 raised → **4 confirmed / 8 refuted**) then caught a **fourth instance of the same class**: `doc.rename` re-derives + writes `docs.slug` but the effect carried only `title`, so replay kept the stale create-time slug — fixed parallel to `collection.update`, and the unit test that *masked* it (renamed the doc yet asserted the old slug) corrected. Same commit makes the per-kind forcing function real: `STATE_KIND_FIXTURES satisfies Record<StateKind,…>` (compile lock) + `it.each` through the reducer (runtime lock vs `applyStateEffect`'s `default` throw); reducer is now purely effect-driven (drops the unused `row` param); `workspace.update` carries `output.settings` (post-JSON-round-trip form the column yields) so the #23 compare matches even non-JSON-clean inputs. 26 files; all gates green with Docker up (db is affected → Postgres testcontainers ran for real): audit 31 / capabilities 472 / dispatcher 28 / api-server 244, coherence 0/0. **Next:** #26 (bootstrap-audit, HIGH 2) → #23 (integration property test — the gate that would have caught the slug drop).
- **2026-06-02** — **Web UI #13 begins: `apps/app` Vite + React skeleton lands** (`5112801`, ADR 0035 §1–3, deliverable #3 first increment). The `@editorzero/app` package — Vite 8 + `@vitejs/plugin-react-swc` + React 19.2.6, building to `apps/app/dist` (the `serveStatic` artifact; production attach wiring stays deferred to the ADR 0027 pass). Dev **same-origin reverse proxy** (`vite.config` `server.proxy`) forwards the trunk-owned prefixes to :3000 so ADR 0030's Lax/no-CORS holds in dev; the proxy targets + the future client-route guard share one `RESERVED_API_PREFIXES` list (`src/lib/reserved-prefixes.ts`, the unit-covered ADR 0035 §2 contract) so they can't drift. Two-pass typecheck (`tsconfig` + `tsconfig.test.json`) + vitest via `extendVitestConfig` (UI render entries excluded from coverage, `lib/**` at the 95% floor — 100%). **Supply-chain (ADR 0035 §5):** frontend deps exact-pinned, verified against the live npm registry at scaffold time (0035 pins still resolve; `@vitejs/plugin-react-swc@4.3.1` / `@types/react@19.2.16` / `@types/react-dom@19.2.3` resolved as the "current X.x" ones); lockfile committed. Lockfile change is **additive** — vite 7.3.2 retained for vitest, vite 8.0.14 new for apps/app; full repo suite re-run green (**1166 tests**) to confirm the shared peer-hash recompute regressed nothing. **Kept on pnpm 10.x** — latest pnpm is now 11.x, a major beyond ADR 0035's sanctioned same-major bump; the `packageManager` bump + `minimumReleaseAge` cooldown land in the next increment. **Next #13 sub-steps:** pnpm posture → file-based TanStack Router (§4) → Base UI + Meridian Zero tokens (0037/0036) → api-client sign-in + `doc.list × Web UI` parity cell → editor (0038) → PWA (0039). Coherence 0/0 (apps/app outside the coherence glob).
- **2026-06-02** — **Replay engine *contract* LANDS: invariant 3a's reducer + effect→state classification** (`d0fd16c`, ADR 0040 build-order step 2). The audit-replay engine is the **first hard gate before any ACL capability** — invariant 3a ("the audit log alone reconstructs final state"). Landed in `@editorzero/audit`: **`state.ts`** = `PersistentWorkspaceState`, the semantic metadata projection the replay reconstructs (workspaces / members / collections / docs — identity, hierarchy, doc read-scope, attribution, soft-delete); **`reducer.ts`** = `replay()` folding audit rows into it, with **`REPLAY_CLASS`** classifying **all 68** `AuditEffect` kinds into **state** (19, metadata-spine transitions) / **content** (8, invariant-3b CRDT boundary, no-op here) / **audit-only** (11, reads + admin, no-op forever) / **deferred** (30, durable but no shipped capability emits them yet — the anti-ossification seam Codex named). **Forcing function (compile-time, stronger than the once-planned `audit-effect-exhaustiveness` arch-lint F89):** `REPLAY_CLASS satisfies Record<AuditEffect["kind"], ReplayClass>` won't compile until a new kind is classified, and `applyStateEffect`'s `default` throws if a kind is classed `state` without a transition — which the per-kind unit test turns red. **Contract honesty — the excluded set IS the invariant-3a surface:** timestamps are **not** audit-reconstructable (the handler stamps `created_at`/`deleted_at` on `ctx.now()`, the audit writer stamps the row on a different tick, and create/`soft_delete` effect bodies carry none) → soft-delete is modeled as a `deleted` boolean and `created_at`/`updated_at` are excluded; `diagnostic_salt` (server secret) + workspace create-time defaults (`trash_retention_days`/`settings`) excluded; docs read-scope kept as **one replaceable `visibility` field** so the ADR 0040 Step-5 `access_mode`+`published_at` split lands **additively**. 11 typed-fixture unit tests (every state transition + every gating path); 100% line/function, 98.11% branch (> floor). All gates green with Docker up (db is in the affected set — it depends on `@editorzero/audit` — so the Postgres testcontainer suite ran for real, not skip-flagged). **Pending:** (a) the **integration property test (#23)** — real dispatcher → recording `AuditWriter` captures typed records → `replay()` → deep-equal vs a live-DB projection — lives in `packages/dispatcher/prop`, **gated on Codex's independent effect→state contract pass** (Nomi flagged this exact artifact as where the proof could "accidentally encode today's behavior while missing the new Space/access_mode/grants future shape"); (b) **two contract questions out to Codex** — is "semantic final state, timestamps excluded" the right reading of 3a (vs. create/`soft_delete` effects must carry their timestamp, an effect-shape change), and does the signup bootstrap (workspace + owner-member written via `driver.system()` with **no** `audit_events`) need to emit `workspace.create`+`member.add` to close invariant 3 for signup, or is it a sanctioned genesis exception. Coherence 0/0.
- **2026-06-01** — **Tenancy & IA model DECIDED: ADR 0040** (Org → Space *hard-ceiling* → Collection/Doc per-doc ACL; Personal spaces; item-scoped guest grants; publish as an independent dimension; agents as peer grant-targets). Nomi approved Model B from a visual decision brief (`docs/ia/tenancy-models.html`, now committed) grounded in three product-research runs (Notion / Linear / cross-tool). The ADR + a roadmap-cohesive impact plan came out of a 4-phase Workflow (`wf_d4d021be-118`, 15 agents, ~2.08M subagent tokens): an **8-area corpus map** (permissions, schema, capability-parity, docs/visibility, web-ui, audit/jobs, roadmap, auth) → synthesis → draft → a **5-lens adversarial red-team** (invariant-integrity / migration-safety / permission-soundness / surface-parity / roadmap-scope), producing 2 blockers + 13 highs, all folded in before commit. **The decisive finding: the enforcement machinery the model leans on is specified-but-UNBUILT today — for the existing model too.** Postgres RLS (Layer-3) has zero `CREATE POLICY`; the audit-replay engine + `replay.prop.ts` (invariant 3a's proof) don't exist (no reducer, no `PersistentWorkspaceState` builder, no exhaustiveness lint); the four-way contract-tests harness (`packages/contract-tests`, `apps/app`) doesn't exist (only a CLI↔route parity test); the `acting_as` agent∩delegator scope-intersection is unbuilt. ADR 0040 is honest about all four and sequences each as a hard gate. **Five structural forks resolved** (the 5th added in Codex review): (1) keep `workspace_id` as the physical tenant column — Space is a new table *within* the workspace, **no rename**; (2) cross-space guest reads via a dedicated **audited system-handle read path** (not hot-path predicate-widening) — the Space ceiling is **Layer-1-only** (Layer-2 is workspace-grained), so the resolver must be the sole read authority and the published-render + git-mirror paths must be made ceiling-aware before reads are gated; (3) **one polymorphic `grants` table** (no composite FK → a fuzzer property asserts `resource_id` resolves within its `workspace_id`); (4) **RLS is not a committed layer** — multi-tenant SaaS is a product non-goal (per @numman) and `workspace_id`-keyed RLS can't enforce the cross-Space boundary anyway; the app-layer floor is the enforcement design; (5) **narrowing-below-baseline = the `access_mode` mode-switch** (`space`|`private`), not a per-member deny row (Codex HIGH 1) — positive-only `GRANT_ROLES` stay sufficient. **Blockers fixed in the draft:** Collections hold no ACL (so "move within a Space = no ACL change" is literally true — B1); Space soft-delete **refuses on live descendants** (not cascade — B2, aligning with `collection.delete`). Also corrected: grant subjects are flat `subject_kind`/`subject_id` fields (not a nested `PrincipalRef` that breaks CLI flag-gen — H12); Teams **dropped** from the v1 schema slice (H13). **Build order (no MVP cut — one complete release, sensibly sequenced):** ADR + amendments (this) → replay engine + property test → additive brands/scopes/schemas → `spaces`/`space_members`/`grants` schema slice → reader-path slice (splits `visibility`→`access_mode` + publish columns) → resolver/gate → effects → capabilities; Teams + Guest-principal + guest-editor-collab land later in the order (still in the release). **Extends 0024**; amends 0015 (RLS-unbuilt + algebra inversion) / 0017 (Space refuse-on-live) / 0020 (mirror ACL-leak) + architecture §3.12/§8.1/§9.1. **The one hard ordering against #13: the vocabulary lock** (UI "Space" / route `/spaces` / API `/workspaces`). **Codex ADR-level cross-model review DONE** (`4f935f1`) — kept Model B + all forks, independently converged on resolver-as-sole-read-path, surfaced the narrow-from-baseline gap (resolved via `access_mode`, fork #5), plus three hardening items folded in (HIGH 4 three-green ship-gate; MEDIUM 5 named `grants` constraints; MEDIUM 6 side-channel gate). Then @numman set direction — **no MVP/v1 cut** (one complete first release, sensibly ordered) and **multi-tenant SaaS is a non-goal** → RLS decided not-a-committed-layer. Reframe + rename + RLS-decision in a follow-up commit. Coherence 0/0.
- **2026-05-31** — **Web UI surface-foundations cluster LANDS: ADRs 0036–0039** (brand, design system + theming, owned editor, PWA) — the canonical, citation-backed documentation of the pre-shell decisions. **Brand** chosen by Nomi (*"Meridian Zero — that's the one"*) from 12 full-screen design explorations generated via a divergent-generate-then-curate Workflow (`wf_b3a17378-0fb` design, `wf_545b3708-b04` buildout + a11y audit, audit means DS 8.9 / mobile 7.5 / token 8.4); the 123 a11y flags were systemic and fixed centrally in the token SSOT (`docs/brand/v2/meridian-zero.css`, AA-hardened) — no remediation workflow needed. Then a **research + adversarial-verification Workflow** (`wf_734a602e-3d6`, 7 agents, 815k subagent tokens) investigated PWA/mobile/offline-CRDT **and** verified every library pin against the live npm registry + official docs/specs (2026-05-31), **correcting four draft claims before the ADRs locked**: (i) no `y-prosemirror@2.0.0` exists (latest 1.3.7; the Yjs-v14 work is `@y/prosemirror`); (ii) Hocuspocus latest is 4.1.0, repo pins 3.4.4 (a major-bump decision deferred to the editor slice); (iii) `@base-ui/react@1.5.0` carries `date-fns@^4` + `@date-fns/tz@^1.2` peer-deps ~~**mandatory** (install-breaking if absent — spot-verified by hand against npm)~~ **[corrected 2026-06-03: these are *optional* (`peerDependenciesMeta.optional: true`); the spot-check read `peerDependencies` but missed the `optional` flag — see ADR 0037 Amendments]**; (iv) Floating-UI convergence is at `@floating-ui/dom@1.7.6` via two different adapters (Base UI → `@floating-ui/react-dom@^2`; Tiptap → `@floating-ui/dom@^1` directly). **0036** Meridian Zero (cold-Swiss; ultramarine + agent-cyan; token SSOT). **0037** Base UI headless shell (zero-CSS; *not* Mantine/ShadCN; gap-fill cmdk + TanStack Table + owned Tree; RAC 1.18.0 recorded fallback) + three-layer design tokens (primitive→semantic→component) → curated **and** user-authored themes by `:root` override, no rebuild. **0038** owned Tiptap v3 adopted **directly** (supersedes 0031's BlockNote bootstrap; lockstep exact pins `@tiptap/* 3.24.0` + `@tiptap/y-tiptap 3.0.4` on Yjs v13) — DOM-free JSON-in write path (`prosemirrorJSONToYDoc`/`updateYFragment` + Hocuspocus `openDirectConnection().transact()`) removes the `happy-dom` shim from production, **improving 0018** (write-path attribution must use the inner `doc.transact(fn, origin)` — Hocuspocus #833). **0039** installable PWA (`vite-plugin-pwa 1.3.0` injectManifest; `registerType:'prompt'`; SW denylist derived from the 0035 reserved-prefix SSOT + an equality test); **offline-read-only v1** (naive offline-write would silently break invariants 3+5 — CRDTs solve convergence, not authorization); responsive 3-pane collapse (bottom tab bar + Base UI Drawer; ≥44px targets); iOS standalone-auth verdict (Lax cookie holds inside the installed app; one-time iOS re-login expected; **no architecture change**). Coherence 0/0. Amended 0018 (DOM-free write path), 0031 (→ superseded by 0038), 0035 (editor/design/PWA pins). Brand assets committed under `docs/brand/`. Next: **Web UI shell scaffold (task #13)** — now editor-agnostic foundations are locked.
- **2026-05-31** — **ADR 0035 (Web UI SPA scaffold + frontend supply-chain posture) Accepted.** Closes the four scaffolding gaps ADR 0027–0033 left open: app dir `apps/app` / `@editorzero/app`; Vite dev server + same-origin reverse proxy to the trunk (`/auth` `/mcp` `/collab` + the 5 capability domains → `:3000`, preserving 0030's `SameSite=Lax`/no-CORS in dev); `apps/app/dist` → `serveStatic`; file-based routing via `@tanstack/router-plugin/vite` + `autoCodeSplitting`. Pins the verified version set (React 19.2.6, Vite 8.0.14, `@tanstack/react-router` 1.170.10, BlockNote 0.51.3 with `@blocknote/mantine` view). **Supply-chain posture (decided with @numman): exact-pin every frontend dep + committed lockfile + pnpm `minimumReleaseAge` 3-day cooldown.** Prompted by **CVE-2026-45321 / GHSA-g7cv-rxg3-hmpx (CVSS 9.6)** — the 2026-05-11 `@tanstack/*` npm compromise (cache-poison → OIDC theft → malware under valid SLSA provenance; affected react-router 1.169.5/1.169.8, both pulled within the hour; 1.170.10 is clean). Verified this repo + machine **uncompromised** (no affected version in the lockfile, no `gh-token-monitor` daemon, no `router_init.js`/`router_runtime.js` payload). Prereq: pnpm `packageManager` 10.0.0 → current 10.x (`minimumReleaseAge` needs ≥10.16.0). Next: scaffold `apps/app`.
- **2026-05-31** — **Co-hosting red-team hardening** (Codex peer review of deliverable #2). Verdict: no bypass in the upgrade → `onAuthenticate` handoff; the core split is sound. Landed a **same-socket multiplex regression test** (`apps/server/src/cohost.integration.test.ts`, now 5 tests / apps/server 11): Alice authenticates her own doc, then an Auth frame for Bob's doc sent over the *same* socket is denied — turning the multiplex worry that forced authZ into `onAuthenticate` into a regression. Captured four production-attach blockers (role-aware read/write, WS-upgrade `Origin` check, revocation freshness on the upgrade-time principal snapshot, `onAuthenticate`-by-construction) in ADR 0030 + *What's next* #2 + task #15; adopted Codex's construction-factory framing (`createAuthenticatedCollabHocuspocus` + negative test) over the boot-time-assert line. No production code changed (test + docs only). Next: SPA bootstrap (#3) — needs a focused scaffolding ADR first (app dir name, dev-loop topology, `serveStatic` root↔Vite `outDir`, router file-based-vs-code-based; ADR 0027–0033 leave these open) and a TanStack Router supply-chain advisory to verify.
- **2026-05-31** — **Deliverable #2 LANDS: co-hosting + auth smoke** (ADR 0027/0030). One owned `http.Server` co-hosts the HTTP trunk + `serveStatic` SPA assets + the `/collab` WebSocket upgrade — proven on `@hono/node-server` **v1**, retiring ADR 0027's "v2 prerequisite" framing (v1 ships `serveStatic`; raw `ws` `WebSocketServer({ noServer })` + `server.on("upgrade")` → `hocuspocus.handleConnection` replaces v2's `upgradeWebSocket`). Security edge proven end-to-end through the real surfaces (sign-up → `doc.create` → WS): **authN at the upgrade** (cookie → shared Better Auth `resolver`, now exposed on `BootedApp`; no principal → socket destroyed before the WS establishes) + **per-document authZ at `onAuthenticate`** (principal injected via `handleConnection(ws, req, { principal })` → `payload.context`; `documentName` authorized against `principal.workspace_id` by a tenant-scoped doc lookup → throw → Hocuspocus permission-denied). The split is forced by Hocuspocus multiplexing docs per socket — verified in `@hocuspocus/server` 3.4.4 source (subagent): the gate is fail-closed (non-Auth frames queued; a doc establishes only after a *successful* Auth frame) but keys on the frame, not hook presence, so "`onAuthenticate` is registered" is a **boot invariant** the production WS-attach pass must assert (no in-package backstop). Asserts no-cookie→rejected, same-workspace→authenticated, cross-workspace→denied. Codex (peer) reviewed the design — adopted his authN/authZ split + `handleConnection` context-injection — and is red-teaming the implementation. Verification: apps/server typecheck + 10 tests green (`runtime.ts` 100% coverage); api-server typecheck + build green; no casts. Files: `apps/server/src/cohost.integration.test.ts` (smoke), `packages/api-server/src/server.ts` (`resolver` on `BootedApp`). Next: SPA bootstrap (#3).
- **2026-05-30** — **Deliverable #1 of the Web UI keystone LANDS: composition root + boot entrypoint** — ADR 0027's "genuine first deliverable." Four commits. (a) `371e7d0` — the **materialized typed-RPC precompile** (`packages/api-client/src/client-type.ts`): `const _client = hc<AppType, string>("")` → `export type ApiClient = typeof _client`, the one form that materialises the resolved client type in declaration emit (tsc preserves `ReturnType<typeof hc<…>>` aliases + conditional types unexpanded; const-inference forces materialisation — Hono's `hcWithType` recipe). Coherence check 8 bans `ReturnType<typeof hc>` in api-client. (b) `ce63bd6` — `createDefaultRegistry()` (all 24 caps) as the registry SSOT, replacing apps/cli's hand-list; per-surface exposure stays each capability's `surfaces` array. (c) `0723be5` — **`getApiApp()`** (`packages/api-server/src/server.ts`): driver → `ensureSchema` → auth (+migrations) → registry → sync → dispatcher → loadRoles → `createApiApp`; returns `BootedApp` (trunk + dependency-ordered idempotent `close()`, sync→driver). Two supporting primitives: `@editorzero/config` `resolveSecretRef` (concrete env/file secret resolution — the sanctioned `process.env` boundary, §16.8; vault fails loud) + `@editorzero/db` `ensureSchema` (restart-safe sentinel-probe DDL bootstrap — `FULL_DDL` is non-idempotent; distinct from Atlas migration evolution). (d) `e1bc56d` — **`apps/server`** (`editorzero-server` bin): loads `RuntimeConfig`, builds the stack via `getApiApp`, binds it over a concrete `node:http` server (`getRequestListener` — not `serve()`'s `http|http2` union, so `closeIdleConnections` needs no narrowing branch), and turns SIGTERM/SIGINT into a graceful drain (release idle keep-alives → stop accepting → finish in-flight → tear down sync→driver). New `config.port` (`PORT`, default 3000 — container-internal, independent of `public_origin`: a reverse proxy terminates the public URL elsewhere) + three `server.*` `LogEvent`s. HTTP-only on `@hono/node-server` **v1** (the installed pin); `serveStatic` + the `/collab` WS upgrade need v2 and are the co-hosting smoke (deliverable #2). Verification: typecheck across 21 packages+apps; api-server 244 (`server.ts` 100%); apps/server 6 (`runtime.ts` 100%); config/db/observability green; coherence 0/0; Biome clean; all pre-push lanes (contract/e2e/lint/typecheck/integration/property) green on each push.
- **2026-05-30** — **`doc.create` slug-collision gap closed** (last "Current-code follow-up" from the API migration). Added the NULL-aware sibling-slug pre-check to the `doc.create` capability handler (mirrors `collection.create`): a SELECT on `(collection_id scope, slug, deleted_at IS NULL)` throws `SlugCollisionError` (→ 409 `slug_collision`) *before* the INSERT, so a colliding create returns a typed 409 instead of a raw `docs_root_slug_unique` UNIQUE violation projected as `internal` (500). The route's `describeRoute` now also declares the 404 (`collection_id` not found) + 409 arms it could already return at runtime via `errorResponse`. Fixed a stale handler-header comment that claimed `docs` had no slug-uniqueness constraint (the collections slice-1 DDL added the two partial indexes). 5 capability unit tests (root + nested collision, NULL-aware cross-scope reuse, soft-deleted-sibling reuse) + 1 auth-chain e2e (two creates same title → 409 + create-allow/create-error audit trail). capabilities 470 / api-server 240 / CLI 101 green; coherence 0/0; Biome clean.
- **2026-05-30** — **API substrate migration LANDS: code-first `hono-openapi` + `hono/factory`, with `@editorzero/schemas` as SSOT** (ADR 0029 substrate + ADR 0034 schemas package). The whole ~26-route API moved off `@hono/zod-openapi` (`OpenAPIHono` + `openapiRoutes([...] as const)`) to per-route `factory.createHandlers(describeRoute(…), validator(…), handler)` plain-`Hono` sub-apps, composed by chained `.route()` — at two levels: each domain `index.ts` is now a `new Hono<ApiEnv>().route("/", a).route("/", b)…` sub-app, and the trunk mounts the five domains via `trunk.route("/infra", infra).route("/docs", docs)…`. `@hono/zod-openapi` is dropped from `api-server`. **SSOT (the decisive pivot):** routes no longer re-declare wire schemas — a new light-leaf `@editorzero/schemas` package holds one zod module per capability (`<domain>/<action>.ts`) + shared sub-schemas (`ids`/`visibility`/`fields`/`audit`); **both** the capability **and** its route import the same schema. The kernel's `Capability.input: ZodType<unknown>` erasure (which collapses `z.input` to `unknown`) is bypassed by importing the schema directly — a transform-bearing schema carries both shapes (`z.input` = wire UUIDv7 string, `z.output` = branded), and `z.coerce.number()` lets one schema serve HTTP-query-string + CLI/MCP numbers. Naming (ADR 0034): values `PascalCase+Schema` (`DocCreateInputSchema`); types `<Cap>{WireInput,Input,Output}` from the capability contract; `<Cap>WireOutput` reserved-not-exported. The bulk fan-out ran as **one orchestrated Workflow** (79 agents: 3 shared sub-schemas + 23 capability schema extractions/re-points → Gate A typecheck+test, which caught and fixed a `.strict()` regression on the two list-query caps → 25 route+test migrations each adversarially verified → Gate B). I did the trunk/index integration + the `openApiDocument(app)` doc-generation seam (the `hono-openapi` fence is realised as a module — `src/lib/openapi.ts` — not a package; CLI parity imports the seam, never `hono-openapi`) + the `OpenAPIHono`→`Hono` swap in the middleware/CLI test harnesses + verification by hand. **Findings vs the ADR 0029 plan:** `.openapi("Name")` did *not* become `.meta({$id})` — hono-openapi 1.3.0 inlines response schemas (no named `components.schemas`), so the trunk test asserts the inlined 200 schema. Verification green end-to-end: full monorepo typecheck (19/19 packages+apps), **api-server 239** (incl. `auth-chain.integration` 110 — real trunk + Better Auth + SQLite, MCP, dispatcher), **CLI 98** (incl. `parity` — every capability's derived binding resolves to a real route on the new `.route()`-composed trunk via `openApiDocument`), **capabilities 465**, **api-client 6** (`hc<AppType>` typed RPC), coherence 0/0, Biome clean. Docs: ADR 0034 created (Accepted, refines 0029); ADR 0029 stamped *landed* with the three implementation findings; AGENTS.md "adapter schemas mirror" rule **retired** → "surface adapters reuse the capability schema (SSOT)". Capability count unchanged (24); the contract didn't move, only its single-source-of-definition.
- **2026-04-22** — **Workspace membership CRUD quartet lands end-to-end** (`9d23602` list/remove/update_role + `fab271c` Codex PG-race/409-shape + `f38583c` member_add + `0bc185c` Codex ON-CONFLICT). `workspace.member_{add,list,remove,update_role}`, all `workspace:admin`; the three mutators metadata-only. `member_list` paginates by composite `(created_at, user_id)` cursor (audit.list shape), active-only, `role` filter. `member_update_role`: 404 missing, 400 `role_unchanged`, **409 `LastOwnerError`** (code `last_owner_protected`) if it would demote the only live owner; COUNT+UPDATE atomic in the write-path tx. `member_remove`: soft-delete, not idempotent (re-remove → 404), self-removal allowed (last-owner guard blocks the dangerous case). `member_add` (ADR 0024 §5) three-branch on `(workspace_id, user_id)`: live → 409 `MemberAlreadyExistsError`; soft-deleted → revive-in-place UPDATE (preserve `created_at`, may change role); none → INSERT `… ON CONFLICT DO NOTHING RETURNING` → zero-row re-throws the 409 (keeps the conflict projection in-handler; the global mapper deliberately does *not* project raw `23505`). `workspace_members` enters `TENANT_SCOPE_COLUMNS`. Codex drove three fixes: PG SERIALIZABLE last-owner race → mapper projects 40001/40P01 → 409; 409 body widened to `union(last_owner_protected, conflict)`; Branch-C 23505 race → ON-CONFLICT. 465 capability / 237 api-server / 101 CLI green.
- **2026-04-22** — **CLI dogfood findings captured** (`09c635b`; + `dcc8143` Codex-envelope `reply-to`). Two general-purpose subagents drove the compiled `ez` binary end-to-end; convergent UX findings bucketed in *What's next* (capability-level / cross-surface-contract / CLI-adapter). One durable AGENTS.md Working rule landed — *"adapter schemas mirror capability schemas."* Codex rebutted a shadow `docs/ux-debt.md` tracker (continuation owns the "what's next" lens) and an `auth.signup` ADR (backlog until a slice commits). Dogfood-as-boundary-practice went to feedback memory, not the repo.
- **2026-04-22** — **Audit-inspection surface lands end-to-end** (`8bbd9e3` caps/routes + `d9d6580` e2e + `c6b62f8` Codex fixes). `audit.list` + `audit.get` — first read of `audit_events` (§3.11) through a capability; invariant 3 previously had no inspection surface. `workspace:admin`-only (audit rows reveal cross-principal activity). `audit.list`: composite `(before_created_at, before_id)` cursor with both-or-neither refine (epoch-ms collides under bursty writes; UUIDv7 tiebreak keeps order stable; cursor fields explicit on the wire, not opaque), filters `subject` / `capability_id` / `outcome` / `since` / `until`. First GET with a query-string schema (`z.coerce.*` at the route). Codex post-impl narrowed route contracts (uuid + the three semantic refines) and toned collapse framing to "declared policy, backend deferred." 41 capability / 4 route / 9 e2e.
- **2026-04-22** — **Workspace domain lands end-to-end** (`693cada` infra/bootstrap + `bf1a283` caps/routes/CLI + `e66b7d5` e2e + `d67e9e0` Codex fixes). `workspace.get` + `workspace.update`. First *self-scoped* tenant table: `workspaces.id` IS the workspace id, so `TENANT_SCOPE_COLUMNS` (per-table `Record<table,column>`) replaces the flat `TENANT_SCOPED_TABLES`; scope error broadens `workspace_id_mismatch` → `scope_mismatch`. DDL §3.2: `trash_retention_days` (DEFAULT 30, CHECK 7..365), `diagnostic_salt` BLOB, `settings` JSON, NO `updated_at` (audit log is the mutation history), partial-unique `slug WHERE deleted_at IS NULL`. Bootstrap hook now seeds **both** the `workspaces` anchor row **and** the `workspace_members` owner row (workspaces first — the auto-appended scope predicate joins `workspaces.id`). `workspace.update` metadata-only, `workspace:admin`, slug deliberately immutable, rejects empty `{}`. Codex drove route-refine mirror + slug widened 6→12-hex. 13+19 unit / 4 route / 7 e2e.
- **2026-04-22** — **Collections slice-3 docs tail** (`6cf5e3a`). architecture.md §3.5 records the `COLLECTION_MAX_DEPTH = 8` strict-`>=` rule + *why* `collection.restore` must enforce it locally — so future tree-reshaping caps inherit the locality obligation without re-deriving it. Docs-only.
- **2026-04-22** — **Slice-3 follow-on: `collection.restore` enforces the depth cap.** Closes a cross-slice correctness gap Codex caught reviewing slice 3: `collection.move`'s subtree-height walk only sees *live* descendants, so a "delete deep subtree bottom-up → move parent deeper → restore subtree top-down" sequence bypasses the cap — each individual restore looks cap-safe in isolation because the immediate subject has zero live subtree (per the `delete-refuses-with-live-descendants` invariant), but the deepest restore lands past `COLLECTION_MAX_DEPTH`. Exact minimal sequence: `A → B → C → D` live (depths 0..3) → delete D, C, B → move A under a parent at depth 4 (A now 5; move's subtree walk sees 0 live → passes) → restore B (at depth 6, OK) → C (7, OK) → D (8 → INVARIANT BROKEN). Fix: `collection.restore` now runs the same ancestor walk + BFS subtree walk `collection.move` uses and refuses with `ValidationError { depth_cap_exceeded }` when `parent_depth + 1 + subtree_height >= MAX_DEPTH`. Strict `>=` mirrors `collection.create` / `collection.move` exactly so no op that makes a collection live can produce a tree another would have rejected. Per the delete-live-descendants invariant the subtree_height should always be 0 on the normal path; the walk stays for defence-in-depth against future cascade-delete semantics or corrupt state. 3 new unit tests on `collection.restore` exercising the exact sequence (D-at-depth-8 refusal + B/C intermediate passes + root-restore-always-passes) + 1 new auth-chain e2e walking the full delete/move/restore trunk flow end-to-end and asserting 400 `validation_failed` on the final restore. 328 capabilities / 168 api-server / 99 CLI green. Invariant locality: every op that makes a collection live (create / move / restore) enforces `COLLECTION_MAX_DEPTH`.
- **2026-04-22** — **Collections slice 3 lands end-to-end** — `collection.move` + `doc.move` + `/docs/create` route widened to accept optional `collection_id`. First tree-reshaping capabilities; closes the "move is not just parent existence + cycle check" gap Codex flagged in the slice-2 handoff. `collection.move` enforces four preconditions before the UPDATE: (1) 404 on missing/soft-deleted moved collection, (2) single ancestor walk from `new_parent_id` to root doing cycle detection + target existence + depth-of-new-parent in one pass (refuse `cycle_detected` if the moved id appears anywhere in the chain; includes direct self-parent as iteration-0), (3) BFS subtree-height walk of the moved collection's descendants (levels counted from the moved node; upper-bounded by `COLLECTION_MAX_DEPTH` for corrupt-state defence), (4) `new_deepest_depth = target_parent_depth + 1 + subtree_height` must be `< COLLECTION_MAX_DEPTH` — strict `>=` reject matches `collection.create`'s rule exactly so a move cannot produce a tree `create` would have refused (Codex caught the off-by-one in the pre-code plan; `<=` would have allowed depth 8 which `create` rejects). Target-scope sibling-slug pre-check reuses `SlugCollisionError` from slice 2 (slug stays fixed on move, so the check is only "does another sibling already hold this slug in the target scope"); runs only when the parent changes. Fresh UUIDv7 `order_key` on write → moved item appends to end of target's sort (single-replica semantics, same as `create`). `doc.move` is the simpler sibling — docs are tree leaves so no cycle walk, no subtree-height; just target-collection 404 + target-scope slug pre-check + UPDATE. `doc.move` also re-seats `order_key`. New errors: no new error classes needed — cycle + depth both project as `ValidationError` with structured issue codes `cycle_detected` / `depth_cap_exceeded`, same shape `collection.create`'s existing `depth_cap_exceeded` uses. Slug collisions reuse `SlugCollisionError`. Routes `POST /collections/move/:collection_id` (body `{new_parent_id}`) + `POST /docs/move/:doc_id` (body `{new_collection_id}`) — `deriveHttpBinding` auto-derives both from the `<domain>_id` convention; CLI picks them up via `registerCapability(docMove)` + `registerCapability(collectionMove)` in the registry. Side-effect this slice: `/docs/create` route's request schema widened to forward optional `collection_id` to match the capability's input — slice-1 left the handler accepting it but the route silently dropped it; fixed-in-place since full-stack move coverage needed the route path to set up source/target docs in different collections. `doc.create` + `collection.create` additionally re-exported via `registerDocMove` / `registerCollectionMove` in the auth-chain fixture. 53 new capability unit tests (30 move + 23 doc.move) + 10 new route unit tests (5 per) + 9 auth-chain e2e (re-parent happy path x2, slug-collision 409 x2, cycle 400, 404 target missing, 401 unauth x2, one extra). Monorepo green end-to-end: 325 capabilities / 167 api-server / 99 CLI. Codex peer-reviewed twice pre-code: round 1 flagged the "subtree-height vs moved-node-depth" gap + target-scope slug checks on both move ops; round 2 caught the `<=` vs `<` off-by-one against `collection.create`. Slice-3 shape landed correct on first commit.
- **2026-04-22** — **Collections slice 2 lands end-to-end** — `collection.update` + `collection.delete` + `collection.restore` + widened `doc.restore` with parent-collection precondition. First non-`doc.*` mutation triplet; second time the trunk's `{error: err.code}` envelope carries a typed 409 past `stale_precondition`. Three new `EditorZeroError` subclasses land in `@editorzero/errors` following the `StalePreconditionError` precedent (per-subclass `code` field; the global mapper at `apps/web/api-server/src/app.ts` serialises `err.code` directly, so widening `ConflictError` would collapse all conflicts to `"conflict"` and lose the forensic distinction): `SlugCollisionError` (code `slug_collision`, carries `slug` + `parent_kind` + `parent_id`), `HasLiveDescendantsError` (code `has_live_descendants`, carries `descendant_counts: {collections, docs}`), `ParentDeletedError` (code `parent_deleted`, carries `parent_kind` + `parent_id`). All three project to `{kind: "conflict"}` in their `HandlerError` cut; audit trail stays coarse-grained while the wire contract stays precise. `collection.update` rename-in-place: SELECT current → sibling-slug collision pre-check (NULL-aware, only when slug changes) → UPDATE with RETURNING; metadata-only, `doc:write` scope, `agentAllowed: {}` placeholder (no agent-principal slice yet). Slug pre-check is the common-path 409; the partial UNIQUE index lands the race-guard 409 only under concurrent writes (Codex's concern post-slice-1 that silent-SELECT-check alone can race — pre-check + DB UNIQUE together is defense-in-depth, not duplication). Same pre-check retrofitted into `collection.create` for shape parity. `collection.delete` refuses-with-descendants (not cascades): parallel `COUNT(*) WHERE deleted_at IS NULL` on child collections + child docs → throw `HasLiveDescendantsError` if non-zero → otherwise UPDATE `deleted_at = now`. Refusal over cascade preserves the 1:1 restore invariant (ADR 0017) and the already-enshrined `doc.delete` shape (single subject, not a subtree); cascade is a future batching capability, not this slice. `collection.restore` symmetric to `doc.restore`'s parent-collection precondition: SELECT deleted row → if `parent_id !== null` SELECT parent → throw `ParentDeletedError` if parent `deleted_at !== null` OR missing → otherwise UPDATE to clear `deleted_at`. `doc.restore` widened identically: one extra SELECT before the UPDATE when `collection_id !== null` (fetches parent deletion state); refuse if parent deleted or missing. Handler-side (not DB-side) because `docs.collection_id` still has no DB-level FK (Atlas + kysely-codegen slice lands that, per architecture.md §16.9); the query-based check works regardless. Routes `POST /collections/update/:collection_id` (body `{title}`), `POST /collections/delete/:collection_id` (no body), `POST /collections/restore/:collection_id` (no body) — `deriveHttpBinding` auto-promotes `<domain>_id` input field to path param, so the CLI's generator picks up the new routes via registry alone (`registerCapability(collectionUpdate/Delete/Restore)` in `apps/cli/src/registry.ts`); CLI ↔ server parity test green against the registry-derived bindings + trunk OpenAPI document. 49 new capability unit tests (20 update, 16 delete, 13 restore) + retrofit tests for slug-collision path in `collection.create` + 4 new parent-collection precondition tests in `doc.restore.unit.test.ts`. 9 new api-server tests (3 unit × 3 routes + 6 new auth-chain e2e: rename happy path / rename 409 sibling-slug / rename 401 / delete empty happy path / delete 409 live-descendants / restore happy path + 404 already-live). Monorepo: 272/272 capabilities / 149/149 api-server / 99/99 CLI. Codex peer-reviewed slice 1 between slice 1 commit + slice 2 start; the error-code plumbing concern ("trunk serialises `err.code` verbatim; typed 409s need per-subclass error classes or widening, not a magic `ConflictError.code` rewrite") shaped the slice 2 error design end-to-end.
- **2026-04-22** — **Collections slice 1 lands end-to-end** (DDL + `collection.create` + `collection.list` + `doc.create` widened to accept optional `collection_id`). Tenth and eleventh capabilities; first non-`doc.*` domain to reach all four surface hooks. Two DDL additions: (a) new `collections` table per architecture.md §3.5 (composite self-ref FK `(parent_id, workspace_id) REFERENCES collections(id, workspace_id)` — F99 pattern, prevents cross-workspace parenting even against a guessed `parent_id`; partial unique indexes on `(workspace_id, slug) WHERE parent_id IS NULL` and `(workspace_id, parent_id, slug) WHERE parent_id IS NOT NULL` + `deleted_at IS NULL` so NULL-is-distinct SQL semantics don't let two root collections collide on slug and soft-deleted rows don't block replacements) and (b) matching partial unique indexes on `docs` for the same NULL-aware slug-uniqueness invariant (`docs_root_slug_unique` + `docs_nested_slug_unique`). `collections` added to `TENANT_SCOPED_TABLES` so the scoping plugin auto-injects `workspace_id` on every query. `docs.collection_id` intentionally remains **handler-enforced** (no DB-level FK) — 12+ test files stand up `DOCS_DDL` in isolation without bringing `COLLECTIONS_DDL`; Atlas + kysely-codegen (architecture.md §16.9) lands the FK alongside the codegen move. `collection.create` handler walks the ancestor chain on each `parent_id !== null` call to enforce the `COLLECTION_MAX_DEPTH = 8` soft cap (new `@editorzero/constants` export; 8-level Notion-class cap, not structural) — O(depth) SELECTs, capped by the constant; recursive CTE deferred until depth cost is measurable. Missing / soft-deleted parent → 404; depth at cap → 400 `depth_cap_exceeded`. `collection.list` is flat (returns all non-deleted in `order_key` order) — callers group by `parent_id` client-side; per-parent filter can land on the same input schema later without a breaking shape change. `doc.create` widening accepts optional `collection_id: CollectionId | null`; handler SELECTs the live collection before the INSERT (404 on missing/soft-deleted, cross-workspace isolation via tenant plugin). Routes `POST /collections/create` + `GET /collections/list` wired at `/collections/*` prefix behind the same principal + dispatcher middleware chain as `/docs/*`; `createRootCommands(cliRegistry)` picks up the new domain automatically (the `docs` → `docs` naïve plural extends to `collections`). Codex cleared the ADR-level scoping pre-code across 5 questions (live-descendants-only cascade, restore precondition, doc integration in slice 1, docs partial indexes, `createRootCommands` as coherence guard). 36 capability unit tests / 5 route unit tests / 5 auth-chain e2e (happy-path round-trip with audit trail, 401 x2, 404 parent-not-found, 400 empty title). Postgres test harness updated to DROP the new table; sync integration test helper slugs derive from doc_id to avoid false collision under the new partial index. Full monorepo green; 11 test-passing packages (206 capabilities / 135 api-server / 50 sync / 104 db / 28 dispatcher / 96 CLI / rest unchanged).
- **2026-04-21** — **`doc.update` F12 batch-mutation capability lands end-to-end** (`05bd2e0` + Codex-review follow-on `5aa7693`). Ninth capability past `doc.rename`; first capability to exercise the batched op-list shape F12 reframing committed to. Slice 1 covers three of the five `doc.update_batch` ops (`insert` / `update` / `remove`); `move` + `set_visibility` reject at the discriminated-union schema (400 `invalid_union_discriminator`) until their follow-on slices land. Single-tx atomicity: all ops apply inside one `editor.transact` → one Yjs update blob → one `doc_updates` row. Handler threads `ctx.transact` → `withLiveEditor` → `editor.transact` exactly once. Optimistic concurrency (ADR 0022 §precondition): `update` + `remove` accept optional `expect_prior_content_hash` (canonical JSON sha256 over `{type, props, content}`; location fields excluded so a future `move` op can't invalidate an in-flight update's precondition). Mismatch throws new `StalePreconditionError` (code `stale_precondition`, httpStatus 409) inside the transact closure; transact unwinds + dispatcher rolls the whole tx back → no partial write lands. Row-metadata bridge thinner than rename's (just `updated_at`, no title/slug) — same §6.5 pattern for row-side freshness of `doc.list` / `doc.get` parity with the CRDT mutation. Route `POST /docs/update/:doc_id` body={ops}; `deriveHttpBinding` auto-derives (body_key `ops` after path-param strip); parity test green. Six auth-chain e2e cases: 401 / 400-bad-uuid / 400-empty-ops / 403-guest / happy path (asserts `doc_updates seq:[1,2]` + inserted block in `GET /docs/get` + audit trail) / 404 / 409 stale-precondition (asserts `doc_updates seq:[1]` unchanged + error-class audit row). Codex peer-review drove two substantive post-commit fixes (`5aa7693`): (1) 409 response-schema literal was `"conflict"` but runtime returns `{ error: err.code }` = `"stale_precondition"` (trunk's error mapper uses `err.code`; `StalePreconditionError.code = "stale_precondition"`) — fixed response schema to the union of both literals, added body-shape assertion to the auth-chain 409 test; (2) empty `UpdatePatchInput` `{}` passed schema but drove ProseMirror's `setNodeMarkup` → `ReplaceAroundStep` (not a guarded no-op), producing a `doc_updates` write + `updated_at` bump for a semantic no-op — fixed with `.refine(...)` requiring at least one of `type`/`props`/`content`. Codex cleared the remaining slice-1 decisions: synthetic fixed-width `order_key` placeholder (replaced by projection-blocks job when it lands), `{type, props, content}` as the hash domain, discriminated-union rejection over runtime 501, thinner row-metadata bridge, BlockNote normalisation-tail test-side handling. §6.5 current-state caveat widened to frame the bridge as a shared pattern across content-mutation capabilities rather than rename-specific; capability count flipped 8 → 9. 31 capability unit tests / 8 route unit tests / 6 auth-chain e2e / 41 property tests; full monorepo green with all pre-push gates.
- **2026-04-21** — **`doc.rename` lands end-to-end** as the first content-mutation capability past `doc.create`; closes the `updateBlock` empirical gap. Four-commit slice: (a) `00335ee` — `withLiveEditor(ydoc, fn)` helper in `@editorzero/sync` owns the BlockNoteEditor mount/dispose lifecycle for handlers that need `view.dispatch`; blocknote smoke grows a 4th case pinning `editor.transact(updateBlock)` → `doc_updates seq:2` + replay projects `["heading", "New Title"]` (the write path `doc.rename` uses). (b) `aface08` — `ensureDomGlobals()` lands in sync with a save/restore dance that preserves Node's fetch suite + AbortController + crypto + streams across happy-dom's register (happy-dom's impls break Better Auth's Set-Cookie round-trip and throw against undici's Request/AbortSignal contract); `createApiApp` calls it when `dispatcher !== undefined` so dispatcher-bound trunks boot with DOM globals available (zero-arg `createApiApp()` stays side-effect-free for api-client typed-RPC consumers). (c) `6077a8f` — `doc.rename` capability (`doc:write`, `category: "mutation"`, NOT in `METADATA_ONLY_CAPABILITIES`) + `POST /docs/rename/:doc_id` route + sync-owned `setDocTitle(ydoc, title)` helper enforcing the title-slot rule (block 0 heading-1 → updateBlock in place; otherwise insertBlocks heading-1 at index 0 with `placement: "before"`). Dual-writes inside the write-path tx: UPDATE `docs.title / slug / updated_at` first (404 short-circuit), then `ctx.transact` → `setDocTitle`. 6 auth-chain e2e cases: 401/400-bad-uuid/400-empty-title/403-guest (doc:write gate)/create→rename→get happy path (asserts bridge + `doc_updates seq:2` + hydrated block reflects rename + audit trail create/rename/get allow)/404 missing with error-class audit. `deriveHttpBinding` auto-derives `POST /docs/rename/:doc_id` body=[title]; cliRegistry + parity test pick it up. 651/651 monorepo tests green. (d) This commit — docs sweep: architecture.md §6.5 current-state caveat frames the row-metadata bridge (title + slug + updated_at) + v1 slug-tracks-title semantic choice (future `doc.set_slug` decouples); Current phase count flipped to 8 capabilities; this history entry. Codex peer-reviewed before docs commit — cleared the slice with one push (caveat wording: row-metadata bridge, not title-only), applied pre-commit.
- **2026-04-21** — **Metadata-only atomicity CLOSED end-to-end** (outbox un-stub `4032c55` + property test `d6a82d5`). Slice closes Appendix C item 16 in two commits. Commit 1: `createOutboxWriter()` lands in `@editorzero/db` (UUIDv7 id, JSON-serialised payload at the column boundary, per-tx flush), `createApiDispatcher` write-path queues `ctx.outbox(event, payload)` calls during `fn(extras, auditTx)` and flushes via `outboxWriter.append(auditTx, …)` before the `withSystemTx` commits — single-tx atomicity with the handler's `ctx.db` writes, the `audit_events` row, and the paired `outbox(audit.appended)`. Read-path `ctx.outbox` throws loud (reads must not emit). `doc.publish` / `doc.unpublish` now emit `doc.visibility_changed` with `{doc_id, visibility, visibility_version}` for §5.4 cache invalidation. Commit 2: `packages/api-server/prop/metadata-only-atomicity.test.ts` exercises the real `createApiDispatcher` via `wrapDriverWithPlugin(real, createQueryFaultPlugin())` — wrapping the driver (not the dispatcher) means future changes to the factory's `runInWriteTx` fail the property test. Nine tests (1 baseline + 8-ordinal N-way crash-fuzz); reject arm asserts `docVisibility === "workspace"`, `allowAuditCount === 0`, `visibilityChangedOutboxCount === 0`, error-audit survives in separate tx. `FaultController.hit` self-disarms on trigger so the dispatcher's post-rollback `withAuditTx` (observability path) isn't re-faulted. Codex peer-reviewed across two rounds: round 1 drove (a) mirror-vs-real-factory refactor (property test originally duplicated `createApiDispatcher.runInWriteTx` wiring — drift risk), (b) flush-order comment fix (handler writes → allow-audit writes → handler-outbox flush), (c) same-commit docs flip (§6.5 + §10 + §17.1 row 7b + Appendix C rows 12/16 → CLOSED 2026-04-21); round 2 reran the real-factory suite and cleared.
- **2026-04-21** — **MCP adapter slice 1 lands end-to-end** (ADR 0026 `75faf08` + `@editorzero/mcp-server` scaffold `016c03b` + `/mcp` trunk mount + full-stack integration `55b3728`). Narrow-by-construction ADR records two first-slice choices ADR 0009/0021 did not pin: transitional cookie auth posture + deliberately stateless. Six load-bearing commitments: (1) principal via Hono middleware chain — `authInfo.extra.principal` smuggling rejected, per-request `McpServer`+`StreamableHTTPTransport` construction is slice-1 illustration NOT an ADR commitment; (2) deliberately stateless; (3) registry → tool loop with `cap.input.shape`; (4) three-way error cut (`EditorZeroError` → `{isError, structuredContent}`; protocol → `McpError`; unexpected → SDK default); (5) `humanOnly` structural filter; (6) no OAuth discovery metadata in slice 1. Peer-reviewed by Codex across two rounds: round-1 pre-draft delivered 5 pushes (authInfo.extra rejection, stateless-as-constraint, error three-way cut, humanOnly filter, no-half-OAuth); round-2 post-draft narrowed commitment 1 from "request-scoped transport lifetime" to "principal-via-Hono" (freezing object lifetime would implicitly foreclose the GET/SSE shape `StreamableHTTPTransport` supports). Scaffold exports `createMcpHandler({registry, dispatcher, serverInfo})` as a terminal Hono handler; `isMcpTool(cap)` + `toToolConfig(cap)` surface for contract-matrix parity. Four modules at 100% line / 95% branch with 24 tests (19 unit + 5 in-isolation integration via MCP SDK's `Client` + custom fetch). Trunk mount: `createApiApp({auth, loadRoles, dispatcher, registry})` mounts `/mcp` behind the same principal chain `/docs/*` uses; three composition-time guards fail loud at boot (registry-without-dispatcher, registry-without-auth, existing auth-loadRoles pairing). Full-stack `composition/mcp-chain.integration.test.ts` drives real SQLite + Better Auth + HocuspocusSync + dispatcher + MCP adapter behind cookie auth: tools/list, doc.list via tools/call (empty workspace), doc.create via tools/call (content mutation through write-path tx + Hocuspocus with `doc.list` thereafter showing the row), unauthenticated 401 at principal middleware. api-server: 93 tests (up from 89), 99.87 line / 97.1 branch / 100 function. Still open for slice-1 follow-on: MCP ↔ registry contract-matrix parity test, Appendix C row 15 update, architecture.md §15.1 tool-adapter pointer.
- **2026-04-20** — **CLI binary ↔ real-HTTP e2e harness lands** (`03519f5`). Closes Nomi's catch — every prior CLI test ran in-process (`trunk.request.bind(trunk)` as injected `fetch`), so the `bun build --compile`-produced `ez` binary had never hit a real HTTP server. New `apps/cli/src/e2e/cli.e2e.test.ts` compiles the binary fresh per run (~180ms bundle+compile), boots `createApiApp` on an ephemeral port via `@hono/node-server`, and spawns the binary as a subprocess with `HOME` pointed at a per-test tmp dir so `SessionCookieStore` starts empty each test. Cases: happy path (`ez auth login --password-stdin` → `ez doc create` → `ez doc list` — asserts exit codes, AXI stdout shape, credential file at `$HOME/.editorzero/credentials`); inverse (`ez doc list` with pristine HOME → AXI `auth_expired` + exit 1 without network). Codex review drove test-independence (sequential credential dependence between tests would let test 1's failure silently contaminate test 2). Harness caught a real fixture bug on first run — dispatcher-without-sync 500'd on `doc.create` over TCP, fixed by wiring production-shape `HocuspocusSync` into the e2e fixture. Lane split: `vitest.e2e.config.ts` + `test:e2e` script (no coverage, 60s timeout), wired into lefthook's pre-push `e2e` lane unconditionally (a scaffold-check here would be a silent-skip foot-gun). Default `pnpm test` excludes `**/*.e2e.test.ts` so pre-commit stays fast. ADR 0012's `bun build --compile` deploy-artifact promise is now verified end-to-end for the first time. Next.js-mounted smoke deferred until `apps/app` lands — Codex + I agreed the CLI slice's question ("does the shipped artifact work against the real HTTP boundary") is a different question from the Next-mount question ("does the app host/mount behave correctly"), and the former shouldn't block on the latter. When `apps/app` lands, the `@hono/node-server` boot step swaps for a Next.js programmatic boot; test body unchanged. MCP + future surfaces inherit the harness pattern.
- **2026-04-20** — **ADR 0025 (CLI auth bootstrap + credential-store seam) lands as the design gate for the P3.7 CLI surface slice.** Settles the three questions ADR 0021 left open: bootstrap credential, on-disk credential shape, CLI principal orientation. Chose option C (email+password → session cookie stored in `~/.editorzero/credentials`) for the first slice as a **bounded transitional bootstrap** — works against the stack we have today (`/auth/sign-in/email` + `/auth/sign-out` already live post-ADR 0024), zero new server-side work aside from a new `/infra/whoami` route. Three fences committed: (1) `AuthCredentialStore` interface so future `DeviceFlowStore` / `BearerTokenStore` / `AgentAuthTokenStore` implementations plug in without command-tree churn; (2) `--password-stdin` for agent-mode (AXI "no interactive prompts") + interactive prompt only in TTY; (3) `/infra/whoami` as editorzero's canonical principal-orientation route — wraps `c.get("principal")`, **not** BA's `/auth/get-session` which would return BA's user/session shape and silently disagree with what the dispatcher/gate enforces. Device flow mechanics + PAT issuance UX + agent-token bootstrap + session-hook self-install (with the `process.execPath`-in-dev-is-Bun / skip-on-CI / env-var-opt-out fences) all deferred to their own follow-on slices. Codex peer review drove three ADR-level corrections: `/infra/whoami` addition (BA's `get-session` ≠ editorzero's `Principal`), store-seam fence, and tight hook-install scoping. Rejected options: device flow (needs Web UI that doesn't exist), paste-PAT (needs admin surface that doesn't exist), agent-token-first (conflates human bootstrap with agent-creation credential). Next commit: `apps/cli` skeleton + auth subtree + `/infra/whoami` route. Then generator-backed `ez doc list` (N=1 already through the registry-driven generator; no hand-wired capability commands). Then widen to the remaining six capabilities + contract test.
- **2026-04-20** — **ADR 0024 workspace_members slice lands.** Replaces the pre-slice hardcoded `roles: ["member"]` in `@editorzero/auth`'s resolver with a Layer-1 role lookup against a new editorzero-owned `workspace_members` table (composite PK `(workspace_id, user_id)`, singular `role` column, ADR 0017 `deleted_at` for cascade, `updated_at` for revive-in-place). Better Auth stays credentials-only (`user`, `session`, `account`, `verification`); BA's `organization` / `teams` / `admin` plugins were rejected (Codex peer review + Opus Explore agent converged on Option C — custom table, BA authenticates-only). Signup bootstrap lands in the slice: `createAuth` adds a `databaseHooks.user.create.after` hook that inserts the `workspace_members` row as `role: "owner"` (the signing-up user owns their auto-minted workspace); BA fires `after` hooks via `queueAfterTransactionHook` post-commit (not atomic with the user insert), so a hook failure commits an orphaned user row — but the error still propagates back to `signUpEmail`, so the failure surface is a loud signup 500 rather than a silent 401 on every subsequent request. `createLoadRoles(driver)` in `@editorzero/db` returns `readonly Role[] | null`; missing membership row → null → 401 (the production path can't hit strict-on-missing today — the null branch exists for future partial-hook-failure, ADR 0017 cascade, and migration-gap scenarios). Driver parameter typed against a narrow `LoadRolesDriver { system(): Kysely<SystemDatabase> }` shape so the helper composes with either `SqliteDriver` or `PostgresDriver`. `createApiApp({ auth, loadRoles, dispatcher })` gains a runtime pairing guard: auth + loadRoles must arrive together or both be omitted. Four happy-path allow tests land end-to-end through the full stack for publish / unpublish / delete / restore (default signup now produces an owner principal via the bootstrap hook). api-server: 79 tests at 99.86% line coverage; @editorzero/db: 99 tests; @editorzero/auth: 7 tests. ADR 0024 documents the four-option decision space + revisit triggers (multi-workspace + invites, orgs-above-workspaces, teams-within-workspaces, platform-admin role). architecture.md §3.4 widened (FK target fixed to `user`, `updated_at` added, revive-in-place + agents-not-members + signup-bootstrap notes). Codex peer review drove two fixes inside the slice: (1) the missing production bootstrap path — test-only seeding was masking a permanent 401 for every fresh signup; (2) `createLoadRoles` over-typed to `SqliteDriver`.
- **2026-04-20** — P3.7 API trunk: **`POST /docs/delete/:doc_id`** + **`POST /docs/restore/:doc_id`** land as the third and fourth metadata-only mutations; AGENTS.md invariant 6 (soft-deletes recoverable via first-class capability) is now reachable end-to-end at the API layer. Both capabilities: scope `doc:delete` (symmetric rollback rights — same posture as publish/unpublish's shared scope), single-statement UPDATE + RETURNING with `eb("visibility_version", "+", 1)` increment (public-route cache invalidation per §5.4 — Codex caught the missing version bump mid-review before code landed). Delete: flips `deleted_at` NULL→now, 404 on already-deleted (prevents recovery-window slide); capability id `doc.delete` but audit effect kind `doc.soft_delete` (forensic-reader asymmetry, distinguishes from future `doc.purge`). Restore: inverse flip, 404 on already-live (no no-op audit rows). Added both to `METADATA_ONLY_CAPABILITIES` in `packages/scopes`; widened the corresponding architecture.md enumerations (§3.5 DDL comment, §5.4 public-cache contract, §6.2, §6.5 metadata-only set, §16.2 single-tx note, Appendix C rows 7b + 16). v1 defers ADR 0017 cascade side-effects (search-index rebuild, embedding re-activation, notification cancellation) to their respective backing-system slices — cascade jobs will enqueue off the already-emitted `outbox` row when those systems land. 28 new capability unit tests (14 per capability, 100% coverage on both `delete.ts` + `restore.ts`), 6 new route unit tests, 8 new auth-chain e2e (401 / 400 / 403-member / 403-cross-workspace for each side). api-server: 73 tests (up from 59), 99.86 line coverage preserved.
- **2026-04-20** — P3.7 API trunk: **`POST /docs/unpublish/:doc_id`** lands as the inverse of `doc.publish` (second metadata-only mutation). Same shape — `category: "mutation"`, zero `ctx.transact`, single UPDATE + RETURNING with `eb("visibility_version", "+", 1)`; flips `visibility="workspace"` (default post-create state) and always-bumps the version so F5-driven cache invalidation fires even on re-assertion. Scope `doc:publish` (mirror of publish — admins retain rollback). 14 capability unit tests (100% coverage), 3 route unit tests, 4 auth-chain e2e tests (same 401 / 400 / 403-member / 403-cross-workspace envelope as publish; allow blocked on `workspace_members` role widening). Refactor committed separately: route layout flattened from `routes/<domain>/<capability>/index.ts` to `routes/<domain>/<capability>.ts` — one `index.ts` per domain as aggregator, matching the capabilities-package pattern.
- **2026-04-20** — P3.7 API trunk: **`POST /docs/publish/:doc_id`** lands as the first metadata-only mutation capability route. `doc.publish` capability is the first real user of the `METADATA_ONLY_CAPABILITIES` allowlist in `@editorzero/scopes` — category: "mutation", zero `ctx.transact` calls, single UPDATE + RETURNING with Kysely expression-builder increment (`eb("visibility_version", "+", 1)`). Visibility-only slice: flips `visibility="public"`, bumps `visibility_version`; `published_slug` + `published_at` columns are deferred to the public-route renderer slice (see `docs/architecture.md` §3.5 scope note). *(Historical — superseded by ADR 0040 Step 5: this de-overloads `visibility`, so `visibility="public"` becomes `published_at IS NOT NULL` and read scope moves to `access_mode {space,private}`.)* 4 auth-chain e2e tests (401 / 400 / 403-member / 403-cross-workspace — happy-path allow blocked on `workspace_members` role widening per `packages/auth/src/resolver.ts` header). 14 capability unit tests, 3 route unit tests. Codex peer-reviewed pre-commit; flagged the architecture/DDL mismatch on `published_*` (deferred explicitly) and a stale SELECT-then-UPDATE rationale (switched to single-statement Kysely increment).
- **2026-04-20** — P3.7 API trunk slice: `GET /docs/get/:doc_id` lands (`eae0945`). Closes the read-path on the three shipped capabilities — list / create / get all reachable through `hc<AppType>` RPC + HTTP. `createApiDispatcher.runRead` now routes `ctx.transact` through the tx-less `HocuspocusSync.read` seam (clone-before-fn; no live-doc contamination). Global `trunk.onError` mapper added to project `EditorZeroError` subclasses to their `httpStatus`. AGENTS.md: added "push after every commit" to the Commit ritual.
- **2026-04-19** — P3.7 API trunk slice: `POST /docs/create` lands end-to-end (`23cf89d`). `createApiDispatcher` gains an optional `sync?: HocuspocusSync` seam; `runInWriteTx` binds per-invocation so `ctx.transact` persists `doc_updates` inside the write-path tx and `bound.rollback()` drops the Y.Doc on handler throw (P3.6e pattern lifted across the composition boundary). 36 api-server tests at 100% line/branch. `GET /docs/get/:doc_id` still blocked on a read-path `ctx.transact` design decision — the hydration hook needs a SQL read handle but the dispatcher's `runRead` must not open a tx (avoids RESERVED lock). Options: extend `HocuspocusSync` with a tx-less `bindForRead` variant, add a per-capability read-via-projection mode, or read directly from `doc_updates` without Hocuspocus for Phase 3. Worth consulting Codex before committing to an approach.
- **2026-04-19** — P3.7 API trunk first slice: `GET /docs/list` + Better Auth + principal + dispatcher middleware (`157a36e`, `b4596d3`, `d019447`, `d0d99cf`). `createApiApp({ auth?, dispatcher? })` factory + default `app` for `hc<AppType>`. `auth-chain.integration.test.ts` proves the full stack round-trips under an authenticated session with tenant isolation.
- **2026-04-19** — ADR 0023 (Postgres driver substrate) + dual-backend conformance harness landed across 3 commits: ADR writeup (`fb7fe27`), Postgres driver + 9-test testcontainers unit harness (`8c52ee3`, `packages/db/src/drivers/postgres.ts` / `postgres-ddl.ts`), and the parametrised SQLite+Postgres conformance suite (`87e233b`, `packages/db/test/integration/` — 14 tests). Activates lefthook's pre-push `integration` lane. Codex adversarial review pre-coding drove three reversals (selective BIGINT/INTEGER split, per-pool `types` override, retry-semantic framing). Closes Appendix C item 4.
- **2026-04-19** — BlockNote adapter-boundary smoke landed (`packages/sync/src/blocknote.integration.test.ts`): closes the no-WS half of Appendix C item 11 (3 tests: smoke / projection / rollback). Empirical finding — server-side `BlockNoteEditor` mutation needs a DOM shim (`happy-dom` + `editor.mount`); without it the y-prosemirror plugin's writes never reach the fragment. AGENTS.md gotcha + ADR 0018 § Empirical verification updated.
- **2026-04-19** — P3.6 closes for content mutations: write-path-atomicity crash-fuzz property test; ADR 0018 § Empirical verification written up.
- **2026-04-18** — P3.6a–e: dispatcher write-path tx primitive → `HocuspocusSync` persists `doc_updates` in-tx → audit writer emits `outbox(audit.appended)` in same tx → `onLoadDocument` hydration + `BoundSyncService.rollback`.
- **2026-04-18** — Red-team pass #4 against landed code (F85–F97): `WorkspaceScopingPlugin` alias/join-aware, coverage floor 95/90, runtime `ctx.transact` at-most-once backstop.
- **2026-04-18** — P3.5: `doc.create` + `doc.get` + `doc.list` end-to-end through dispatcher → tenant-scoped DB → audit. ADRs 0021 (surface transport) + 0022 (agent editing) landed.
- **2026-04-18** — Phase 3 foundations landed: `constants`, `ids`, `scopes`, `audit`, `principal`, `capabilities`, `errors`, `dispatcher`, `db`, `sync`, `blocks`, `observability`, `config`.
- **2026-04-18** — Phase 2 closed after three red-team passes (F1–F84, cross-model on pass-3). Phase 1 ADRs 0001–0020.

## Resume protocol

1. `AGENTS.md` is auto-loaded.
2. Read this file for current focus + what's next.
3. `git log --oneline -20` for in-flight work.
4. Read only ADRs relevant to the immediate focus.
5. If an open question would block the next action, raise it; otherwise proceed.

## Update protocol

- At phase boundaries: update Current phase, Immediate focus, What's next, Recent history; commit.
- Mid-phase: one-line entries in Recent history. Don't paste commit bodies or review transcripts — those live in the commit itself.
- Roll entries older than ~7 off the bottom. `git log` carries the deeper trail.
