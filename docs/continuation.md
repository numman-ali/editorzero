# Continuation — rolling work state

*Rolling work-state journal. `AGENTS.md` is already in your context — `CLAUDE.md` is a 1-line `@AGENTS.md` import that Claude Code expands at session start. This file is what an agent updates as work progresses; commit with each phase-boundary update.*

---

## Current phase

**Phase 2 (architecture) CLOSED 2026-04-18.** Three red-team passes (F1–F30 / F31–F53 / F54–F84, cross-model Opus + Codex on pass-3) all applied. Architecture stabilized at ~2616 lines; ADRs 0004, 0006, 0007, 0014, 0016, 0018 updated to match; AGENTS.md invariants refined; disposition docs under `docs/adr/red-team-phase-{1,2,3}.md`. Phase 3 (verification harness) open.

## Immediate focus — Phase 3.6 (4/6 sub-slices done) + Phase 3.7

**P3.5 "create doc, read doc" CLOSED 2026-04-18.** Eight commits shipped: constants → ids → sync/MemorySyncService → seed/readBlocks → `doc.create` → `doc.get` → Appendix-A coherence activation → this roll-forward. The kernel pipeline is proven end-to-end for one mutation + one read (dispatcher gate → tenant-scoped db → `ctx.transact` → audit projection), with Codex pre-commit review applied on every commit. Two ADRs landed alongside: [0021](adr/0021-surface-transport-topology.md) (Hono trunk + typed RPC, `citty` + AXI, MCP Streamable-HTTP-only) and [0022](adr/0022-agent-editing-constraints.md) (`expect_prior_content_hash`, reserved `precondition_policy` + `markdown_anchor`). Coverage floors hold (95/90); `pnpm coherence` green across 6 active checks (drift-prevention is real, not aspirational — F89 repaired).

**P3.6 sub-slice status (ADR 0018 empirical verification, 4/6 done):**

- **P3.6a ✅** *(5005e9d)* — `doc.create` pre-mints `BlockId`s and records them in the audit effect's `seed_blocks: SeedBlock[]` field. Closes gap (b): seeded block IDs now in the audit envelope, no more "anonymous blocks BlockNote minted that the audit trail never saw." Invariant 3a becomes complete for `doc.create`.
- **P3.6b ✅** *(cd2ebdd)* — Dispatcher gains the write-path tx primitive (F31). `DispatcherDeps` splits into `runInWriteTx` (mutation/auth/admin/system — opens one `BEGIN IMMEDIATE`, commits handler DB writes + allow audit atomically), `runRead` (`category: "read"` — no SQL tx, audit via `withAuditTx`), and `withAuditTx` (short-lived audit-only tx for deny/error/input-validation rows). SQLite driver narrows `BEGIN IMMEDIATE` to `withSystemTx` via `setIsolationLevel("serializable")` so `system()`/`scoped()` stay on DEFERRED begin; savepoint passthroughs delegate to the inner `SqliteDriver`. Seven integration tests cover allow/throw/shape-violation/post-parse-deny/gate-deny/input-validation/read with empirical `runners.writeTx === 0` proof on reads. `@editorzero/db` audit writer lands with `deny_reason` pulled from `effect.reason_code` (capability-owned public classification).
- **P3.6c ✅** *(5583e42)* — `HocuspocusSync` lands as the production `SyncService`: `bind(HocuspocusTxContext): SyncService` closes over the caller's `AuditTx` so `transact(doc_id, fn)` opens `server.openDirectConnection`, runs `fn` against the live Y.Doc, captures `document.on("update")` deltas across the full promise chain (sync + post-`await` tails), merges via `Y.mergeUpdates`, and persists through the injected `DocUpdatesWriter` under the same SQL tx `withSystemTx` opened. `@editorzero/db` gains `createSqliteDocUpdatesWriter` — auto-bootstraps `doc_counters` via `ON CONFLICT DO NOTHING`, then SELECT + UPDATE for gapless seq allocation (§6.4, no `FOR UPDATE`). `doc.create` re-orders to INSERT docs before `ctx.transact` so the FK on `doc_counters` / `doc_updates` is satisfied when the writer fires. Dispatcher write-path integration extended with 4 content-mutation cases (allow commit, handler-throw rollback, output-shape-violation rollback, post-parse-deny rollback); sync package ships 14 integration tests against real Hocuspocus + in-memory SQLite. Three implementation subtleties worth flagging: (1) direct connections are held alive in a `#liveConnections: Set` until `sync.close()` because `DirectConnection.disconnect()` in 3.4.4 unconditionally unloads the Y.Doc when the last connection drops (regardless of `unloadImmediately`) — dropping between invocations would silently wipe resident state; (2) the `update` listener brackets the full `fn` Promise, not the sync callback, so `async` handlers yielding on `await` still have their post-yield mutations captured (matches `SyncService.transact`'s published contract); (3) a per-doc async mutex (`#docLocks: Map<DocId, Promise<void>>`) serialises same-doc transacts so one invocation's listener never cross-contaminates another's — SQLite's single-connection serialisation already funnels `withSystemTx` calls so the mutex is belt-and-suspenders there, but is load-bearing under Postgres (ADR 0007). Three Codex adversarial findings addressed mid-slice: P1 same-doc concurrency (fixed with the mutex), P2 rolled-back content visible to later reads (remains deferred to P3.6e — the fix is hydration + rollback-signal, which wiping Y.Doc state pre-hydration would regress onto seeded-via-`ctx.transact` test fixtures), P3 `doc.create` bootstrap ordering (fixed with the writer auto-bootstrap + handler re-order). Invariant 7 closes end-to-end for **durable** content mutations: `docs` INSERT/UPDATE + `doc_counters` bootstrap + `doc_updates` + `outbox(doc.updated)` + `audit_events` + `outbox(audit.appended)` commit atomically or not at all.
- **P3.6d ✅** *(49988aa)* — `createSqliteAuditWriter` emits `outbox(audit.appended)` inside the same tx as the `audit_events` INSERT (architecture.md §6.2/§6.3). Payload `{ audit_id, capability_id, outcome, category }` — `audit_id` is the join key for downstream re-fetch; the other three let the poller compose webhook event keys (`audit.appended.<capability>.<outcome>`, §15.4) without a round-trip. Same-tx rollback coverage pins that the pair never desynchronizes. The original P3.6d scope (dispatcher primes `doc_counters` on the `doc.create` audit effect) is subsumed by P3.6c's writer auto-bootstrap — strictly more general, runs inside the same write-path tx, already shipped. Integration tests in the P3.6c content-mutation section updated: allow now commits 2 outbox rows (`doc.updated` + `audit.appended`); rollback paths (handler-throw / output-shape-violation / post-parse-deny) commit 1 outbox row from the separate `withAuditTx` so webhook subscribers still observe the failure. 174 tests passing.
- **P3.6e** — `write-path-atomicity.prop.ts` crash-fuzz property test: random fault injection across the four-row commit (`doc_updates` + `outbox(doc.updated)` + `audit_events` + `outbox(audit.appended)`) proves the commit boundary is all-or-nothing. Phase 3's flagship verification artifact. **Also closes P3.6c's one carry-over** (Codex P1 from P3.6c's review): `onLoadDocument` replay from `doc_updates` + a `SyncService.rollback(doc_id)` signal threaded from the dispatcher's post-tx failure path so the in-memory Y.Doc unloads on rollback and rehydrates cleanly from durable state. Pre-hydration, a naive "unload on abort" would wipe seed state placed by the same `ctx.transact` call (test fixtures break), so the fix ships together with hydration here.
- **P3.6f** — ADR 0018 empirical-verification writeup; continuation roll-forward; P3.6 closes as Phase 3's write-path-atomicity gate.

**P3.7 — Appendix C entry checklist green.** Each item in `architecture.md § Appendix C` (verification stack) passes in CI/pre-commit as applicable, plus the new coherence rule from this session (Check 5/6) is referenced for capability-matrix drift prevention. Last gate before Phase 4.

## Drift-prevention posture (new, Phase 3 onward)

Root-cause observations from the three red-team passes: prose-as-spec hides bugs that types + tests catch instantly; hand-maintained duplicates drift. Therefore:

- **Code-as-spec over prose-as-spec.** Types + property tests are canonical. ADRs explain *why*, not *how*.
- **Single source of truth, derived elsewhere.** Registry → OpenAPI / MCP / contract matrix / Appendix A. Constants → `packages/constants/`. Enumerations → `as const` arrays.
- **Coherence script at pre-commit** (item 2 above) is the load-bearing enforcement.
- **Coherence checks protect *derived* artifacts** (architecture.md, Appendix A) against canonical code. AGENTS.md is agent-loading context, not a derived artifact — it holds rules + pointers, not enumerations. Do not add coherence checks against AGENTS.md prose.
- **Cross-model red-team reserved for ADR-level decisions + phase boundaries.** Not routine — it earns its keep on BLOCKER-class questions (pass-3 caught invalid SQL that way). Per-PR is ceremony.
- **Small diffs per commit during implementation.** Big sub-agent fix-batches regressed things in Phase 2 (pass-3's F74 was introduced by pass-2's F40 fix). Small focused edits + CI gate are the Phase 3 discipline.

## Resume protocol (for a fresh-context agent)

1. **`AGENTS.md` is already loaded** — `CLAUDE.md` is a 1-line `@AGENTS.md` import expanded by Claude Code; canonical rules + invariants + gotchas are in your context.
2. **This file is your next read** — current phase, immediate focus, open questions (below).
3. Read `docs/brief.md` — Phase 0 framing.
4. Read `docs/adr/README.md` — ADR index.
5. Skim `docs/adr/red-team-phase-1.md` + `docs/adr/refresh-research-phase-1.md` for the reasoning trail behind the stack (why we landed here, not just what).
6. `git log --oneline -20` — work-in-progress head.
7. `TaskList` — task state carries across sessions. Also: check the latest `docs/adr/red-team-phase-*.md` for outstanding unrebutted findings.
8. **Do not read all 20 ADRs front-to-back unless necessary.** Read only those relevant to the immediate focus.
9. If any "Open questions" below would block the next action, raise them before acting.

## Open questions for @numman

1. **Commercial arm** (carried from `docs/brief.md`): OSS-only or OSS + hosted? AGPL-3.0 + DCO (ADR 0001) does not pre-commit the answer; adopting any `@blocknote/xl-*` package in the future would foreclose permissive/dual-license moves (ADR 0004). **Still open; does not block Phase 3.**
2. **Agent offline-edit** (carried from `docs/brief.md`): do agents get offline/reconcile semantics or are they assumed always-online? Default "always-online" stands unless challenged. **Still open; does not block Phase 3.**

### Archived (resolved 2026-04-17)
- Scale target — 500–1,000 minimum, 10k headroom. Folded into [ADR 0007](adr/0007-database-strategy.md).
- Sub-block ACLs — deferred; `AccessPath.selector` reserved. Folded into [ADR 0015](adr/0015-permission-enforcement.md).
- BlockNote vs Tiptap — re-validated via research pass; BlockNote confirmed, ADR 0004 + 0018 corrected on primitive naming and license framing.

Nothing open blocks Phase 3. Defaults for (1) and (2) are captured in `architecture.md`.

## Recent history

- **2026-04-17 (Phase 0 + 1):** Phase 0 scaffolded → Phase 1 v1 ADRs (12 → 19, after first red-team pass) → Phase 1 v2 ADRs (post-refresh; BlockNote replaces Milkdown, Better Auth consolidation, Next 16.2, block-model rewrite, new git-mirror ADR 0020). Public repo created at https://github.com/numman-ali/editorzero. Workflow confirmed: direct-to-main, pre-commit hooks, no PRs needed.
- **2026-04-17 (Phase 2 — drafting):** `docs/architecture.md` drafted end-to-end (data model, capability matrix, four-surface adapters, permission worked examples, event/audit model, OpenAPI + MCP surface drafts, verification wiring, engineering primitives §16 for agentic coding). Red-team pass #1 produced F1–F30; applied.
- **2026-04-17 (Phase 2 — BlockNote validation):** research pass confirmed stack vs Tiptap; corrected ADR 0018 write-path primitive naming (`ServerBlockNoteEditor.transact` → `BlockNoteEditor.create({ collaboration }).transact` inside `openDirectConnection.transact`) and ADR 0004 license framing + DINUM/ZenDiS funding signal. AGENTS.md + continuation.md audit + edits applied.
- **2026-04-17 (Phase 2 — pass #2):** red-team pass #2 produced F31–F53 (3 BLOCKER: audit atomicity, deny/error audit, move-op; 7 HIGH: DR, secrets, seq HA, reconcile clobber, residency, HNSW memory, outbox HA; 9 MEDIUM; 4 LOW). All applied. Architecture grew 2061 → 2434 lines.
- **2026-04-18 (Phase 2 — pass #3, cross-model):** Opus + Codex independent red-team pass on the same prompt. Opus produced F54–F72; Codex produced F73–F84 (renumbered from C54–C65 to avoid collision). Combined: 25 unique findings — 3 BLOCKER (`state_vector_at_fetch` unreconstructible → replaced with opaque `reconcile_base_token`; HA outbox lost-event regression from pass-2 F40 → single-tx claim+enqueue; `SELECT max(seq) FOR UPDATE` invalid SQL → `doc_counters` row-lock), 10 HIGH (ADRs 0006/0018 stale; `CapabilityContext.transact` type wrong; `doc.rename` misclassified; webhook CRUD + attachment multi-step missing from Appendix A; `mirror.reset` split; revocation event-key mismatch; secret cache vs rotation; orphan uploads), 10 MEDIUM, 2 LOW. All applied. Architecture at 2616 lines. Phase 2 CLOSED. Cross-model review proved its keep: Codex caught three BLOCKERs Opus missed; Opus caught the `CapabilityContext` type bug Codex missed.
- **2026-04-18 (Phase 3 — foundational packages landing):** P3.1–P3.4 wrapped for the contract layer. Packages now real: `constants` (numeric SSOT), `ids` (branded + UUIDv7/v4 validators), `scopes` (Role/Scope/Surface/CapabilityCategory/SubjectKind + METADATA_ONLY_CAPABILITIES + AGENT_SCOPE_TIERS + QueueName + FidelityTier as-const arrays), `audit` (~90 AuditEffect variants from §16.3 + AuditRecord envelope + AuditWriter interface), `principal` (UserPrincipal/AgentPrincipal union + TenantContext + AccessPath + reserved SubBlockSelector brand), `capabilities` (Capability<I,O> + CapabilityContext kernel), `errors` (EditorZeroError family per §16.10). Toolchain decisions: `moduleResolution: "Bundler"` + `module: "Preserve"` so relative imports stay extensionless (bundlers handle resolution at app build; `tsc -b` emits for typecheck only); `useNamingConvention` off (snake_case mirrors DB columns throughout; enforcing camelCase would need a mapping layer without safety gain); kernel's `CapabilityContext<TEditor>` is generic with default `unknown` so the package stays dep-light, sharpened to `BlockNoteEditor<BSchema, ISchema, SSchema>` when `@editorzero/blocks` lands (BlockNote's real tri-generic; earlier drafts said `BlockSpecSchema`, a made-up name). One arch-drift fix applied: `admin.diagnose` AuditEffect variant was missing from §16.3 but referenced in Appendix A — added to both code + arch. Remaining P3: dispatcher, db, sync, blocks, auth(+service), api-server, cli, mcp-server, jobs, mirror, webhooks, observability, config, contract-tests, e2e, apps/{app,admin} — scaffold lazily as the P3.5 "create doc, read doc" slice pulls them in.
- **2026-04-18 (P3.5e — dispatcher + typed error subclasses):** dispatcher package landed with the parse → gate → invoke → parse → audit pipeline and `scopeOnlyGate()`; 4 unit tests cover allow / deny / input-validation / handler-throw. The work drove a kernel-wide cast purge: `EditorZeroError` now an abstract base with typed per-subclass fields (`NotFoundError.subject_kind`, `UpstreamError.service`, etc.) and its own `toHandlerError(): HandlerError` — dispatcher narrows via `instanceof`, no central `switch`, no `fields?: Record<string, unknown>` bag. `audit` re-exports `DenyReason`/`HandlerError` from `errors` (dep direction: errors is downstream of ids/scopes, audit depends on errors). `scopes` replaced a narrow-tuple cast with `ReadonlySet<string>` for `isMetadataOnlyCapability`. `capabilities` introduced `RegisteredCapability<TEditor>` (heterogeneous shape the registry stores) + `registerCapability()` wrapper — typed `I`/`O` live inside a closure, no `as` at the registration boundary. `RegistryLookupError` is now a real class for clean `instanceof` narrowing. One design bug caught by the input-validation test: calling `capability.audit.effectOnError(rawInvalidInput, …)` would re-parse via zod and throw — fixed with a dispatcher-owned audit row (`writeInputValidationAudit`) that bypasses the projections (which correctly assume validated input).
- **2026-04-18 (P3.5f — db package + Layer-2 tenant scoping):** Layer 2 of the permission model landed as `@editorzero/db` — a Kysely 0.28 `OperationNodeTransformer` plugin that auto-injects `workspace_id = <scope>` into every SELECT/UPDATE/DELETE WHERE against tables listed in `TENANT_SCOPED_TABLES` and forces the column into every INSERT values list. Explicit `workspace_id` disagreement throws `TenantScopeViolationError`; INSERT…SELECT and `DEFAULT VALUES` into tenant-scoped tables are rejected (shapes the plugin can't safely modify). `TenantScopedDb = Kysely<Database>` with no structural brand — the invariant is enforced by scope (the unscoped Kysely base is never exported) plus the planned `no-raw-kysely-outside-db` arch-lint rule. SQLite driver wraps `better-sqlite3` behind `SqliteDialect`; the narrow public surface is `scoped(workspace_id)` + `exec(sql)` + `close()`. 7 integration tests against real in-memory SQLite prove the four invariants (cross-tenant SELECT returns 0 rows without explicit predicate; INSERT auto-injects scope; UPDATE/DELETE only affect scope's rows; mismatched explicit scope throws). `capabilities/kernel` now imports the real `TenantScopedDb` from `@editorzero/db`, replacing the placeholder `TenantScopedDbHandle` brand; dispatcher test fixture swaps its brand stub for `driver.scoped(WORKSPACE_ID)`. Schema (just `docs` table) remains hand-written until Atlas + `kysely-codegen` wiring lands in a later P3.5 sub-slice — comment in `schema.ts` flags the planned replacement.
- **2026-04-18 (typecheck hole closed):** `tsconfig.base.json` excludes `**/*.test.ts` from emit to keep `dist/` clean, and vitest strips types without checking — so `tsc --noEmit` was silently skipping every test file. Found when an `audit.access_log` discriminator not actually present in `AuditEffect` compiled clean in two test files. Per-package `tsconfig.test.json` (extends package config, re-includes tests, `noEmit: true`, separate `.tsbuildinfo.test`) now runs as a second pass after the main `tsc --noEmit`; pre-commit `types` hook exercises it via each package's `typecheck` script. Applied to `dispatcher`, `capabilities`, `db` (the three packages with tests today). Real drift the hole hid: (a) missing `audit.access_log` variant — added as the canonical read projection (§9.3), collapse stays on `AuditWriteInput.collapsed_count` not the effect; (b) `AuditTx` not re-exported from the audit barrel despite test references; (c) `seedRow` widened branded IDs to `string` — signature now accepts `DocId`/`WorkspaceId`/`UserId` directly. The `INSERT-without-workspace_id` test uses a single line-local `@ts-expect-error` (plugin AST injection runs *after* type checking, so Kysely's types can't model it; fails closed if the plugin ever starts satisfying the static shape). Followup: `audit.access_log` effect body stripped to `{ kind: "audit.access_log" }` — `principal_kind`/`capability_id` already live on the `AuditWriteInput` envelope, duplicating them in the effect body was a must-stay-in-sync hazard for no gain.
- **2026-04-18 (P3.5g doc.list — first real capability):** the baseline read capability that proves the full kernel pipeline works end-to-end without sync/blocks: dispatcher gate → tenant-scoped db → zod output parse → audit projection. `doc.list` issues a single SELECT against `docs` through `ctx.db`, relying on the `WorkspaceScopingPlugin` for Layer-2 isolation — handler never references `workspace_id`. Output IDs (`DocId`, `CollectionId`) survive zod parse via `z.string().transform((s) => DocId(s))` so the inferred output type preserves the brand; factories are idempotent, so re-parsing handler-produced branded strings is a no-op at runtime. This is the local pattern until `@editorzero/ids` gets branded zod helpers in a later sub-slice. 7 tests against real in-memory SQLite cover: empty list, order_key ordering, soft-delete filter (`deleted_at IS NULL`), cross-workspace isolation (composes with Layer-2), metadata declaration, `audit.access_log` allow effect, collapse policy (`{ collapsible: true, window_ms: 1000, collapseKey: () => "doc.list" }` — constant key because input is empty). Added `better-sqlite3` + `@types/better-sqlite3` to capabilities devDeps for the driver-backed test; runtime deps unchanged. `docList` exported from the capabilities barrel; registration into a `createRegistry([...])` is left to the consumer (app adapter), keeping the barrel dep-light.
- **2026-04-18 (red-team pass #4 — first pass against *landed code*):** Codex, invited with an open-scope prompt, produced F85–F97 (11 findings: 3 BLOCKER, 4 HIGH, 1 MEDIUM, 1 LOW, 4 UNUSUAL-GOOD). Headline pattern: "claim true in prose, not yet true in code." F85 — the prior-day typecheck-hole commit (`e72970f`) was itself an instance: per-package `tsconfig.test.json` existed but the pre-commit hook ran `pnpm -r … exec tsc -b` which skipped them, and the root `tsconfig.json` was missing `dispatcher` + `db` references (closed at `ec5f46f`). F86 — `DispatchInvocation` carried `principal`, `tenant`, and `access` as independent inputs, so Layer 1 could authorize workspace A while Layer 2 read workspace B; derive tenant from principal, assert `access.workspace_id === principal.workspace_id` at entry (`7c748cd`). F87 — `WorkspaceScopingPlugin` was neither alias-aware nor join-aware; aliased tables emitted invalid SQL and JOINed tenant tables never got the predicate (`31c5e28`). HIGHs: F88 post-parse deny audit path (`f605ade`), F89 downgrade coherence/arch-lint present-tense prose (`60424d1`), F90 widen `AuditWriteInput` + swap FNV-1a → sha256 (`0150ad2`), F91 apply ADR 0007 SQLite runtime pragmas at connection open (`0534825`). MEDIUM: F92 runtime `ctx.transact` at-most-once backstop + `TransactCalledTwiceError` (`2d854d6`). LOW: F93 `doc.list` sources `AUDIT_READ_COLLAPSE_WINDOW_MS` from `@editorzero/constants` (`54a21fb`). Coverage enforcement added alongside: 95/90 floor via `vitest.shared.ts` thresholds enforced on every affected package at pre-commit; Codex rescue used for the WorkspaceScopingPlugin branch-coverage fix (taught the TS+v8 pragma dance: `/* v8 ignore start/stop */` works where `next` / `if/else` forms didn't in the monorepo's remap pipeline). Close commit `6498464` moves F94–F97 + the F92 runtime guard into AGENTS.md § "Defend these" so future refactors meet explicit argue-past friction.
- **2026-04-18 ([ADR 0021](adr/0021-surface-transport-topology.md) — surface transport topology closed):** the previously ambiguous question "are the four surfaces peers sharing the dispatcher, or clients of a single HTTP trunk?" is now committed. **Decision:** Hono app is the single trunk; every surface consumes it via typed RPC (`hc<AppType>`). Server-side callers (Server Actions, RSC) use `testClient(app)` bound to `app.request` — full Hono middleware chain runs in-process, zero TCP hop (Hono's documented pattern). Client-side callers (CLI, remote MCP) use `hc(baseUrl)` over real HTTP. **MCP transport narrowed to Streamable HTTP only** — stdio dropped. `@hono/mcp` + `@modelcontextprotocol/sdk` 1.x retained; `xmcp` and `FastMCP TS` evaluated-and-rejected (xmcp has no Hono adapter, file-system routing fights the capability registry; FastMCP wraps Hono backwards). **CLI framework = `citty`** + `bun build --compile`; commander / oclif / clipanion evaluated and rejected. **CLI output governed by [AXI](https://github.com/kunchenguid/axi)** in agent mode (stdout errors, minimal schemas, idempotent mutations, session-hook self-install for Claude Code + Codex); clig.dev governs TTY mode. **Agent-mode stdout format (TOON vs JSON vs alternatives) deferred to an eval harness** at CLI-slice implementation time — judged on token cost AND agent task-completion success; JSON is the interim default.
- **2026-04-18 ([ADR 0022](adr/0022-agent-editing-constraints.md) — agent-editing constraints):** before block capabilities ship, narrowly-scoped ADR captures the non-reversible decisions and defers the reversible ones. Research (Notion MCP, Linear GraphQL, Outline #9521 split-authority bug, Claude Code `Edit(old, new)` contract, ProseMirror rebasing) informed three shipped commitments: (1) **optional per-op `expect_prior_content_hash`** (sha256 of canonicalized prior block JSON) on `update`/`move`/`remove`/`set_visibility` ops in `doc.update` — human UI omits; agents always send; mismatch throws `StalePreconditionError` inside the `ctx.transact` closure before op application; (2) **reserved `precondition_policy?: "strict"`** discriminator — v1 supports only strict; future `"rebase"`/`"advisory"` land as additive extensions; (3) **reserved `AccessPath.markdown_anchor`** sibling to the already-reserved `AccessPath.selector` — keeps content-addressed selection shapes (Notion's `selection_with_ellipsis`) open; null-only in v1. Full agent-ergonomic wrapper ADR (`doc.read`, `doc.grep`, `block.edit(id, old, new)`, `doc.outline`) + markdown block-ID anchor syntax + conflict-outcome policy beyond `"strict"` all explicitly deferred with trigger: first 100 real agent edit sessions OR Phase 5 hardening, whichever first. The other agent working through blocks / sync / `doc.update` reads this ADR before finalising the capability input schema. Cross-refs: ADR 0013 (anchor syntax open), ADR 0015 (`markdown_anchor` reservation), ADR 0018 (precondition inside transact). Architecture §6 header + Appendix A `doc.update` row updated. Architecture.md §5 + ADR 0009 + ADR 0012 updated with cross-references. The other agent implementing §5.1–§5.4 reads ADR 0021 before editing those slices.
- **2026-04-18 (P3.5 commit 6/8 — `doc.get` capability + Codex F105):** single-doc read — SELECT through `ctx.db` (Layer-2 scoped; handler never names `workspace_id`), block-array projection via `ctx.transact(doc_id, (ydoc) => readBlocks(ydoc))` (sole-path contract, invariant 7 + ADR 0018). Input shape `{ doc_id }` uses `z.uuid({ version: "v7" })` ahead of the `DocId` brand so malformed ids fail as 400 instead of unraveling the zod-4 transform-throw footgun. Soft-deleted rows surface as `NotFoundError` (invariant 6 — `doc.restore` owns recovery; leaking trash visibility to a non-`doc:delete` reader would be partial access). Cross-workspace reads return the same `NotFoundError` — the plugin's predicate makes row existence invisible, matching §8.3(a). `collapseKey: (input) => \`doc.get:${input.doc_id}\`` partitions reads per-doc (vs. `doc.list`'s constant key). **Codex F105 P1 fixed pre-commit**: initial handler returned `blocks: []` when `readBlocks` came back empty on an existing `docs` row, which on the `MemorySyncService` backend means "state was lost on restart" — a silent-overwrite footgun if a client saves the "empty doc" back (Outline #9521 shape). Handler now throws `InternalError` when `blocks.length === 0` on an existing row; `doc.create` always seeds heading + paragraph, so zero blocks is always an inconsistency in v1. 15 tests, 100% line coverage. Bundled the ADR 0021 / 0022 landing + architecture.md + continuation.md cross-references into the same commit (commit `25b4f87`, 13 files, 973 insertions).
- **2026-04-18 (P3.6a — pre-mint block IDs in `doc.create` audit envelope, commit `5005e9d`):** closes Codex F104 P1 / gap (b). `doc.create` now mints `BlockId`s via `generateBlockId()` before seeding, threads them into both BlockNote's `PartialBlock.id` (which `blocksToYXmlFragment` honours — verified in `@blocknote/core/src/api/nodeConversions/blockToNode.ts`) and the `doc.create` audit effect's new `seed_blocks: SeedBlock[]` field. Why: invariant 3a (audit-log-alone reconstructs final state) was a half claim for `doc.create` — BlockNote minted anonymous block IDs that the audit trail never saw, so a later `doc.rename`/`doc.update` that referenced them had no stable audit-recorded target to replay against. `SeedBlock` lives in `@editorzero/audit`, mirrors BlockNote's `PartialBlock` loosely (`id`/`type`/`props?`/`content?`); `content: unknown` is deliberate (narrowing would pull BlockNote's full inline-content tree into audit). Agent callers get the mapping back in the response so an immediate follow-up `doc.update` doesn't need a re-read. Regression test pins the BlockNote id-passthrough behaviour. Gap (a) — `doc_counters` priming — remains deferred to P3.6d.
- **2026-04-18 (P3.6b — dispatcher write-path tx primitive, commit `cd2ebdd`):** F31 lands. `DispatcherDeps` splits into `runInWriteTx` (allow path for mutation/auth/admin/system; opens one `BEGIN IMMEDIATE`, commits handler DB writes + the allow audit INSERT atomically; rolls back on handler throw / output-validation failure / post-parse deny), `runRead` (allow path for `category: "read"`; no SQL tx, handler runs against tenant-scoped read handle, allow audit via short-lived `withAuditTx`), and `withAuditTx` (audit-only tx for deny/error/input-validation rows and the read-path allow row — separate from the write-path tx so rollback doesn't take the audit entry down). Driver side, `BEGIN IMMEDIATE` is scoped to `withSystemTx` via `setIsolationLevel("serializable")`; `EditorZeroSqliteDialect` detects the signal and issues `BEGIN IMMEDIATE`, everything else falls through to the stock `SqliteDriver`'s DEFERRED begin. Savepoint passthroughs (`savepoint`/`rollbackToSavepoint`/`releaseSavepoint`) delegate to the inner driver so `ControlledTransaction` hooks issue real SQL. `@editorzero/db` gains `createSqliteAuditWriter` — maps `AuditWriteInput` → `audit_events` INSERT; `deny_reason` reads `effect.reason_code` (capability-owned public classification), not `reason.kind`, aligning with the `AuditDeny` contract real capabilities already use. Integration tests (`packages/dispatcher/src/writepath.integration.test.ts`, 7 scenarios) cover the full rollback matrix; the read test asserts `runners.read === 1` and `runners.writeTx === 0` — behavioural guard against a regression that would re-route reads into the write-path tx. Two codex adversarial-review findings addressed mid-slice: narrowing `BEGIN IMMEDIATE` to `withSystemTx` (was applied to every tx through `system()`/`scoped()`), and restoring savepoint delegation on the wrap (was dropped and would have silently no-opped `ControlledTransaction` savepoints). One `/codex:review` P1 finding (dispatcher "unconditionally" opens write tx for reads) was a false positive — the branch at `dispatcher.ts:460` routes reads through `runRead` and the integration test empirically proves it; rebuttal captured in the commit body. Not yet atomic with this tx: content mutations (P3.6c Hocuspocus `onStoreDocument`), `doc_counters.next_seq` priming (P3.6d).
- **2026-04-18 (P3.6d — audit writer emits `outbox(audit.appended)` in same tx, commit `49988aa`):** closes the remaining real slice of P3.6d. Every `audit_events` INSERT now pairs with a transactional-outbox row written inside the same tx — architecture.md §6.2/§6.3 was claiming this in prose before code; now it holds end-to-end. Payload `{ audit_id, capability_id, outcome, category }` stays tight: `audit_id` is the join key for downstream re-fetch; the other three let the poller compose webhook event keys (`audit.appended.<capability>.<outcome>`, §15.4) without a round-trip. Captured `audit_id` + `ts` once so the pair shares both values across retries. The original P3.6d bullet ("dispatcher primes `doc_counters` on the `doc.create` audit effect") is subsumed by P3.6c's writer auto-bootstrap — the ON CONFLICT DO NOTHING inside `createSqliteDocUpdatesWriter` is strictly more general (runs for any first-write, not just `doc.create`) and already lives inside the same write-path tx. All four outcomes (allow / deny / error / input-validation) flow through `createSqliteAuditWriter`, so every outcome produces exactly one `audit.appended` row. Rollback coverage: if the enclosing `withSystemTx` rejects, audit row + outbox row roll back together (unit test pins this). Integration tests updated: allow cases in the P3.6c content-mutation section now commit 2 outbox rows (`doc.updated` + `audit.appended`); rollback paths (handler-throw / output-shape-violation / post-parse-deny) commit 1 outbox row from the separate `withAuditTx` tx, so security/webhook subscribers still observe the failure even when the write-path tx rolls back. `doc-updates-writer.ts` docstring corrected — the `audit.appended` emission no longer lives in the "NOT emitted here" list. `/codex:review`: no findings. 174 tests passing across affected packages.
- **2026-04-18 (P3.6c — Hocuspocus sync persists doc_updates inside write-path tx, commit `5583e42`):** ships `HocuspocusSync` as the production `SyncService`. `bind({ sqlTx, principal, workspace_id })` returns a per-invocation `SyncService`; `transact(doc_id, fn)` opens a `DirectConnection` on a headless Hocuspocus server, subscribes to `document.on("update")`, runs `fn`, merges captured deltas via `Y.mergeUpdates`, and persists through the injected `DocUpdatesWriter` on the caller's `AuditTx`. Composition closes invariant 7 end-to-end for durable content mutations: `docs` INSERT + `doc_updates` + `outbox(doc.updated)` + `audit_events(allow)` + `outbox(audit.appended)` commit atomically or not at all; handler throw rolls all five back. `@editorzero/db` adds `createSqliteDocUpdatesWriter` (auto-bootstraps `doc_counters` via ON CONFLICT DO NOTHING; SELECT+UPDATE for gapless seq allocation, no FOR UPDATE). `doc.create` re-orders to INSERT docs row before `ctx.transact` so the writer's FK resolves against the real Hocuspocus-backed backend (prior seed-first ordering was safe only against the in-memory `MemorySyncService`). 14 integration tests in `packages/sync` (same-doc concurrency, post-await listener lifetime, FK on missing docs row, atomicity of the four-row commit) + 8 writer unit tests + 4 new dispatcher write-path cases. **Three codex adversarial findings addressed mid-slice:** P1 same-doc concurrency — `DirectConnection.transact` in Hocuspocus 3.4.4 doesn't serialise the mutation phase (only the subsequent `storeDocumentHooks` call is under `saveMutex`), so concurrent transacts could cross-contaminate the update listener; closed with a per-doc Promise-chain mutex (`#docLocks` + `#withDocLock`) serialising `open → mutate → capture → persist`. P2 rolled-back content visible to later reads — durable truth is `doc_updates`, server restart rehydrates Y.Doc from there; pre-hydration a naive "unload on abort" would wipe seed state placed by the same `ctx.transact` call (test fixtures break). Deferred to P3.6e which wires `onLoadDocument` replay + `SyncService.rollback(doc_id)` signal. P3 bootstrap ordering — fixed via writer auto-bootstrap + handler re-order. Pre-review listener-lifetime fix: update listener must span the full `fn` Promise, not just the sync callback, so post-await mutations aren't dropped. 171 tests passing across affected packages; `/codex:review` round 2 surfaced only P1 (captured as rebuttal in the commit body).
- **2026-04-18 (P3.5 commit 7/8 — Appendix A coherence activation + Codex F106):** the two long-deferred drift checks in `scripts/coherence.ts` replaced with real implementations. **Check 5**: every `CapabilityId("x.y")` literal under `packages/capabilities/src/**` (non-test) must have a row in architecture.md § Appendix A — asymmetric; Appendix A may list forward-looking capabilities, but an implemented one missing from the matrix is drift. **Check 6**: for each matched row, the rightmost `| Audit effect kind |` cell must either name the `read` family (category marker — reads audit as `audit.access_log`) or a `kind: "…"` variant in `packages/audit/src/effect.ts`. Catches the specific drift shape that would silently land at `{ kind: "internal" }` via `projectErrorAudit` (F95) without a compile-time signal. Probe output: 88 Appendix A rows parsed, 68 union variants, 3 implemented capabilities (`doc.create`/`doc.get`/`doc.list`) all resolve, coherence green. **Codex F106 P3 fixed pre-commit**: initial `kind:` regex also matched nested `subject_kind: "user"` / `index_kind: "fts"` property literals, which would have silently whitelisted "user" and "fts" as valid effect kinds — exactly the drift the check is meant to catch. Swapped to `(?<![A-Za-z_])kind\s*:\s*"…"` so only the top-level discriminator is parsed; probe confirms the two polluters drop with no true variants lost. Also collapses a pre-existing `+ "/"` concat to a template literal while in the file. Commit `cc52dfa`.

## Signals to check when resuming

- If `docs/architecture.md` exists and has content → Phase 2 is underway.
- If `package.json` / monorepo tooling exists → Phase 3 harness is underway.
- If route handlers / capability implementations exist → Phase 4 slices are underway.
- If security pass / load tests / runbook artifacts exist → Phase 5.

## Update protocol for future me

- At every phase boundary: update the "Current phase," "Immediate focus," and "Recent history" sections; commit with a message like `continuation: close Phase N, open Phase N+1`.
- Mid-phase, if context is about to be compacted and important nuance is at risk of loss, flush a short note to the "Recent history" section so it's visible on next load.
- When an "Open question" is answered, strike it through (do not delete — the record is useful) and add a note about what the answer was.
