# ADR 0040 — Tenancy & information-architecture model: Org → Space (membership ceiling) → Collection/Doc (per-doc ACL), with Personal spaces, item-scoped guest grants, publish as an independent dimension, and agents as peer grant-targets

**Status:** Accepted (new, 2026-06-01)
**Date:** 2026-06-01
**Deciders:** @numman, Claude Opus 4.8

## Context

editorzero already commits to three things that constrain how tenancy and sharing can work, and these are not negotiable:

- **Per-doc granular ACLs** (architecture §3.12; ADR 0015). The container is not the only source of access — a doc carries its own access decision.
- **One audit entry per mutation, and the audit log alone reconstructs persistent state** (AGENTS.md invariant 3; architecture §9.1 invariant 3a). Access state must be replayable from `audit_events`.
- **Humans and AI agents are peer principals** (ADR 0016; `packages/principal/src/index.ts`). Both flow through the same `PermissionGate`.

The third constraint is the sharp one. Agents mutate ACLs at machine speed. If "who can read doc X?" requires walking a whole sharing graph, that answer is un-auditable and un-cacheable at the rate agents move. The access model must keep that question a **local, provable lookup** — and the audit log must be able to reconstruct the answer at any past time T.

Today the codebase is **single-tenant-per-user and greenfield**. `workspaces` is the tenant (architecture §3.2); `workspace_members(workspace_id, user_id) → role` sources the role (ADR 0024; `packages/db/src/schema.ts` `WorkspaceMembersTable`); one workspace is auto-minted per signup (ADR 0024 §4; the `create-auth.ts` after-hook seeds both the `workspaces` row and the `workspace_members` `owner` row post-commit). Agents are workspace-scoped but are **not** members (architecture §3.4). The collection tree (architecture §3.5) and `docs.visibility ∈ {workspace, public, private}` exist; `doc.publish`/`doc.unpublish` are visibility-only today, and `published_slug`/`published_at` are **absent from the live schema** (verified — `DocsTable` has only `slug`, `visibility`, `visibility_version`). **This single-tenant shape is deliberate and permanent: editorzero is self-hostable / managed-single-tenant — multi-tenant SaaS (many customers on one database) is an explicit non-goal — so tenant isolation is an app-layer concern, not a shared-database one. This decides the RLS question (fork #4).**

A great deal of what Model B needs is **reserved scaffolding, not invention.** ADR 0024 deliberately reserved four evolution axes as "new ADR, new slice" (ADR 0024 Revisit triggers): multi-workspace + invites; **orgs-above-workspaces** (`organizations` table + nullable `workspaces.organization_id`); **teams-within-workspaces** (`workspace_teams` + `team_members` + an ACL-resolver layer in `PermissionGate`); platform-admin. Architecture §3.12 already sketches `doc_acls`/`collection_acls`; the capability matrix already reserves `permission.grant`/`revoke`/`list` rows; `packages/audit/src/effect.ts` already declares `acl.grant`/`acl.revoke`; `@editorzero/scopes` already carries `permission:grant`/`permission:revoke`. This ADR **cashes out the org + teams reserved axes** and pins the access algebra. It **extends ADR 0024 and supersedes nothing.**

> **What is genuinely new (only three things):** (1) the **ceiling rule** — it inverts §3.12's most-permissive union; (2) **Space** as a membership boundary distinct from the tenant; (3) **item-scoped guest grants** (and Teams-as-grant-targets, reserved). Everything else is repositioning.

The decision is grounded in how six products actually model this, live-verified 2026-06-01 across three research runs (Notion, Linear, cross-tool), and laid out visually in [`docs/ia/tenancy-models.html`](../ia/tenancy-models.html) (subtitled "decision brief for ADR 0040"; its section 05 maps the model onto the current schema):

- **Notion** — teamspaces (Open/Closed/Private) as a membership home, per-page ACLs that can narrow, page-level guests outside the teamspace, publish independent of edit. The closest prior art; we adopt its shape but make the ceiling **hard** (see Decision).
- **Linear** — teams as the boundary, private teams, shareable issues that can't be re-shared, no personal/draft space, no native publish. We borrow "shared item can't widen further" (bounded blast radius) and reject "no personal space."
- **Confluence** — spaces with space permissions as a true ceiling; cross-space sharing is **forbidden** (you must grant space access). We take the hard ceiling and **soften only via an explicit, audited guest grant** so cross-space sharing remains possible without dissolving the ceiling. (Confluence patched a CVE for silently applying the new parent's permissions on move — directly motivating our audited-move rule.)
- **Slack** — guests are *yours* (your directory, your audit, your revocation), not federation. Shapes the future guest-principal as a zero-membership directory entry, not a cross-tenant trust edge.
- **GitHub** — outside collaborators as item-scoped grants; org/team grouping. Shapes Teams-as-grant-target and item-scoped guest grants.
- **Google Drive** — shared drives + per-item shares; the universal footgun where moving across a boundary silently drops or changes shares. Motivates the explicit move-transition prompt.

## Vocabulary

One cohesive set (mirrors section 01 of `docs/ia/tenancy-models.html`). This is the control-plane vocabulary the whole product — API, CLI, MCP, Web UI — speaks.

- **Org** — the self-hosted deployment; the tenant root. **One by default.** Org is **conceptual** (= the deployment); the existing `workspaces` table stays the physical tenant-isolation root, and the Layer-2 scope column stays `workspace_id`. No `organizations` table — multi-org is a product non-goal (see fork #4 / Decision). Holds the member directory + all agents.
- **Member** — a human in the org directory; a full principal who can join Spaces & Teams. Maps to a Better Auth `user` + a membership row.
- **Agent** — an AI principal in the org (ADR 0016). Receives grants **exactly like a member** (peer), with its own attribution, rate limits, and revocation. Not a new principal kind for sharing — `AgentPrincipal` already exists.
- **Space** — a membership boundary inside the Org (a team's shared home) with a default access baseline. Types: **Open / Closed / Private**. The **hard ceiling** for ACL resolution. Implemented as a **new `spaces` table** FK'd to `workspace_id` — a row *within* the workspace, **not** a rename of `workspaces`.
- **Personal** — each member's private, owner-only-baseline Space where drafts live; promote by **moving** a doc into a shared Space (an audited cross-boundary transition). A `spaces` row of `kind='personal'` with `owner_user_id`. Auto-seeded at signup.
- **Collection** — a folder tree *inside* a Space. **In v1 a Collection is pure navigation: it carries no ACL of its own** (see the ceiling rule and blocker B1 below). The **existing `collections` table** (`CollectionsTable`), reparented under a Space via a new `space_id` column. Collection-level grants are a **reserved** extension.
- **Doc** — the leaf. Carries its **own ACL** (a narrowing of the Space baseline, or an item-scoped guest grant). The existing `docs` table.
- **Team** — a named group of members used as a **grant target**. **Reserved** for a later slice per ADR 0024; mocked in the members-admin screen but feature-gated until the schema + capabilities land.
- **Grant** — an ACL entry: (principal-or-Team) → role ∈ {owner, edit, comment, view} on a (Space/Doc; Collection reserved).
- **Guest grant** — an **item-scoped** grant to a principal *outside* the Space — **the only way to widen past the ceiling.** Enumerable, revocable, audited. A `grants` row with `is_guest=true`.
- **Publish** — an **independent visibility dimension** on a Doc (public-read link / published site), **orthogonal** to the ACL. A "guest editor" = a guest grant with `edit` on a published doc.
- **Guest principal (future)** — a directory entry that is *not* a member; sees only what it is explicitly granted. **Reserved.** v1-minimal modeling: a Better Auth `user` with zero Space memberships, so "sees only what it's granted" falls out of the grant resolver with no new principal kind.

## Options considered

### Option A — Flat Org + per-doc ACL

Everyone is a member of the one Org; collections are pure navigation; every doc carries an ACL and that ACL is the *only* access decision. No membership boundary between Org and Doc.

**Pros.** Dead simple; matches the per-doc-ACL commitment literally. Best for a solo user or a tiny team. Closest to GitHub-without-teams.

**Cons.** "What can Alice see?" becomes *walk every doc* — the exact whole-graph lookup the agent-speed constraint forbids. No team homes; large orgs drown in per-doc grants. No membership grouping to onboard/offboard against. No place for private drafts distinct from shared work.

**Rejected** — fails the locality constraint at scale and gives agents no bounded "who can read X" answer.

### Option B — Org → Spaces (membership ceiling) + Personal — **CHOSEN**

Org holds the member directory + agents. Spaces are membership boundaries with a default baseline (Open/Closed/Private). Collections are folders inside a Space. Docs carry per-doc ACLs that may only **narrow** within the Space. A Personal space per member holds private drafts. Cross-space sharing is an explicit, audited **guest grant**. Publish is an independent dimension. Agents and Teams are grant targets.

**Pros.** One model scales solo → small team → large org. "What can X see?" = the Space baseline (or, for a `private` doc, its allow-list) ∪ enumerable guest grants on X — **bounded and auditable** (indexed lookups, never a graph walk). Clean cross-boundary + guest + publish story. Maps onto the existing schema as a **repositioning, not a rewrite** (Notion-class, with Confluence's hard ceiling). Personal space gives drafts a home Linear lacks.

**Cons.** More concepts than Option A. The hard ceiling needs the guest-grant escape hatch so it doesn't feel rigid. Inverts the documented union algebra (a real, scoped amendment). The cross-space guest grant is the one row-type that deliberately crosses the Layer-2 scope predicate, requiring a carefully-chosen read path (see Decision → open structural forks).

**Chosen.** It is the only option that keeps the agent-speed locality constraint *and* serves all eight scenarios, and it consumes ADR 0024's reserved axes rather than inventing a parallel structure.

### Option C — Multi-workspace tenancy + federation

Keep `workspaces` as the hard tenant boundary; a solo user, a team, and a customer are each a separate workspace; cross-workspace sharing is federation (the reserved `workspace_trust_edges` axis, architecture §3.2).

**Pros.** Hard isolation by construction; least change from today's code. Right for agencies / true multi-customer isolation.

**Cons.** Solo→team and "share one doc out" get awkward — expanding a personal workspace into a team means spinning a new tenant and re-adding identity. Identity and agents duplicate per tenant. Over-built for a single self-hosted Org. Federation is a v2+ research problem (architecture §3.2 defers cross-workspace to v2+).

**Rejected for v1 as the *primary* model** — but **preserved as the reserved multi-org axis** (ADR 0024). Model B sits *inside* one workspace; if true multi-customer isolation is later needed, the `organizations` table layers above (each Org = one `workspaces` row family) without reshaping Model B.

## Decision

**Adopt Model B.** Concretely, for v1:

- **Org = the deployment (conceptual).** `workspaces` stays the physical tenant-isolation root and the Layer-2 / (future) Layer-3 scope column stays `workspace_id`. No `organizations` table. This preserves `TENANT_SCOPE_COLUMNS`, the `workspaces`-self-scoped special-case (`workspaces: "id"`), the Kysely `WorkspaceScopingPlugin`, the agent api-key `referenceId = workspace_id` binding (architecture §3.3), and every branded ID and capability noun **untouched**. (Resolved structural fork #1.)
- **Space = a new `spaces` table within the workspace**, FK'd to `workspace_id`, registered in `TENANT_SCOPE_COLUMNS` (scope = `workspace_id`), so it inherits Layer-2 isolation for free. (A drift guard already fails the build if a tenant table is added without an isolation test — so this registration is enforced, not merely conventional.) A Space is **not** a renamed `workspaces` row. Columns: `id (SpaceId)`, `workspace_id`, `kind ∈ {team, personal}`, `type ∈ {open, closed, private}`, `owner_user_id` (null for team spaces), `name`, `slug`, baseline-access, `created_by`, timestamps, `deleted_at`.
- **Collection = the existing `collections` table** + a new nullable `space_id` column (handler-enforced, no SQL FK — matching the existing `docs.collection_id` pattern). **Collections hold no ACL** (pure navigation).
- **Doc = the existing `docs` table**, ACL via the grants table below.
- **Grant = one polymorphic `grants` table** (resolved structural fork #3): `(id (GrantId), workspace_id, resource_kind, resource_id, subject_kind, subject_id, role, is_guest, created_by, created_at)`. **v1 enums:** `resource_kind ∈ {space, doc}` (`collection` reserved — B1), `subject_kind ∈ {user, agent}` (`team` reserved — H13), `role ∈ GRANT_ROLES`. This **supersedes the architecture §3.12 two-table `doc_acls`/`collection_acls` sketch** (union-based, `subject_kind = user|agent|role`). One table collapses "who can read X" to a single indexed query keyed on `(resource_kind, resource_id)`. The enums are **open for greenfield widening** (`collection`, `team`) — a widening, not a breaking change.
- **Grant lifecycle (resolved — H1).** Revoke is a **hard `DELETE`** (matching the §3.12 `UNIQUE(resource, subject)` shape); `grants` carries **no `deleted_at`**. Grant rows **persist through their resource's soft-delete** (inert while the doc/space is trashed because the resource isn't readable), so `doc.restore`/`space.restore` recover the exact grant set 1:1 for free. `doc.purge` hard-deletes a doc's grants and enumerates them in its preimage (so replay stays exact). Forward-replay of `acl.grant` then `acl.revoke` nets to no row — sufficient for invariant 3a; "undo a revoke" (which would need a preimage on the `acl.revoke` effect) is **reserved**, and grant state on restore is **state-as-of-delete** (matching ADR 0017's workspace-restore semantics).
- **No composite FK on `grants` (resolved — H6).** The repo's tenant-integrity pattern gives child rows a composite `(id, workspace_id)` FK so the scoping plugin can't be paired with a wrong-tenant `id`. A **polymorphic** `resource_id` cannot carry per-kind composite FKs. `grants` (and `collections.space_id`) therefore **forgo composite-FK integrity by design**; the compensating control is explicit: the read-only resolver + a **dedicated isolation-fuzzer property** asserts every grant's `resource_id` resolves within the same `workspace_id`. This is the highest-risk table (machine-speed agent ACL writes, deliberately cross-boundary guest rows), so the guard is named here, not left to "matches `collection_id`" (whose no-FK is itself a test-fixture concession). **Named write-time controls (Codex MEDIUM 5) — not only the read fuzzer:** the `grants` DDL carries from day one a **unique `(workspace_id, resource_kind, resource_id, subject_kind, subject_id)`** plus lookup indexes **`(workspace_id, subject_kind, subject_id, is_guest)`** (the "what can this principal see" path) and **`(workspace_id, resource_kind, resource_id)`** (the "who can see X" path); and the grant handler **validates the target `resource_id` exists within the same `workspace_id` inside the write transaction before insert.** The fuzzer catches drift after the fact; the uniqueness constraint + handler check keep most bad state from ever landing.
- **GRANT_ROLES = {owner, edit, comment, view}** as a new `as const` in `@editorzero/scopes`, **distinct** from the existing workspace `ROLES = {owner, admin, member, guest}`. Different vocabularies — conflating them is a real drift hazard, so contract tests pin GRANT_ROLES separately.
- **Grant subject is two flat fields, not a nested object (resolved — H12).** Capability inputs use flat sibling fields `subject_kind: z.enum(...)` + `subject_id: <branded string>` at the top level (the shipped `workspace.member_add` exemplar), **not** a nested `PrincipalRef {kind,id}` field. A nested object field does *not* trip the generators' top-level-ZodObject guard, so it would ship a broken `--subject` CLI flag and a misleading MCP arg silently. `PrincipalRefSchema` is reserved for internal/handler composition only.

### The ceiling rule (the crux)

Within a Space, a Doc's read set is bounded by **one of two access modes**, set by the Doc's `access_mode` (Collections hold no ACL — see B1) — narrowing below the baseline is a **doc-level mode switch, not a per-member subtraction** (resolved fork #5 — Codex HIGH 1). `access_mode` is the renamed, de-overloaded successor to `docs.visibility`: the reader-path slice (Step 5) splits the old `{workspace, public, private}` enum into `access_mode ∈ {space, private}` (read scope) + the orthogonal `published_slug`/`published_at` columns (publish) — see "Publish" below.

- **`access_mode='space'` (the default):** the Space baseline applies. Every Space member reads; a Doc grant may only **raise** a member's role (`view`→`comment`/`edit`/`owner`) — it never removes read. This is what "all members see all the Space's docs" means (scenario 3).
- **`access_mode='private'`:** the Space baseline does **not** apply. Readers are exactly the **owner** + holders of an explicit grant + guest grants. This is how a Doc is made more private than its Space (the "restricted doc," as in Notion).

**`owner(X)` is concrete (Codex precision):** `docs.created_by` is the **implicit, permanent owner** — always a reader/owner regardless of mode, never materialised as a `grants` row. Explicit `owner`-role grants are **additional** owners. Ownership *transfer* is a separate future capability/effect — it never happens by mutating `created_by` (that would corrupt audit-replay attribution).

The **only** way to grant a principal who is not a Space member is an explicit **Guest grant** on that item. Therefore:

> **who-can-read(X) = ( access_mode(X)='space' ? Space-baseline-members(X.space) : { created_by(X) } ) ∪ explicit-grants(X) ∪ guest-grants(X)**

This stays a **local, provable lookup** — indexed reads on stored rows, **never a recursive graph walk** — and is **representable with positive-only `GRANT_ROLES`** (no deny rows; see fork #5). The reconciliation, stated precisely so the resolver has a single spec and no surface re-derives it (invariant 5):

1. **Select the baseline term by mode.** `access_mode='space'` → the principal's Space-membership role → baseline access (and, reserved, Team-derived grants on the Space). `access_mode='private'` → no baseline; `created_by` is the sole implicit reader.
2. **Union** any explicit Doc grants for that principal (`is_guest=false`) — these **add or raise**, never remove: a positive role cannot drop a `'space'`-mode member below read. To exclude members, flip the Doc to `'private'`.
3. **Union** any guest grants on the item for that principal (`is_guest=true`).
4. The **cross-tenant hard-deny is unchanged and orthogonal** (architecture §8.3(a)): if `Principal.workspace_id ≠ AccessPath.workspace_id`, deny at Layer 1 before any of the above.

This **inverts** the documented resolution algebra. Architecture §3.12 and §8.1 Layer 1 describe a **most-permissive union** (`role_default ⊕ workspace_default ⊕ collection_acls ⊕ doc_acls`). Under Model B that becomes **mode-select-then-union** — the `'private'` mode (not a per-member subtraction) is how access narrows below the Space baseline. ADR 0015 and architecture §8.1/§3.12 are amended accordingly (see Relationship to existing ADRs).

> **Blocker B1 — why Collections hold no ACL.** If a Collection could carry a narrowing grant *and* a doc inherited its Collection's baseline at read time, then moving a doc between two differently-permissioned Collections **in the same Space** would silently change the doc's *resolved* access with **no stored-row mutation → no audit row → no prompt** — a silent privacy change the model forbids. v1 resolves this the smallest, provable way: **Collections are pure navigation and cannot hold grants**, so "move within a Space = no ACL change" is *literally* true. Collection-level grants are a reserved extension; if they land, a same-Space move that changes the resolved set becomes an audited transition like a cross-boundary move (below).

### Privacy invariant

Privacy tightens **downward**: a private Space cannot expose a Doc to the public **unless that Doc is explicitly published** (the deliberate, audited widening). No DB constraint expresses this — it is a **handler check** on `doc.publish` and a property test.

### Drag-drop / move = audited ACL transition

- **Moving WITHIN a Space** (reorder, or move between Collections in the same Space) = **no ACL change** — pure navigation, instant, no prompt. (True by construction: Collections hold no ACL — B1.)
- **Moving ACROSS a boundary** (Space↔Space, Personal↔Space) = an **explicit, audited ACL transition.** The Web UI **prompts**: *"adopt the destination Space's baseline"* vs *"keep this doc's own grants."* **Never silent.**

This rides the **existing `doc.move` capability** (verified metadata-only; its effect carries only `{doc_id, new_collection_id, new_order_key}` — zero ACL fields). The cross-boundary case extends the effect with a single `acl_transition` field `{adopt_baseline | keep_grants, before, after}` so it remains **exactly one audit row** (invariant 3) and the prompt's two-choice outcome is replay-deterministic. **Personal→Space promotion reuses this same branch** — not a separate capability. Splitting a move into two audit rows (move + acl) would break invariant 3.

### Publish as an independent dimension

Publish is decoupled from the access baseline. The reader-path slice (Step 5) **splits the overloaded `docs.visibility {workspace, public, private}`** into two orthogonal concepts: **`docs.access_mode ∈ {space, private}`** (the intra-Org read scope — the ceiling baseline above) and **publish**, carried by new `published_slug`/`published_at` columns. The old `'public'` value is **retired** — "published" is `published_at IS NOT NULL`, independent of `access_mode` (a `private` doc can be published; a `space` doc can be unpublished). A "guest editor" = a guest grant with `edit` on a published doc. Greenfield, so this is a clean enum split with **no backfill** (architecture §3.5). Codex flagged the prior "reinterpret `visibility`" phrasing as a generator/schema-drift hazard; the rename removes the overloaded name entirely.

### Agents as peer grant-targets

`Grant.subject` accepts `agent_id` exactly like `user_id` (`subject_kind = agent`). An agent's **effective doc access** is intended to layer as:

> `intersect(agent.scopes, delegator.permissions for acting_as)` ∩ who-can-read(X)  *(the mode-select-then-union read set above)*

**Honest status (corrected — H8): both conjuncts are unbuilt today.** `effectiveScopes()` returns an agent's `scopes` verbatim (no intersection with the delegator); `acting_as` is read **only for audit attribution**, never for narrowing; the gate docstring lists "agent↔delegator intersection for `acting_as`" as deferred. The ceiling term is likewise new. **Consequence for soundness:** until the intersection lands, a delegated agent holding `permission:grant` could escalate beyond its delegator. The `acting_as` intersection MUST therefore land **in or before** the `workspaceAwareGate` slice (the gate is the single composition point for both terms) — captured as a revisit trigger and a Step-6 gate. The `AgentPrincipal` type + gate branch exist; the `agents` table + api-key→agent resolver are a prerequisite slice before an agent can *use* a grant.

### Open structural forks — resolved here

1. **Org vs Space tenant-column (highest blast radius).** **Resolved: keep `workspace_id` as the physical tenant column; Space is a new table within the workspace.** Renaming `workspaces → spaces` would break all 7 scoped tables, branded IDs, `loadRoles`, the gate cross-workspace check, and every capability noun. No code-wide rename.
2. **Cross-space guest-grant visibility vs the `WorkspaceScopingPlugin` (highest risk).** **Resolved: resolve guest-granted items through a dedicated, audited read path on the system/unscoped handle**, **not** by widening the hot-path scope predicate to `(scope_col = scope OR id IN <guest set>)` — the latter reaches into the exact cross-tenant-leak class the isolation fuzzer exists to catch. **Honest scope (corrected — H3/H9/H10):** in v1 guest grants are within the **single** Org (one `workspaces` row), so "cross-space" = cross-`spaces`-row *within one tenant* — the plugin's `workspace_id` predicate does **not** block same-Org cross-Space reads. **Therefore the Space ceiling is enforced ONLY at Layer-1 (the resolver) in v1; Layer-2 is workspace-grained and provides no Space-level isolation.** It follows that **the resolver must be the SOLE read authority for every Space-scoped `list`/`get`/`list_grants`** (it must filter *reads*, not only gate mutations), and **every read path that bypasses the gate — the event-rendered published path (§5.4) and the git-mirror (ADR 0020) — must be made ceiling-aware (or scope-restricted) *before* per-doc ACL gates reads.** The dedicated guest read path is the forward-compatible, auditable shape and is gated behind Codex + Opus red-team as its own seam.
3. **Grants table shape.** **Resolved: one polymorphic `grants` table** (supersedes the §3.12 two-table sketch), so "who can read X" is one indexed query — with the explicit no-composite-FK fuzzer compensation above (H6).
4. **Postgres RLS Layer-3 — decision: not a committed layer; the app-layer floor is the enforcement design (updated per @numman, 2026-06-01).** Architecture §8.1 / ADR 0015 originally asserted a three-layer model, but **Layer-3 does not exist in code** (verified: zero `CREATE POLICY`, zero `ENABLE ROW LEVEL SECURITY`, no `app.workspace_id` GUC in `postgres-ddl.ts` *or* `postgres.ts`). Two facts decide it: **(a)** RLS keyed on `workspace_id` **cannot** enforce the boundary Model B actually introduces — cross-**Space** *within* an Org (Spaces share a `workspace_id`); guarding Spaces via RLS would mean re-encoding the whole ceiling resolver as Postgres policies — a divergent second copy of the most security-sensitive logic (violating invariant 5), and Postgres-only (SQLite has none). **(b)** **Multi-tenant SaaS — the classic trigger for DB-enforced tenant isolation — is an explicit product non-goal** (this is a self-hostable / managed-single-tenant product, not a shared-DB SaaS). **Therefore the enforcement design is the app-layer floor on _both_ backends — Layer-1 resolver-as-sole-read-path + Layer-2 `WorkspaceScopingPlugin` + the cross-tenant/ceiling isolation fuzzer (which asserts correct _results_, not merely zero rows) — built to provable completeness.** RLS is a **triggered-future** option, not a deferred-MVP cut; the trigger is opening a read path the app layer cannot mediate (raw-SQL / BI / external analytics). If it ever lands it must be co-designed with the guest-grant predicate so the two are one definition at two sites. ADR 0015 is amended accordingly.
5. **How narrowing-below-baseline is represented (Codex HIGH 1 — the fork the first draft left implicit).** **Resolved: a doc-level mode switch on `docs.access_mode` (`'space'` = baseline applies + grants may only raise; `'private'` = allow-list of `created_by` + explicit grants + guests), NOT a per-member deny row on `grants`.** `GRANT_ROLES` stays **positive-only**, so "who-can-read X" is a positive union — audit- and replay-friendly (a deny row would invert the meaning of every union and complicate 3a replay). The one capability this deliberately does **not** support: excluding a single member from an otherwise-`'space'` doc while everyone else keeps reading (this matches Notion, which flips the page to *restricted* rather than per-person deny; Codex concurred it is not worth a deny-precedence algebra without evidence of the need); the path is `access_mode='private'` + an explicit allow-list. **Deny / `effect ∈ {allow,deny}` grants are a clean future fork** behind that specific need.

## Scenarios → mechanism

Walking all eight scenarios from section 03 of `docs/ia/tenancy-models.html`:

1. **Solo user.** Sign up → auto-minted Org + **Personal** space (the signup hook extended to seed the Personal `spaces` row). Everything private to the owner. *Mechanism: Personal space.*
2. **Personal that expands into a team.** Create a **Space**, **move** drafts in. *Mechanism: cross-boundary `doc.move` (Personal→Space) with the audited ACL transition.*
3. **Small team: shared + private drafts.** The team works in one **Space** (all members see all its docs via the baseline); each member keeps a **Personal** space for private drafts. *Mechanism: Space baseline + Personal space.*
4. **Private doc shared with specific people.** Doc in Personal → add a **grant** to specific members (or, when they're outside the Space, a guest grant). They see just that doc; the ceiling stops sibling leakage. *Mechanism: item grant / guest grant.*
5. **Cross-space sharing.** Doc in Space A; share with someone in Space B (or in no Space) → **guest grant.** They do *not* join Space A; it appears in their "Shared with me." Enumerable, revocable, audited. *Mechanism: guest grant (`is_guest=true`), resolved via the dedicated audited read path (fork #2).* **You can share both within and across Spaces** — within = a grant inside the baseline; across = a guest grant.
6. **Publishing.** Flip the Doc's **publish** dimension → public read at a slug. Independent of who can edit. *Mechanism: reframed `doc.publish`/`unpublish` + `published_slug`/`published_at`.*
7. **Guest editors on a published doc.** A published doc needing outside editing → **guest grant with `edit`** to a specific external principal. Public still reads; the guest edits just that doc; the grant can't be re-shared (bounded blast radius). *Mechanism: guest grant (`edit`) ∘ publish dimension. Live-edit collab for an external guest inherits the three open WS-attach write blockers — see Evolution path.*
8. **Invite an org guest (future).** A **Guest principal** in the Org directory but not a member — sees only what it's explicitly granted. *Mechanism (reserved): a zero-membership Better Auth `user`; "sees only what it's granted" falls out of the grant resolver. Agents reuse the same scoping.*

## Invariant interactions

The model preserves every hard invariant. Where the supporting machinery is **not yet built**, this is stated plainly rather than assumed:

- **Invariant 3 (one audit row; audit reconstructs state).** Every grant/revoke/guest/space mutation emits exactly one `AuditEffect`; the cross-boundary move carries a single compound `acl_transition` effect, never two rows. **Load-bearing reality (corrected — H2):** the replay property test that §9.1 cites as the proof of invariant 3a **does not exist** — and neither does the reducer, the `apply()` function, the `PersistentWorkspaceState` builder, nor the arch-lint exhaustiveness rule. **Invariant 3a is currently unproven for the existing model, not just for Model B.** The `PersistentWorkspaceState` tuple also does not list `spaces`/`space_members`/`grants`. Sequencing is therefore non-negotiable: **build the replay engine + property test against today's ~50 effect kinds FIRST** (Step 2), then extend the tuple in the same commit as the tables (Step 4), then add each new effect with its reducer branch in the same commit (Step 7). Adding machine-speed agent ACL effects onto an unproven replay is the single riskiest ordering in the rollout.
- **Invariant 4 (capability parity on all surfaces).** **Honest status (corrected — H11): the four-way matrix is not mechanically enforced today.** `packages/contract-tests` (ADR 0033 §3) and the Web UI surface (`apps/app`) **do not exist yet**; the only parity test is CLI↔route (`apps/cli/.../parity.unit.test.ts`), plus an MCP self-consistency check. Declaring `surfaces:['api','cli','mcp','ui']` on a new capability today would pass every existing test while having **zero `ui` binding** — a silent hole. **v1 capabilities declare only the surfaces they actually bind (`api`/`cli`/`mcp`); the `ui` cell is added per-capability as the Web UI surface + the `contract-tests` harness land** (with task #13). The harness is a **hard prerequisite** for any honest `ui` parity claim.
- **Invariant 5 (no surface re-implements permission logic).** The ceiling rule lives entirely in the composed `workspaceAwareGate` — a single read-only resolver injected once at composition (the gate docstring already reserves exactly this). Surfaces and the dispatcher pipeline do not move. Because the ceiling is Layer-1-only in v1 (fork #2), the resolver must be the **sole read authority** for Space-scoped reads, or "who can read X" silently enumerates the whole tenant.
- **Invariant 6 (soft-deletes recoverable).** Space is a new deletable container. **Corrected (B2/H7): Space soft-delete does NOT cascade.** It **refuses on live descendants** — matching the shipped `collection.delete` (which refuses with a `HasLiveDescendantsError` counts payload rather than cascading, precisely to keep restore a 1:1 inverse and avoid N audit rows). `space.archive` refuses if the Space has live Collections/docs/members; the caller empties first; `space.restore` checks a live path (a `ParentDeletedError` analogue). One audit row per op (inv 3), bit-identical 1:1 restore (inv 6). Grants need no `deleted_at` — they ride with their resource (above), so a delete-restore cycle cannot silently change access.
- **Invariant 7 (content vs metadata write paths).** Every Model B mutator (`space.*`, `permission.grant/revoke`, guest grant, `doc.move`-across-space) is relational-metadata-only — no Y.Doc — so each joins `METADATA_ONLY_CAPABILITIES` and takes the dispatcher single-tx `withSystemTx` path. Zero `ctx.transact` calls. Extends the proven `doc.publish`/`collection.*` precedent (ADR 0018); no amendment needed.
- **Invariant 8 (agents are first-class).** Agents are peer grant-targets (`subject_kind = agent`), with their existing distinct rate limits, attribution, and revocation (ADR 0016). The agent's effective access is intended to intersect scopes with the ceiling — **both halves unbuilt today** (see Decision → agents-as-peer-grant-targets); the `acting_as` intersection is gated to land with the resolver.

## Relationship to existing ADRs

ADR 0040 **extends ADR 0024 and supersedes nothing.** It instantiates reserved scaffolding rather than inventing structure.

- **Extends ADR 0024 (workspace-membership-shape).** ADR 0024's Revisit triggers explicitly pre-authorize Model B's deltas (orgs-above, teams-within, invite-flow, platform-admin). ADR 0040 cashes out the **org + teams** axes in v1 form. **Vocabulary reconciliation:** ADR 0024's `workspace` is *not* renamed; Space is a new sub-table. The product **label** is "Space"; the API **path** and tenant column stay `workspace_id` (deliberate label-vs-path split — see Consequences). **First-release form:** Space (new table), Personal (auto-mint), per-doc ACL via `grants`, agents-as-grant-targets, publish-dimension. **Later in the order (still in the release):** Teams-as-target, Guest-principal. **Non-goal:** multi-org.
- **Amends ADR 0015 (permission-enforcement).** (a) Corrects the record: Layer-3 Postgres RLS is specified-but-**unbuilt** (zero `CREATE POLICY`); Layer-2 is the sole floor on both backends. (b) Inverts the Layer-1 algebra from most-permissive union to ceiling (narrow-then-guest-union). (c) States resolution stays a **local** lookup, replayable from audit, and that the resolver filters **reads**, not only mutations. The §8.3(a) cross-tenant hard-deny is unchanged.
- **References ADR 0016 (principal-model).** `Grant.subject` accepts `agent_id` like `user_id`. States the layered effective-access formula **and that both conjuncts are unbuilt** (the `acting_as` intersection must land with the resolver). The `agents` table + api-key credential + agent resolver are a prerequisite slice.
- **Amends ADR 0017 (soft-delete-recovery).** Adds Space as a deletable container that **refuses-on-live-descendants** (NOT cascade — aligning with `collection.delete`); `space.archive`/`space.restore` are 1:1 inverses with one audit row each. Grants need no `deleted_at` (hard-delete on revoke; ride with the resource on soft-delete); a cascading Space delete, if ever wanted, is a separate explicitly-designed capability with its own inverse story.
- **References ADR 0018 (write-path).** All Model B mutators are metadata-only → `METADATA_ONLY_CAPABILITIES`, single-tx `withSystemTx`. No amendment.
- **Amends ADR 0009 / capability matrix.** Reserved rows `permission.grant`/`revoke`/`list` are reconciled with the ceiling/guest model; new rows for `space.*`, `doc.add_guest`/`remove_guest`/`list_grants` land **with their capabilities (Step 8)**, each naming a real `AuditEffect` variant (coherence Check 6) added in the same commit, each declaring the surfaces it actually binds (invariant 4 — see the parity note above). `team.*` rows are reserved.
- **References ADR 0027 (web-ui-topology) + ADR 0030 (better-auth-mount).** Publish rides the event-rendered static published path (ADR 0027). External **guest-editor live collab** is write-capable collab and is gated on ADR 0030's three open WS-attach blockers (role-aware `readOnly`, Origin check, `onAuthenticate` revocation-freshness — task #15). Guest-**read** works on today's auth-free published path; guest-**edit** lands later in the order, gated on #15 (still in the release).
- **Amends ADR 0020 (git-mirror-export).** The mirror is mirror-ALL and ACL-unaware; under per-doc ACL it would leak private/guest-only docs. An export-scope decision (mirror-by-publish-state vs Space-baseline vs mirror-all) **must land before the resolver gates reads.** Reserved-future but flagged. The published-render path (§5.4) is likewise ACL-unaware and must assert no guest-only leak before per-doc ACL enforcement.
- **References ADR 0034 (schemas SSOT).** Every new capability gets one `@editorzero/schemas` module imported verbatim by both the capability and its route (verified pattern: `workspace/member_add`). `GRANT_ROLES` is a distinct `as const`. **Grant subjects are flat `subject_kind`/`subject_id` fields, not a nested `PrincipalRef`** (H12). Inputs stay flat `z.object().strict()` or the CLI/parity generators throw — a richer shape is an explicit 0034 revisit, not a silent hack.
- **References ADR 0035 / ADR 0039 (SPA scaffold / PWA).** The UI vocabulary lock must precede the shell scaffold (task #13). The reserved-prefix SSOT is `[/auth, /mcp, /collab, /infra, /docs, /collections, /workspaces, /audit]` (ADR 0035's reserved-prefix decision; ADR 0039 derives its offline service-worker denylist from that same list, with a security-relevant drift test). **Recommendation: UI label "Space" / client route `/spaces`, API path stays `/workspaces`** — stated once here so the rename does not churn the reserved-prefix SSOT, the PWA denylist, or `api-client`. New `/spaces`, `/permissions` prefixes are additive to that SSOT + its equality test.
- **References docs/adr/README.md + docs/continuation.md.** Add the ADR 0040 index row + a continuation entry. Coherence Check 1 requires `docs/adr/0040-*.md` to exist before any `.md` cites "ADR 0040" — so the ADR file lands first.

## Evolution & delivery path

These steps are a **build order toward one complete first release — not an MVP scope.** There is **no v1/v2 feature cut**: everything in Model B ships in the first release; the step number is the *sequence*, chosen so each lands on proven foundations. The repo is **greenfield** (ADR 0024 §3 "pre-production tree") — no production data, so every schema change is a clean-slate edit of the three lockstep files (`schema.ts` + `sqlite-ddl.ts` + `postgres-ddl.ts`) at one commit, **no backfill, no dual-write.** The 7 live tenant tables are untouched; everything Model B needs is additive. Bracketed tags mark **reversibility / parallelism / blocker-class**, not scope. Step 9 separates *later-in-the-order* work (still in the release) from **deliberate design choices** and **product non-goals** (multi-tenant SaaS, the `organizations` multi-org table).

**Step 1 — Author ADR 0040 (this doc) + control-plane amendments.**
Prose + schema-reshape only, no capability code. Settle the five structural forks (resolved above). Add dated amendment notes to architecture §8.1 / §3.12 (union→ceiling) and §9.1 (the `PersistentWorkspaceState` tuple gains the new tables when they land). Create the ADR file + README row + continuation entry; commit `docs/ia/tenancy-models.html` as the source-of-record visual. Run `pnpm coherence`. **Mandate Codex cross-model review** on the ceiling resolution + the workspace→Space blast-radius (ADR-level + control-plane). *Must land before the #13 shell scaffold commits its sidebar/route tree.* Fully reversible (docs only).

**Step 2 — Build the audit replay engine + `replay.prop.ts` against TODAY's effect union.**
This is **not a pure test addition (H2)** — it builds the reducer + `apply()` + `PersistentWorkspaceState` builder from zero for the current ~50 effect kinds, proving invariant 3a on the known-good model (`foldl(apply, ∅, allow-rows by created_at) ≡ live`, both SQLite and Postgres). Establish the ratchet **before** any ACL effect lands on it. Optionally stub the arch-lint exhaustiveness rule. Reversible.

**Step 3 — Additive foundations, zero behaviour change.**
Add `SpaceId`/`GrantId` brands (`parseV7`; `TeamId` lands with the Teams slice; **no `OrgId` — multi-org is a non-goal**). Add `SUBJECT_KINDS` entries `space`/`grant` (`team` lands with Teams); `GRANT_ROLES` + `ACCESS_MODES = {space, private}` as-const; new `METADATA_ONLY_CAPABILITIES` entries (`permission:grant`/`revoke` scopes already exist). Add `@editorzero/schemas` shared modules (`GrantRoleSchema`, new ID schemas; `PrincipalRefSchema` internal-only). Registry untouched, so `default-registry.unit.test.ts` stays green. Reversible.

**Step 4 — NEW-TABLE schema slice across the 3 lockstep files at ONE commit.**
`spaces`, `space_members`, `grants` (single polymorphic) — each registered in `TENANT_SCOPE_COLUMNS` (scope = `workspace_id`). **Teams tables are not in *this* slice (H13)** — `teams`/`team_members` land later in the order (Step 9 list). `collections` gains nullable `space_id`. Add the **schema↔DDL↔TENANT_SCOPE_COLUMNS coherence check in this same commit**, and **fix coherence Check 7's index regex (H5)** to match `CREATE [UNIQUE] INDEX`, capture+diff the `WHERE` predicate, and record UNIQUE-ness — otherwise the new partial-unique indexes are invisible to SQLite↔Postgres parity. `grants` lands with its **unique `(workspace_id, resource_kind, resource_id, subject_kind, subject_id)` + the two lookup indexes (Codex MEDIUM 5)** in this same commit. Purely additive — non-breaking on both backends. Reversible.

**Step 5 — Reader-path slice (folds in publish-orthogonality). [parallel-safe]**
ADD-COLUMN `published_slug`/`published_at` on `docs` + the partial unique index `ON (workspace_id, published_slug) WHERE published_slug IS NOT NULL AND deleted_at IS NULL` (both dialects, guarded by the fixed Check 7). **Split `docs.visibility` → `access_mode ∈ {space, private}` + the orthogonal `published_*` columns** (the de-overloading rename — Codex precision; clean greenfield enum split, no backfill). Outbox→render consumer. **Must NOT enable Space-scoped listing before Step 6** (the resolver is the sole read authority — fork #2/H9): publish filtering is orthogonal, but any *Space*-scoped `doc.list` waits for the resolver. Reversible.

**Step 6 — Build `workspaceAwareGate`. [BLOCKER-class]**
Compose `scopeOnlyGate` (unchanged) ∩ a **read-only** ACL/ceiling/guest resolver — **local lookup only, never a graph walk** — that filters **reads** (`doc.list`/`get`/`list_grants`), not only mutations. Add `space_id` (and likely `collection_id`) to `AccessPath`. **Commit to the F88 post-parse `PermissionDeniedError` handler-deny channel as the mechanism (H4/H10)** — it is already proven (rolls back + writes one deny audit row) — rather than the unbuilt `AccessPath`-derivation codegen; guest cross-space reads bypass the gate's `workspace_id`-equality assertion by construction and flow through the dedicated audited read path (fork #2) **outside** the gate, red-teamed as its own seam. Land the **`acting_as` ∩ delegator intersection** here too (the gate is the single composition point — H8). Build/extend the cross-tenant fuzzer (architecture §8.1a, itself unbuilt) with the ceiling, privacy, the `resource_id`-resolves-within-`workspace_id` property (H6), and the ACL-audit-replay property. **Gate behind Codex + Opus red-team.** Does not block #13 or #15.

**Step 7 — Extend `AuditEffect` union + replay reducer + fixtures TOGETHER, one family per commit.**
Append-only — get post-state shapes right the first time. Add `{space_id}` to the `acl.*` scope union; `space.create/update/archive/restore`, `space.member_add/remove/update_role`; the guest-grant marker; the `doc.move` `acl_transition`. Each carries **post-state (a SET, not a delta)**. Coherence Check 6 gates union-vs-matrix; the replay test gates replay-sufficiency. Amend the §9.1 tuple in lockstep. Effectively irreversible.

**Step 8 — Ship capabilities + routes.**
Order: `permission.grant/revoke/list` first (already reserved), then `space.*`, then `doc.add_guest`/`remove_guest`/`list_grants`, then the `doc.move` cross-boundary branch. Each = `registerCapability` + `EXPECTED_IDS` sibling + one `@editorzero/schemas` module (flat `z.object().strict()`, imported verbatim) + a hand-written route mirroring `member_add.ts` + a `METADATA_ONLY_CAPABILITIES` entry + a matrix row. **Surfaces declared = those actually bound** (`api`/`cli`/`mcp`); `ui` added per-capability as the shell + `contract-tests` harness land (H11). CLI/MCP derive for free. Extend the signup hook to seed the Personal space. Each capability independently reversible.

Two **hard ship-gates** on `permission.grant/revoke` (promoted from prose caveats — Codex HIGH 4 / MEDIUM 6), not advisory:

- **Three-green gate (HIGH 4):** the capability ships only when **all three** are green *together* — the audit-replay property (Step 2), the resolver isolation property (Step 6), and the `acting_as` ∩ delegator intersection (Step 6). Replay proves state is *reconstructable*; it does **not** prove the online resolver *reads* safely. Replay alone is insufficient.
- **Side-channel gate (MEDIUM 6):** no read-gating ACL capability ships while **any** export/render path can still enumerate docs *without* the resolver — the event-rendered published path (§5.4) and the git-mirror (ADR 0020) must be ceiling-aware or scope-restricted first (the Step-9 export-scope decision, pulled forward as a precondition). Otherwise the app is correct and the side-channel leaks. **The audit read surface is on this list too** (Step-6 red-team): `audit.list`/`audit.get` require only `workspace:admin`, and audit effects carry doc titles/slugs — once Step 7/8 narrows admin doc-reads, the audit log would still reveal a ceiling-denied doc's title, existence, and activity to any admin. Before the first read-gating ACL ships, decide explicitly: audit is **see-all-by-design for compliance** (documented, defensible — audit is the forensic record and admins are the audience) **or** audit projections get ceiling-filtered. **DECIDED 2026-06-12 (cross-model concurrence — Codex Step-7 exchange): see-all-by-design.** Audit is a privileged forensic plane, not the document-read plane; ceiling-filtering it would blind an admin to exactly the private-space / guest-grant / exfiltration events the log exists to explain. Conditions that make this defensible, binding on future slices: (1) `workspace:admin` stays the floor on `audit.list`/`audit.get` — audit-read **never widens to `member`**; (2) personal-space privacy means privacy from *peers and ordinary document reads*, **not** from the Org's forensic record — product copy and docs must say so; (3) if product pressure ever demands softening, the shape is **presentation-layer redaction** of content-like fields (titles/slugs) in the UI, never resolver-filtering the audit capabilities themselves — `audit.*` stays resolver-independent in v1.

**Step 9 — Later in the build order, deliberate design choices, and product non-goals.**
No MVP cut — the first bucket is *in the release*, just sequenced after a named dependency; the second is settled design; the third is out of the product.

***Later in the order*** (in the complete release, each its own ADR + slice):
- **`teams`/`team_members` + `team.*` + `team` as a `grants` subject_kind + the Teams UI** — after core `spaces`/`grants` (H13).
- **Guest-principal** — a non-member org-directory entry (Slack-style), after item-scoped guest grants.
- **External guest-*editor* live collab** — gated on WS-attach hardening (task #15). Guest-*read* on published docs works today; guest-*edit* inherits the WS-attach write blockers, so it sequences *after* #15 — do not fork #15.
- **The git-mirror + published-render export-scope decision** — already a **hard precondition** (the Step-8 side-channel gate) before any read-gating ACL ships; not optional.

***Deliberate design choices, not deferrals*** (stay unless evidence flips them):
- **Collections hold no ACL (B1)** — pure-nav keeps same-Space moves audit-free; collection-level grants would reintroduce the silent-move problem and need the audited-transition machinery.
- **Positive-only grants, no per-member deny (fork #5)** — `access_mode='private'` + allow-list covers the need; a deny-precedence algebra is a clean future fork behind evidence (Codex concurred).
- **Postgres RLS is not a committed layer (fork #4)** — the app-layer floor is the design; RLS is triggered-future (raw-SQL / BI / external query access), never the Space-boundary enforcer.
- **Ownership transfer is a separate future capability/effect** — never a `created_by` mutation.

***Product non-goals*** (not this product):
- **Multi-tenant SaaS** (many customers, one shared DB) and the **`organizations` multi-org table.** editorzero is self-hostable / managed-single-tenant; monetisation, if any, is managed *single-tenant* deploys, which is not multi-tenancy. Cross-Org federation (`workspace_trust_edges`) stays a research axis, not a release goal.

**Web UI shell (task #13) interaction.** The shell builds the sidebar + Spaces grid + Collections tree **first**, binding to the existing `workspace.*`/`collection.*`/`doc.*` capabilities — it proceeds independently. The **only** hard ordering against #13 is the Step-1 vocabulary lock. Deferred to later slices (bind to unbuilt caps): the per-doc Share+Publish dialog, sidebar drag-drop + the cross-boundary ACL-transition prompt, the "Shared with me" route, Personal-space + space-type affordances, and the (feature-gated) Teams UI. Note: the Owned Tree (ADR 0037) now needs drag-reorder in v1 (was a revisit trigger) — budget it larger or re-weigh the native-Tree fallback. None of this blocks the #13 scaffold itself.

## Consequences

**Easier.**
- "Who can read doc X?" is a bounded, indexed, audit-reconstructable lookup — safe at agent speed. This is the whole point.
- Solo → team → large-org scales on one model; Personal space gives drafts a first-class home.
- Cross-boundary moves become reconstructable events, not silent leaks.
- The model is a repositioning of the existing schema; the 7 live tenant tables are untouched and additivity makes "non-breaking on both backends" trivially true.
- Agents and Teams are grant targets with no new principal kind and no parallel permission system.

**Harder.**
- Two access algebras now exist in the docs (union vs ceiling); the §8.1/§3.12 amendment must be precise or the resolver loses its single spec (invariant 5).
- **The model rests on enforcement machinery that does not exist yet** — the audit-replay engine (inv 3a), the cross-tenant + ceiling fuzzer, the four-way contract-tests harness, and the `acting_as` intersection are **all unbuilt**, for the *current* model as well as Model B. This ADR is honest about that and sequences each as a hard gate (Steps 2, 4, 6, 8). The biggest risk is treating any of them as already-done.
- The guest grant is the one deliberately cross-boundary row-type; its read path (fork #2) is the highest-risk new code and is gated behind Codex + Opus red-team. In v1 the ceiling is Layer-1-only, so the resolver must be the sole read authority and the published-render + git-mirror paths must be made ceiling-aware before reads are gated.
- The label-vs-path split (UI "Space" / API `/workspaces`) is permanent, deliberate cognitive overhead, stated once here to prevent a code-wide rename.

## Revisit triggers

- **Multi-customer isolation becomes a real product need** → build the reserved `organizations` table (Org becomes physical); revisit whether the scope column stays `workspace_id` or gains `organization_id`.
- **Teams ship** → land `teams`/`team_members` + `team.*` capabilities + un-gate the Teams UI; widen `grants.subject_kind` to include `team`; confirm resolution in the gate and the fuzzer.
- **Collection-level grants are needed** → widen `grants.resource_kind` to include `collection` and make a same-Space move that changes the resolved set an audited transition (the B1 escape hatch).
- **The ceiling proves too rigid** (guest grants become the common case) → re-evaluate a soft-baseline mode behind an explicit, audited, still-local widening — only if "who can read X" stays bounded.
- **Postgres RLS is implemented** → co-design the guest-grant predicate as one definition across Layer-2 and Layer-3; flip ADR 0015's Layer-3-unbuilt note.
- **The `acting_as` ∩ delegator intersection slips past the resolver slice** → stop: a delegated agent with `permission:grant` can escalate beyond its delegator until it lands (H8).
- **Guest-principal (non-member directory entry) is needed** → extend the ADR 0016 Principal union; until then the zero-membership `user` modeling holds.
- **External guest-editor live collab is requested** → gated on WS-attach hardening (#15); sequence after, do not fork.
- **The flat-ACL `AccessPath` derivation can't be made local** (a doc-level decision needs a graph walk) → stop and redesign before enforcing the ceiling on reads; the locality guarantee is the model's load-bearing assumption.
- **A cross-model review (Codex, at this ADR boundary) rejects the ceiling algebra or the guest read path on grounds this ADR doesn't anticipate** → revisit.

## Amendments

**2026-06-12 — Step 4 LANDED.** `spaces` / `space_members` / `grants` across the three lockstep files at one commit, `collections.space_id` (nullable, no FK — B1), `TENANT_SCOPE_COLUMNS` + the §9.1 `PersistentWorkspaceState` tuple extended in the same commit (the line-167 sequencing). Shapes this ADR left open, resolved at landing:

- **`space_members.role` speaks `GRANT_ROLES`**, not the workspace `ROLES` vocabulary. The ceiling rule ("a doc grant may only *raise* a member's role") compares membership roles against grant roles, so they must be one ordered set; a second ladder would force a lossy mapping at every comparison.
- **`spaces.baseline_access ∈ {edit, comment, view}`** (CHECK excludes `owner`): defined as the implicit `GrantRole` an `open` space confers on Org members holding no membership row. `closed`/`private` confer nothing implicitly. The Step-6 resolver is the sole consumer.
- **kind↔owner tie is a table CHECK** — `(kind = 'personal') = (owner_user_id IS NOT NULL)`, identical text in both dialects — plus the `spaces_personal_unique` partial index (≤1 live personal space per member; the Step-8 signup seeding relies on the constraint, not convention).
- **`grants.is_guest` is `INTEGER` 0/1 in BOTH dialects** (Kysely type `0 | 1`) rather than a Postgres-side BOOLEAN — dialect-uniform reads, and Check 7 string-compares constraint text. Hard-DELETE lifecycle (H1) is honored at the DDL level: no `deleted_at`/`updated_at` columns exist to misuse.
- **The Step-4 coherence check landed twice as strong as specified**: the `Database`-keys ↔ `TENANT_SCOPE_COLUMNS`-keys leg is a compile-time `satisfies Record<keyof Database, "workspace_id" | "id">` on the map itself (a typed-reachable-but-unscoped table now fails tsc), and coherence **Check 11** covers the legs types can't see — schema interfaces ↔ DDL tables/columns, and scope-column existence in DDL. All three script legs negative-tested at landing.
- **`CollectionState.space_id` is deliberately NOT in the replay projection yet**: no effect can carry it until the Step-7 family, and a projected column no effect sets would fail replay the moment a fixture touched it. It joins the projection in the same commit as the effect that mutates it (the Step-7 lockstep rule, documented in `packages/audit/src/state.ts`).
- **`SPACE_KINDS` / `SPACE_TYPES`** joined the Step-3 vocabulary in `@editorzero/scopes` with exact-membership pins beside the `GRANT_ROLES` pin.

**2026-06-12 — Step 5 LANDED.** The de-overload split shipped across schema + DDL (both dialects) + schemas + audit + capabilities + every surface in one commit: `docs.visibility {workspace,public,private}` → `access_mode ∈ ACCESS_MODES` (read scope; **not caller-settable** — `doc.create` hardcodes `'space'`, the mode switch is the Step-8 ACL capability, and both `access_mode` and the retired `visibility` key are strict-rejected at the input boundary) + the orthogonal `published_slug`/`published_at` pair, with `docs_published_slug_unique ON (workspace_id, published_slug) WHERE published_slug IS NOT NULL AND deleted_at IS NULL` under the fixed Check 7. `visibility_version` renamed `render_version` (architecture §3.5 pre-authorized the Step-5 decision). Per the H9 guard, **no Space-scoped listing shipped** — `doc.list`/`doc.get` stay resolver-ungated until Step 6. Decisions resolved at landing:

- **Mint semantics.** `doc.publish` mints `published_slug` from the doc's internal `slug`, collision-suffixed `-2`/`-3`/… against the **live published set** (`deleted_at IS NULL` mirrors the partial index — a trashed doc's URL is reusable by design). The mint SELECT runs inside the dispatcher's write-path tx: SQLite's single writer serializes outright; a Postgres same-candidate race is backstopped by the unique index (loser surfaces through the error projection, never a double-allocated URL). The atomicity property (api-server `metadata-only-atomicity`) pins the tx at exactly 6 queries with the two mint SELECTs *inside* — moving them out re-opens the race and fails the baseline.
- **Idempotent re-publish keeps BOTH halves.** A second `doc.publish` keeps the existing `published_slug` (a live public URL must never rotate under its readers) **and** the original `published_at` (first-published is a fact, not a counter); only `render_version` bumps (F5 always-bump). Replay-proven: the effect carries the handler-computed pair (the `doc.rename`-slug contract), so the reducer applies carried values rather than re-deriving.
- **`doc.soft_delete` CLEARS the publish pair; restore does NOT republish.** A trashed doc must leave the public site immediately, and an admin restoring from trash must never surprise-republish — republishing is a deliberate `doc.publish`. Clearing at delete time also releases the URL and eliminates the restore-time collision class (restore needs no mint path). `doc.unpublish` on a trashed doc stays 404 (the pair is already cleared; a version-bump-only 200 would be a no-op lie).
- **Outbox event renamed** `doc.visibility_changed` → `doc.publish_changed`, payload `{doc_id, published_slug, published_at, render_version}` (nulls = "no longer served"), keyed on `render_version` so a forwarder can reject out-of-order rows without coordination. Emission stays publish/unpublish-only; delete/restore ride `audit.appended`.
- **Render consumer + public route stay deferred** — they need the §12 jobs runner, the §7 markdown projection, and an anonymous principal, none of which exist. The Step-5 deliverable is the complete publish *data model* + the `doc.publish_changed` seam the renderer subscribes to. Same documented-deferral posture the original publish slice used.
- **Web UI mapping**: the doc-list chip labels the read scope (`access_mode`, vocabulary-locked "Space") while the `st-pub` green modifier tracks `published_at IS NOT NULL` — the two dimensions render orthogonally, matching the model.

**2026-06-12 — Step 6 LANDED (Codex + red-team gated; findings applied before commit).** The ceiling went live as TWO composed seams, exactly per H4/H10: gate-side, `workspaceAwareGate({ loadDelegatorRoles })` in `@editorzero/dispatcher` (production-wired in `getApiApp`, reusing the auth resolver's `createLoadRoles` — one role source, two consumers) lands the **`acting_as` ∩ delegator intersection** (H8): a delegated agent's effective scopes are `agent.scopes ∩ roleScopes(delegator)` resolved against the LIVE membership row at check time; a missing row denies with the new structural `DenyReason` kind **`delegator_not_member`** (revocation cuts outstanding tokens instantly; never reported as a misleading `missing_scope`). Handler-side, `loadDocReadResolver` (`packages/capabilities/src/acl/ceiling.ts`) is the sole read authority — ≤ 4 single-table plugin-scoped SELECTs, pure in-memory predicate, F88 `PermissionDeniedError` with the pre-reserved `acl_deny` reason (403 + deny audit row). `AccessPath` gained inert `space_id`/`collection_id` (forensic; the resolver derives placement from rows, never the caller's claim). Decisions resolved at landing:

- **NULL-space = workspace-legacy baseline; broken refs fail closed.** Root docs and unspaced collections (the entire pre-Step-7/8 universe) read as the pre-Spaces world — every workspace principal — so Step 6 landed with **zero observable narrowing** on live data (all 191 pre-existing doc-handler tests passed untouched); narrowing activates as Step 7/8 populate Spaces. A dangling `collection_id`, dangling `space_id`, or soft-deleted space is an ANOMALY, not a legacy state: creator + doc-grants only. Anomalies never widen a read set.
- **The ceiling formula as implemented**: `created_by` always reads (implicit permanent owner — both modes, unconditional); explicit doc-grants (guest or not) read in both modes; `'private'` mode is creator + doc-grants ONLY (space grants and baselines are baseline-tier reach — private exists to cut that tier); `'space'` mode adds space-membership, space-grants, and the open-space Org baseline. The open baseline is **user-subjects only** — agents are principals, not Org members. **No workspace-role bypass**: owner/admin are ceiling-denied like anyone (scenario 3's personal-space privacy is only real if it holds against admins; proven end-to-end — an OWNER 403s on a closed-space doc in the auth-chain suite).
- **Delegated agents evaluate the ceiling as their delegator** (memberships, grants, created_by — the whole identity); the agent's OWN grants are ignored while delegated, since `agent ∪ delegator` would read MORE than the human it acts for — the same intersection posture as the gate's scope term. A non-delegated agent evaluates as itself: `subject_kind='agent'` grants, no memberships, no baselines. **The two axes are deliberately asymmetric (Step-6 red-team — record it so nobody mistakes one for the other): the gate INTERSECTS scopes; the ceiling does NOT intersect rows.** A delegated agent claiming only `doc:read` still reads its delegator's FULL readable row set — the scope intersection bounds *which capabilities* fire, never *which rows* they reach. All row-level safety of delegation therefore rests on `acting_as` being an authenticated, delegator-consented claim. That is fine today (no agent can authenticate — there is no agents table); the **agent-auth slice inherits two binding obligations**: gate `act.sub` issuance on verified delegator consent, and add a fuzzer arm proving a delegated token never exceeds the delegator's row set even when the agent holds richer grants. Relatedly, the resolver is role-blind by design — a `guest`-ROLE workspace user currently gets the open-space Org baseline like any member; whether v1 wants "workspace guests read only explicit grants" must be confirmed-or-fixed before Spaces populate (Step 7/8, aligns with the guest-principal revisit trigger).
- **Reads AND mutations are gated.** `doc.get` asserts after its 404 (trash stays invisible — no existence leak to denied callers) and before `ctx.transact` (no Y.Doc opens for a denied caller); `doc.list` FILTERS (a list is a projection of the readable set; per-doc deny rows would be noise). All seven doc mutations assert post-fetch: the two content-path handlers (`update`, `rename`) added a ceiling pre-read BEFORE their first write (plain auto-commit handle — a late deny could not roll back), proven by side-effect tripwires in the wiring suite. **collection.* stays ungated until the Step-7/8 slice that makes collections space-linked** (all live collections are NULL-space; recorded obligation, includes the collection.delete cascade surface).
- **Placement-changing writes are gated too (Codex review, two HIGHs applied pre-commit).** The resolver carries a placement-authority API beside the read predicate — `placementOf(collection_id) → legacy | space(id) | anomaly` (the single binding spec of the bucket distinction) and `canPlaceIn` = the BASELINE-reach term only (membership / space-grant / open-space user baseline; `created_by` and doc-grants are doc-scoped and there is no doc yet). **`doc.create` asserts `canPlaceIn(collection_id)`** after the target's 404 check and before any write: without it, an outsider with `doc:write` could plant a doc inside a closed Space's collection and become its permanent implicit owner — a write-side hole through the hard ceiling. Root creates skip the resolver load entirely. **`doc.move` enforces a bucket-transition rule**: only legacy → legacy and same-space → same-space survive; every cross-bucket move (closed-space → root would WIDEN to the whole workspace-legacy baseline; root → space narrows; space → other-space re-homes) throws `acl_deny`, after the target 404, off the already-loaded resolver (zero extra queries — the metadata-only pin is unchanged at 10). Anomaly placements are immovable and untargetable, even for the creator. The Step-8 ladder + `acl_transition` audit field REPLACE this blanket prohibition with judged transitions; until then cross-bucket placement simply cannot move, which can never widen a read set. Pre-Spaces data is all-legacy, so both gates land with zero observable narrowing.
- **403-vs-404, decided**: ceiling-denied on a LIVE doc projects 403 `acl_deny` + a deny audit row — the deny trail is the point of the F88 channel; same-Org existence reveal is accepted and documented. Order is always 404-first (missing/trashed), then ceiling.
- **Locality is structural**: a doc's Space is ONE denormalized hop (`doc.collection_id → collections.space_id`); the resolver refuses to walk `parent_id`, which makes "every collection row carries its space_id denormalized" a **binding obligation on the Step-7/8 placement capabilities**. Soft-deleted collections still BIND (restore evaluates the trashed row's stored placement).
- **Metadata-only tx pin moved 6 → 10**: the four ceiling preload SELECTs run inside the dispatcher tx by construction (ctx.db IS the tx handle there) — snapshot-consistent with the row being judged; the atomicity property now also pins one SELECT each on `collections`/`spaces`/`space_members`/`grants` in-tx. Content caps and reads preload on the plain handle.
- **Fuzzer (resolver-grade §8.1a slice) landed**: 60 seeded two-tenant worlds per commit, resolver-vs-independent-oracle equality over every subject × doc, H6 cross-tenant grant garbage in both directions (including the sharpest case: a host-subject grant row sitting in the FOREIGN tenant pointing at a host doc — catches any lost workspace scoping on the grants preload), plus an oracle-independent privacy sweep. **Mutation-tested at landing**: dropping the grants `subject_id` match and breaking the delegated-identity rule were both caught at seed=1. **Deferred, recorded**: the ACL-audit-replay property (no capability can mutate grants/spaces yet — lands with the Step-7 effect family) and the full §8.1a capability-matrix × dual-driver × 1k-rounds fuzz (tracked for Step 8, where the grant capabilities materialize the attack surface).
- **Guest grants in v1 read through the same union term** (same-workspace; the plugin never blocks them) — fork #2's dedicated audited cross-Org read path stays reserved, unbuilt.
- **Review trail (the gate this step was committed behind).** Codex: two HIGHs (the `doc.create` placement hole and the `doc.move` cross-bucket widening — both applied above) + one MEDIUM (H8 was production-wired but not composition-proven — closed by seam tests injecting the REAL `workspaceAwareGate` + REAL `createLoadRoles(driver)` into `createApiDispatcher` with a synthetic delegated principal: `delegator_not_member` deny row, guest-delegator scope intersection surfacing the INTERSECTED set, member-delegator allow with dual-identity attribution). Codex explicitly kept: 403-vs-404, delegated-as-delegator, no admin bypass, anomaly fail-closed. Red-team (frontier subagent, full slice + substrate read, tests re-run independently): verdict LAND, no today-exploitable findings; its audit-side-channel and scopes-vs-rows items are folded in above; its move-source-only restatement was closed by the Codex HIGH-2 fix before commit.

**2026-06-12 — Step 7 LANDED (three commits, one family each, per the step text).** The full ACL effect family is in the union with reducer branches + fixtures + semantic walks, all BEFORE any capability can emit them — the unit walks are the replay-sufficiency proof until Step 8 wires the integration property. Commit A: `space.create/update/archive/restore`, `space.member_add/remove/update_role`. Commit B: `acl.grant`/`acl.revoke` + `{space_id}` joined the `acl_deny` scope union. Commit C: the `doc.move` `acl_transition` + the collection `space_id` placement lockstep (the Step-4 amendment's deferred bullet, now closed). Shapes this ADR left open, resolved at landing:

- **`kind` is the union discriminant, so the spaces row's `kind`/`type` columns ride `space.create` as `space_kind`/`space_type`**; the reducer maps them back to the `SpaceState` row fields. `space.update`'s patch is the mutable subset ONLY — `{name, slug, space_type, baseline_access}`: `space_kind` is structural (team↔personal is a different entity, not an edit) and `owner_user_id` is pinned by the personal-space CHECK, so neither is patchable; replay applies post-state values, the Step-8 capability judges transitions.
- **`space_members` + `grants` replay as hard-DELETE projections** (H1, matching the Step-4 DDL): `space.member_remove` / `acl.revoke` REMOVE the projection key — add-then-remove nets to no entry. `acl.revoke` therefore carries the FULL row preimage (resource, subject, role, guest marker): the row is gone, so the audit body is the only durable forensic record of what existed.
- **`acl.grant`/`acl.revoke` were reshaped, not appended — and that is legitimate**: both kinds were classified `deferred` (no transition written) and NO shipped capability has ever emitted them, so no persisted audit row carries the old sketch shape; append-only constrains *emitted history*, not reserved names. The landed shape mirrors `GrantState` exactly — flat `resource_kind ∈ {space, doc}` / `resource_id` (B1's table shape supersedes the sketch's nested scope union), `subject_kind ∈ {user, agent}`, the `GrantRole` ladder, `is_guest: 0|1` (the guest-grant marker the step text names), `created_by`. Re-grant under the same `grant_id` converges (role-change posture); a future distinct-kind `acl.update_role` would be a clean append if richer forensics are wanted.
- **`doc.move` gained OPTIONAL `acl_transition` `{policy: adopt_baseline|keep_grants, before_space_id, after_space_id, dropped_grant_ids}`** — the §7 compound effect: a cross-boundary move stays exactly ONE audit row (invariant 3). `dropped_grant_ids` is the exact set of doc-grant rows the handler hard-deleted under `adopt_baseline` (empty under `keep_grants`); replay removes precisely those keys. Absent on every same-bucket move — i.e. on every move the shipped handler performs, since the Step-6 transition rule denies the rest; the Step-8 cross-boundary branch is the sole future emitter.
- **The placement lockstep closed in one commit (C), as the Step-4 amendment required**: `CollectionState.space_id` joined the §9.1 projection in the same commit as the effect fields that mutate it — `collection.create` += `space_id` (handler writes the column explicitly; null until Step 8 adds space-targeted creation + parent inheritance) and `collection.move` += `new_space_id` (echo of the row's binding; the shipped handler never rewrites it). Reducer-side, `collection.move` rebinds the WHOLE subtree by BFS over the projection's own parent links: the denormalization invariant (a descendant always carries its root's binding) makes the single `new_space_id` sufficient post-state for every descendant — no per-descendant effect payload, and subtree propagation is unit-proven before any capability can emit a non-null binding. Capability outputs echo the new fields through the ADR 0034 SSOT schemas; the dispatcher replay-property's DB projection reads `collections.space_id` so live-vs-replayed equality now covers the column.
- **All nine kinds are born `"state"`** with transitions + `STATE_KIND_FIXTURES` entries (the fixtures `satisfies` a total record over the state kinds, so a future kind without a fixture fails tsc; every fixture must apply to `EMPTY_STATE` without throwing). Coherence Check 6 verifies matrix→union for IMPLEMENTED capabilities only, so effect kinds landing ahead of their capabilities is green by design; Appendix A already maps `permission.grant`→`acl.grant`, `permission.revoke`→`acl.revoke` for Step 8.
- **Review trail (post-landing Codex pass, inside the reshape window).** Two HIGHs, both the hard-delete-preimage class, both applied before any emitter exists: `acl.revoke` gained `workspace_id` + `created_by` (it claimed "full forensic preimage" while omitting two `GrantState` fields — a revoke row must answer *which workspace* and *who originally created the grant* without the envelope or prior replay) and `space.member_remove` gained `workspace_id` + the removed `role` (the remove is enough to net the projection but was not the durable record of *what* membership was removed). The reducer ignores the preimage fields — removal stays by key. One MEDIUM: architecture §16.3's hand-mirrored `AuditEffect` union had drifted (six stale shapes) — **the mirror is retired**; §16.3 now points at `packages/audit/src/effect.ts` as canonical and keeps only the architectural contract (replay classes, effect-carries-everything, post-state-not-deltas, forcing functions). Codex explicitly kept: the `space.update` mutable subset, the grant-family reshape legitimacy, `dropped_grant_ids`-only (no survived-grants list) on `acl_transition`, and the subtree-derive replay contract (a Step-8 handler updating descendants non-atomically is exactly the bug replay-vs-DB should catch). The audit-read fork was decided in the same exchange — see the Step-8 side-channel gate above.

**2026-06-12 — Step 8 slice 1 LANDED (two commits): the granting-authority ladder + `permission.grant`/`permission.revoke`, the FIRST `acl.grant`/`acl.revoke` emitters.** Commit 1 widened `loadDocReadResolver` with the authority surface (`canAdministerDoc`/`canAdministerSpace` + F88 asserts, `hasBaselineReach`); commit 2 shipped the two capabilities end-to-end (ADR 0034 schemas, registry, hand-written `/permissions/*` routes, CLI/MCP by registry derivation, replay-property extension). `permission.list` follows as slice commit 3. Decisions this ADR's step text left open, resolved at landing:

- **The granting-authority ladder (the §6 "who may grant" rule, now code).** Doc owner-tier = `created_by` OR a non-guest `owner`-role doc grant — guest grants confer NOTHING (scenario 7, re-share-proof). Else by placement: legacy → workspace owner/admin backstop; space → space owner-tier (PERSONAL: `owner_user_id` ONLY — the scenario-3 privacy pin holds against admins; TEAM: owner-role membership ∨ non-guest owner-role space grant ∨ admin backstop); anomaly → owner-tier only (fail closed). The admin backstop is USER-principal-only: the resolver cannot see a delegator's workspace roles, so it fails closed for every agent principal — a delegated agent administers exactly what its delegator's owner-tier reaches, never via backstop. A latent bug fell out of deriving the ladder: the personal-space OWNER had zero baseline reach (no `space_members` row exists by construction — the CHECK pins `owner_user_id`), so the signup-seeded Personal space would have denied its own owner doc placement; `baselineReach` gained the structural owner term.
- **L1 scope is coarse; the ladder is the bound.** `ROLE_SCOPES.member` += `permission:grant`/`permission:revoke` — same layering as `doc:write` + the placement gate: the scope says the verb exists, the resolver says on which resources. Guests keep neither.
- **The user-subject standing rule (what `is_guest = 0` MEANS).** A non-guest doc grant is a within-standing refinement, so `permission.grant` requires a user subject to (a) hold a LIVE workspace membership and (b) for space-placed docs, have baseline reach into that Space *as the subject* — evaluated by loading a subject-side resolver and calling the SAME `hasBaselineReach` predicate the read ceiling uses (no parallel standing predicate to drift). Failing either is the GUEST flow: a typed `ValidationError` (`subject_not_workspace_member` / `subject_lacks_space_standing`) routes to `doc.add_guest`, which mints the explicit `is_guest = 1` escape hatch. Legacy placements need membership only (pre-Spaces baseline = whole Org); ANOMALY placements also need membership only — there is no LIVE ceiling to cross, and the owner-tier caller able to administer one shouldn't be blocked from sharing it while the placement awaits repair. Space-resource grants need membership only: a space grant IS how space standing is conferred.
- **Agent subjects are exempt from membership/standing — and that is the honest posture.** Agents are not Org members and carry no baseline; a grant is precisely how an agent acquires resource access, and `is_guest = 0` stays truthful because no baseline ceiling is being crossed. **Recorded obligation:** no `agents` table exists yet, so agent-row existence is NOT validated — a granted-but-nonexistent agent id is an inert, revocable row; when the agents slice lands, `permission.grant` gains the same-tx agent-existence pre-check (symmetric with H6's resource check, which DID land: the resource SELECT runs inside the dispatcher write tx).
- **Upsert semantics on the unique edge** `(workspace_id, resource_kind, resource_id, subject_kind, subject_id)` — one row per edge; guest-ness is a property of the edge, not a parallel row. Existing guest edge → 409 `ConflictError` (silently flipping `is_guest` 1→0 would erase the audited escape-hatch marker; lifecycle belongs to `doc.add_guest`/`remove_guest`). Existing non-guest edge, same role → idempotent echo, ZERO writes, `acl.grant` re-emitted under the same `grant_id` (replay upsert converges — the `doc.publish` idempotency posture). Different role → UPDATE `role` only under the same `grant_id`; `created_by`/`created_at` are IMMUTABLE (attribution never transfers). Fresh edge → INSERT `is_guest = 0` wrapped in `ON CONFLICT (edge) DO NOTHING`; zero-row returns project to 409 (the member_add Branch-C posture — the global mapper deliberately never projects raw unique-violations).
- **Trash/orphan postures are verb-asymmetric BY the predicates' design.** `permission.grant` on a missing OR soft-deleted resource → 404 FIRST (trash-invisible, the `doc.get` posture: granting new access against trash is refused without confirming existence; restore, then grant). `permission.revoke` on a TRASHED DOC's edge is ALLOWED (offboarding: trimming a trashed doc's ACL must not require restoring it — the grant would resurrect on restore; `assertCanAdministerDoc` evaluates the trashed row's stored placement, the `doc.restore` precedent). A TRASHED SPACE's edge is NOT revocable — `canAdministerSpace` structurally never administers soft-deleted spaces → `acl_deny`, restore-first (the predicate needs the live row's kind/type/owner to evaluate at all; never bypass the authority predicate for convenience). ORPHAN edges (resource row hard-gone — out-of-band damage; `doc.purge` enumerates + deletes its grants) → 404 on the RESOURCE, edge stays inert until repair.
- **`permission.revoke` refuses guest edges** (typed `ValidationError` → `doc.remove_guest`) — mirror of the grant-side 409: the guest lifecycle owns its bookkeeping. No reachable dead-end: `doc.add_guest` hasn't shipped, so no guest edge can exist yet.
- **Replay obligation closed.** The dispatcher replay property now drives grant → role-converge (same `grant_id`) → idempotent re-grant (allow row, zero writes) → hard-DELETE revoke → a surviving second edge through the REAL dispatcher with replay-vs-DB deep-equality after every step — and the surviving edge outlives its subject's `member_remove` (grants persist through membership soft-delete; revocation is explicit, the L1 gate already cuts a removed member's access).
- **Audit subjects:** `permission.grant` → the RESOURCE (`resource_kind` is a `SubjectKind` member by construction; "what was shared" is the forensic pivot, the grantee rides the effect); `permission.revoke` → the GRANT (`{kind: "grant", id}`). Outputs carry the full row (revoke = full preimage, the H1/Codex-HIGH-1 posture) + `created_at`; the effects mirror `GrantState` exactly (no timestamp — replay projections exclude it).
