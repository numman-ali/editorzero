/**
 * Scaffold placeholder. Replaced by the shell layout (Base UI + Meridian Zero
 * tokens, ADR 0037/0036) and the file-based router (ADR 0035 §4) in the next
 * #13 increments.
 */
export function App() {
  return <main>editorzero</main>;
}
